# Handoff — AYR Steel ERP (Siguiente Sesión)

> Subir SIEMPRE al inicio: este HANDOFF + CLAUDE.md (v6.87.0).
>
> ⚠️ **ESTADO — RE-MEDIDO 2026-08-31 (TANDA 3, C2, read-only contra `ayrsteel-2026`).**
> **La línea metallic sigue SIN MATERIA PRIMA, pero ya NO está vacía en ventas ni en stock.**
> Sigue en 0: **bobinas aluzinc** (38 coils, 38/38 `GALV`, `masterWidth` 1200 en 38/38),
> **`production_logs` de la línea** (143 logs, 143/143 `drywall`) y **`scrap_logs`** (0).
> Ya NO está en 0: **`metallic_roofing_stock` = 14 docs (los 14 con `quantity` NEGATIVA)**,
> **`metallic_roofing_stock_movements` = 26**, y **`sales` = 105 docs** (86 `COMPLETED` +
> 19 `QUOTATION`, las 19 con prefijo `COT-`). Son el residuo de la carga del 2026-08-28
> ya descrito en `CLAUDE.md` §11 v6.76.0 (upsert sin guard: un SKU sin doc de stock
> arranca en 0 y la primera venta lo manda a negativo) — **no es carga nueva de agosto**.
> El borrado FÍSICO de `[ALUZINC-RESET]` (2026-08-27) fue real y sigue vigente para
> materia prima y producción; el texto anterior de este banner declaraba 0 en stock,
> movimientos y ventas de la línea, y eso quedó stale al día siguiente de escribirse.
> Los datos de agosto 2026 siguen SIN cargar. Todo doc que describa bobinas aluzinc,
> WAC de metallic o cola de producción de la línea sigue describiendo un sistema
> SIN MATERIA PRIMA.
> ✅ **DECISIÓN DE NEGOCIO (2026-08-31, v6.84.0): los 14 docs de `metallic_roofing_stock`
> con `quantity` negativa y `baseCost:0` SE QUEDAN ASÍ.** Comportamiento ACEPTADO,
> consistente con `[STOCK-NEG-GUARD]` (v6.77.0) — no hay backfill, no hay anulación, no
> se toca prod, no se re-mide. **No re-abrir sin pedido explícito del dueño.**
>
> **BASELINE (2026-08-31, v6.86.1) — TANDA 7.1 (remediación de la Tanda 7):**
>   - **La premisa que motivaba la tanda quedó REFUTADA por medición, y por eso NO se tocó una línea de código.** Se sostenía que el assert (c) de U1.2 ("aceptar NO mueve stock") era **solo por conteo** y por lo tanto ciego a un `update` in-place sobre `metallic_roofing_stock`. **Las 2 mitades cayeron:** el test también compara CONTENIDO del doc de stock (`quotationClientAccepted.integration.test.ts:109` / `:126-127`, `toEqual`), y `metallicRoofingStockStrategy.writeSaleDecrement` **también** crea un doc nuevo en `metallic_roofing_stock_movements`, así que el conteo sí se mueve.
>   - **M3, la mutación que faltaba: espejo mínimo de `approveQuotation:321-343` dentro de `markQuotationAccepted`.** Predicciones opuestas escritas ANTES de correr — director: **(c) VERDE**; ejecutor: **(c) ROJO, `1 failed / 3 passed`**. **Medido: ROJO**, `AssertionError: expected { metallic_roofing_stock: 1, …(3) } to deeply equal { metallic_roofing_stock: 1, …(3) }` — y el rojo cae **en el assert de conteo**, el que la premisa daba por ciego. Blast radius correcto: los 2 guards tiran antes de escribir y el control positivo (`cancelQuotation`) no pasa por el camino mutado. Revertida, `git diff --stat` vacío, 0 residuos.
>   - **La reescritura del assert (R2) NO SE EJECUTÓ, por instrucción explícita del propio enunciado ante el caso ROJO.** El assert (c) queda **sin tocar**: ya discriminaba.
>   - **3 imprecisiones del DIRECTOR, las 3 refutadas con conteo por término y por archivo (B20)** — ver `CLAUDE.md` §11: la premisa de (c)-por-conteo; la cita `"RED contra el mecanismo"` (**0 hits en los 3 docs**); y "el control positivo no se declaró" (**sí se declaró**, `CLAUDE.md:35`, nombrando GRUPO K y con justificación). **1 omisión REAL del ejecutor de la Tanda 7:** no caracterizó su RED de U1 (`is not a function`) como RED DÉBIL.
>   - **NO se crea `B21` — decisión explícita** (mismo criterio que el `B21` descartado en v6.85.0). Las reglas `B` siguen en **20**.
>   - **Fila #49 NUEVA `[RULES-GUARD-CAMPO-FANTASMA]`**, registrada sin atacar (ver COLA). `firestore.rules` NO se tocó.
>   - **Custodios: delta CERO predicho y medido, antes y después** (el árbol de código volvió byte-idéntico a HEAD): `npm run test` **126/1326** exit 0 · `test:emu:rules` **8/131** exit 0 · `test:emu` **38/248/3skip (251)** exit 0 · `check:tsc` **VERDE** · `check:docs` **19 ROTO / 5 saltados**.
>   - **Cero write a los 2 entornos** (todo contra el emulador), cero push, cero `master`, cero deploy, cero `firestore.rules`, cero `firestore.indexes.json`. `firebase use` verificado CRUDO al abrir: **`ayrsteel-test`**.
>
> **BASELINE (2026-08-31, v6.86.0) — TANDA 7:**
>   - **`[QUOTATION-APPROVE-UNREACHABLE]` (#1) CERRADO EN SU TRAMO DE CÓDIGO.** El estado de aceptación del cliente pasa a ser alcanzable: campo ADITIVO (`clientAccepted` + `clientAcceptedAt`), `status` INTACTO, escritor único `markQuotationAccepted`, botón en `/admin/quotations`. **Aceptar NO es vender** — decisión (1) del dueño, asserteada contando colecciones antes/después, no confiada a la lectura del código. 4 commits, uno por ítem.
>   - **U0 fue un GATE, y por eso va primero.** Todo el diseño colgaba de una premisa que la Tanda 6 (T0.5) DERIVÓ por lectura y nunca midió: que `fieldsUnchanged` de `firestore.rules:101-104` es denylist de 7 campos y no allowlist de claves. **MEDIDO: pasa.** Con 3 controles negativos, incluido el par discriminante exacto (`subtotal` y `clientAccepted` son las 2 claves que el builder real NO escribe; la única diferencia es el NOMBRE — si `subtotal` pasara, U0.1 estaría pasando por el motivo equivocado). `firestore.rules` NO se tocó.
>   - **2 mutaciones en U1, las 2 discriminan, control positivo verde en ambas, revertidas:** M1 (`clientAccepted` sin `clientAcceptedAt`) → 1 failed/3 passed, rojo exacto sobre el timestamp. M2 (invertir la condición de idempotencia) → 2 failed/2 passed, y `cancelQuotation` queda VERDE (blast radius correcto).
>   - **Custodios, por separado (B4), delta PREDICHO antes de correr y coincidente 1:1 en las 3 (B2):** `npm run test` 126/1313 → **126 archivos/1326 tests** (+13, 0 archivos nuevos) · `test:emu` 37/244/3skip (247) → **38/248/3skip (251)** (+1 archivo/+4) · `test:emu:rules` 7/127 → **8/131** (+1 archivo/+4). `check:tsc` VERDE antes y después. `check:docs` 19 ROTO/5 saltados, sin moverse. `npm run build` exit 0, **52 rutas** (no agrega rutas).
>   - **Fila #48 NUEVA (U3), registrada sin atacar:** el mensaje del guard de aborto pide "resolvé la percha" y la UI no ofrece ninguna acción para hacerlo sobre una percha `CANCELLED` importada. Medido post-U2.
>   - **Cero write a `ayrsteel-2026`** (ni lectura hizo falta), cero write a `ayrsteel-test` (todo contra el emulador), cero push, cero `master`, cero deploy, cero `firestore.rules`, cero índices.
>   - **Tip de REFS: NO se transcribe (regla C0).** Al abrir: `develop` = `49ce1335`, `origin/develop` = `47a6603b` (**el dueño NO pusheó la Tanda 6 — 1 commit sin publicar al arrancar**), `master` = `origin/master` = `aeb724b5`, divergencia INTENCIONAL. `firebase use` → `ayrsteel-test` (B11, crudo).
>
> **BASELINE (2026-08-31, v6.84.0) — TANDA 5 AUTÓNOMA:**
>   - **1 ítem de COLA CERRADO: `[IMPORT-PERCHA-ARCHIVE]` (#47).** Es la mitad que `[IMPORT-OVERWRITE]` (v6.83.0) dejó fuera a propósito: percha `QUOTATION` sin producción activa ahora se ARCHIVA a `sales/COT-{doc}/history` + `audit_logs` `QUOTATION_REPLACED` (acción propia, no reusa `SALE_REPLACED`) ANTES de pisarse, en vez de pisarse en silencio. El guard de aborto de la Tanda 4 (CANCELLED/producción activa) NO se tocó. Commit único sobre `src/core/import/runSaleImportTransaction.ts` + el test nuevo. Detalle completo en la fila #47 de esta COLA y en `CLAUDE.md` v6.84.0.
>   - **2 correcciones de DOCS (E0), sin código.** (E0.3) la sub-vineta de cierre de #18 en v6.83.0 no aclaraba que `[IMPORT-OVERWRITE]` solo cerró la rama de aborto — corregida para remitir a #47. (E0.4) decisión de negocio grabada en los 2 banners de estado sobre los 14 SKUs negativos (se quedan así, no re-abrir) + regla de negocio sobre `[QUOTATION-APPROVE-UNREACHABLE]` (aprobar = evento real, no código muerto) — la fila #1 YA estaba bien clasificada, solo se agregó la regla con fecha. (E0.2) grepeado "elimina la clase entera de error"/"blindaje"/"garantiza" contra `[CHECK-TSC-SCRIPT]`: **no existe ese texto en ningún doc** — nada que corregir, reportado como medido.
>   - **1 recon read-only (E2), cero código: `[QUOTATION-APPROVE-UNREACHABLE]` (fila #1) actualizada con alcance medido y fecha.** Inalcanzable por UI Y por lógica (ver fila #1). Población en los 2 entornos: prod 19 `QUOTATION`/0 nativas, test 24 `QUOTATION`/1 nativa (`C-000040`). Coordenadas `:82`/`:104`/`:199-200` sobrevivieron la re-verificación sin corrimiento — primera vez desde v6.73.0.
>   - **Write a `ayrsteel-test`: CERO** (no hizo falta — el RED se armó por seed dentro de la transacción del emulador, no por round-trip contra el proyecto real). **Cero write y cero lectura a `ayrsteel-2026`** salvo las 2 queries read-only de E2.2 (con assert de `projectId`). Cero push, cero `master`, cero deploy, cero `firestore.rules`, cero índices.
>   - **Custodios del cierre, reportados POR SEPARADO:** `npm run test` → **126/1313/0, exit 0** (sin cambios — el archivo nuevo es de integración, fuera de su scope). `test:emu` → **37 archivos / 244 passed / 3 skipped / 0 failed (247), exit 0** (+1 archivo/+2 tests sobre v6.83.0, delta PREDICHO antes de correr y coincidente 1:1). `npm run check:tsc` → **0 errores, exit 0**, antes y después. `check:docs` → sin ROTO nuevo sobre el baseline (ver cierre). `test:emu:rules`/`test:emu:functions`/`npm run build` NO corridos (esta tanda no tocó `firestore.rules` ni agregó rutas).
>   - **2 scripts npm TEMPORALES para filtrar sin salir del wrapper** (`test:emu:percha`/`test:emu:percha:inner`, patrón v6.69.0), usados para RED, GREEN, M1, M2 y la corrida combinada con `salesImportOverwrite.integration.test.ts`. Removidos antes del commit — `git diff --stat package.json` vacío confirmado.
>   - **REGLA NUEVA B19** (ver `CLAUDE.md` §11): un fixture que no reproduce la forma del doc de PRODUCCIÓN falla por la razón equivocada, y ese fallo se parece a un RED legítimo — mismo mecanismo ya sufrido por la Tanda 4 con `firestore.rules:101`, reaplicado acá reusando el mismo patrón de seed con `buildImportWrites`.
>   - **E3 — REMEDIACIÓN de esta misma tanda: E1/E2 quedaban bien, E0 tenía una medición fabricada y 2 imprecisiones sin persistir.** `QUOTATION_REPLACED` grepeado (5 hits en `src/`, 0 en el resto) → ya implementado Y ya asertado en el test (`salesImportPerchaArchive.integration.test.ts:151-154`); verificado igual con M3 (saltear SOLO el audit, dejando `history` intacto) → **1 failed/4 passed**, rojo exacto en `auditSnap.size`. E0.1 ya estaba persistido (premisa "no ejecutada" incorrecta). E0.5 del prompt original (2 coordenadas de COLA fabricadas de memoria) nunca se había escrito como imprecisión — corregido en la fila #47 y en esta entrada. El propio reporte de cierre de E0.2 midió mal: dijo "0 hits" para "blindaje"/"garantiza" cuando eran 1/1 y 3/2 contra el snapshot real de v6.83.0 (la conclusión sobrevivió, la medición no). **REGLA NUEVA B20** (ver `CLAUDE.md` §11): un grep se reporta con el conteo crudo por término y por árbol, nunca agregado en un "0 hits". Sin código nuevo commiteado — `git diff` vacío en `runSaleImportTransaction.ts` y `package.json` antes de cerrar.
>   - **3 ítems de COLA CERRADOS, un commit por ítem. Las 3 filas (#12, #13, #46) BORRADAS de la COLA.** `[TEMPLATE-REGISTRY]` (#13, `4bb2e29c`) · `[CHECK-TSC-SCRIPT]` (#46, `7b88c0d5`) · `[IMPORT-OVERWRITE]` (#12, `dd495283`). Detalle completo en `CLAUDE.md` v6.83.0.
>   - **El ítem de la tanda es `[IMPORT-OVERWRITE]`, y su cierre CONFIRMA por la vía positiva que `[IMPORT-EXTRACT]` desbloqueaba esto.** El guard que le faltaba a la percha `COT-*` está puesto: `tx.get(quoteRef)` en la fase de lecturas, y si la percha existe y está `CANCELLED` **o** tiene producción ACTIVA, se lanza y **aborta la transacción entera**. RED crudo contra el módulo extraído (`expected undefined to be 'MARCADOR_DE_CANCELACION_REAL'`), GREEN 3/3, 2 mutaciones que discriminan las 2 (`2 failed / 1 passed` cada una, control positivo verde en ambas).
>   - **Lo más caro de la tanda no fue el fix, fue el FIXTURE.** El primer RED falló con un rechazo de rules en vez del pisado. Causa: el `allow update` de `sales` en `firestore.rules` exige `fieldsUnchanged([...7 campos financieros])`, así que una percha sembrada a mano sin snapshot financiero es rechazada **antes** de llegar al guard. Y ése no es el escenario real: las 2 víctimas (`COT-FFA1-1255`/`COT-FFA1-1250`) eran re-importaciones del MISMO comprobante, con snapshot idéntico. **El fixture se rehízo con el builder real.** Un fixture que no reproduce la forma del doc de producción falla por la razón equivocada, y ese fallo se parece a un RED legítimo.
>   - **ALCANCE DECLARADO de `[IMPORT-OVERWRITE]`: se implementó SOLO la mitad de aborto de la decisión de v6.76.0.** Una percha `QUOTATION` sin producción se sigue pisando. La otra mitad ("archivar a `history` y pisar") queda en la COLA como **`[IMPORT-PERCHA-ARCHIVE]` (#47, NUEVO)**, con su propio RED pendiente. ✅ **CERRADA en TANDA 5 (v6.84.0) — ver #47 en la COLA.**
>   - **`simulateImport` NO se tocó, y la divergencia CRECIÓ — registrada, no unificada.** El guard vive ahora solo en la función real: es el **7º punto de divergencia**, sumado a los 6 medidos en C2. Ver #18.
>   - **`npm run check:tsc` es el custodio nuevo de tipos.** La trampa del pipe deja de depender de que alguien recuerde la regla. Medido: `npx tsc --noEmit | tail -5` reporta exit **0** con un `TS2322` vivo; el script sin pipe reporta **1**, idéntico al `tsc` pelado. Canalizado, el script sigue devolviendo el exit de `tail` (semántica de shell, se declara en vez de venderse como blindaje) **pero el `tail -5` ahora muestra `TSC_VEREDICTO: ROJO`** en vez de ruido — que es exactamente lo que faltó en v6.74.0 y v6.81.0.
>   - **Write a `ayrsteel-test`: CERO** (el permiso de round-trip no se usó; E2 corrió contra el EMULADOR). **Cero write y cero lectura a `ayrsteel-2026`.** Cero push, cero `master`, cero deploy, cero `firestore.rules`, cero índices.
>   - **Tip de REFS: NO se transcribe (regla C0, v6.70.0 — verificar con `git rev-parse develop origin/develop master origin/master`).** Al abrir la tanda: `develop` = `3740f1cb`, `origin/develop` = `56499464` (**el dueño NO pusheó la Tanda 3 — 2 commits sin publicar al arrancar, 5 al cerrar**), `master` = `origin/master` = `aeb724b5`. Divergencia de `master` INTENCIONAL. `firebase use` → `ayrsteel-test` (B11, reportado crudo).
>   - **Custodios del cierre, reportados POR SEPARADO (regla B4, prohibido sumarlos):** `npm run test` → **126 archivos / 1313 tests / 0 skip, exit 0** (+1 archivo / +16 tests sobre v6.82.0, delta PREDICHO antes de correr y coincidente 1:1 — regla B2). `test:emu` → **36 archivos / 242 passed / 3 skipped / 0 failed (245), exit 0** (+1 archivo / +3 tests, delta PREDICHO y coincidente 1:1; los 239 del baseline documentado quedan intactos). `npm run check:tsc` → **0 errores, exit 0**. `check:docs` → **19 ROTO / 5 saltados**, idéntico al baseline de apertura, antes y después de las ediciones. `test:emu:rules` y `test:emu:functions` NO medidos (esta tanda no tocó `firestore.rules` ni `functions/src`). `npm run build` NO corrido: el guard no agrega rutas.
>   - **⚠️ ERROR DE PROCESO DEL EJECUTOR, auto-detectado y declarado:** la corrida baseline de `test:emu` se arrancó a las `02:20:06` y el árbol se editó a las `02:22:04` y `02:24:37`, con la medición EN CURSO. Esa corrida quedó **VOID** (podía estar midiendo una mezcla de árbol viejo y nuevo, indistinguible de una medición limpia), se mató, se limpió el huérfano del 8080 con el `CommandLine` verificado, y se rehízo la secuencia con el árbol quieto. **Regla que queda: una corrida de custodio empieza con el árbol quieto y el árbol no se toca hasta que termina.**
>   - **Baseline de apertura, nota de proceso (idéntica a la de la Tanda 3):** `git status` volvió a mostrar `quotationsFetch.integration.test.ts` como modificado y volvió a ser **byte-idéntico a HEAD** — `hash-object --no-filters` = blob de HEAD = `87b0333d`, `git diff` y `git diff --cached` vacíos, `i/lf w/lf`. Stat-dirty puro de Windows. **No se trató como baseline sucio.**
>
> **BASELINE (2026-08-31, v6.82.0) — TANDA 3 AUTÓNOMA:**
>   - **El resultado principal es que `[QUOT-FETCH-RANGE]` NO EXISTE, y nunca existió.** Instrumentada la función REAL y **impresos** los 2 límites en runtime (C1): el inferior es `'COT-'` (`charcodes [67,79,84,45]`, length **4**); el superior es `'COT-'` (`charcodes [67,79,84,45,`**`63743`**`]`, length **5**), con `BOUNDS_IDENTICAL: false`. `63743` = `0xF8FF` = **U+F8FF**, el sentinela canónico de Firestore para query de prefijo. **La query siempre funcionó.** Medido: E1 (1 doc `COT-VOID-1` VOIDED, con la fuente de status aportando 0) → la de rango devuelve **1**; E5 (2 `COT-*` + `INV-A`/`V-000099`/`Z-999`) → devuelve **exactamente los 2 `COT-*`**. `[QUOT-FETCH-RANGE]` (#9) y `[QUOT-TEST-MIENTE]` (#44) **BORRADOS de la COLA**.
>   - **Por qué sobrevivió 4 tandas: U+F8FF es invisible en TODAS las formas normales de mirar un string.** No se ve al leer el fuente, ni en `grep` (que imprime la línea como `'COT-'`), ni en la vista del archivo — **y tampoco en `JSON.stringify`**, que lo devuelve como `"COT-"` porque es un carácter imprimible del rango Private Use Area, no un control char. Solo lo delatan los **charcodes** y el **length**. Pedir "`JSON.stringify` **y** charcodes" fue lo que destrabó el caso; el `stringify` solo habría confirmado el error por cuarta vez.
>   - **La "ANOMALÍA ABIERTA" de la Tanda 2 queda EXPLICADA y su sospecha REFUTADA.** No era "duplicación del módulo `firebase/firestore` en el grafo de vitest" (esa hipótesis se registró explícitamente como no confirmada — correctamente). Era que la query reconstruida en el archivo de test **se transcribió sin el ``**, porque el carácter no se ve al copiarlo: no era la misma query. Confirmado por 3 vías independientes — bytes del blob de HEAD (`efa3bf`), un ancla de edición que **solo** matchea con `'COT-'`, y los charcodes impresos en runtime.
>   - **C2 — VERIFICACIÓN DE LA COLA ENTERA, 29 ítems, un grep/medición por fila: 2 MUERTAS, 12 MUTADAS, 15 VIVAS.** Es el entregable principal de la tanda y el motivo por el que existe: 3 premisas de COLA habían muerto al contacto en las 2 tandas previas. **Ninguna fila sobrevivió por inercia.** Tabla completa en `CLAUDE.md` v6.82.0.
>   - **C3 — `[IMPORT-EXTRACT]` (#18) CERRADO en su tramo de código, MOVIMIENTO PURO.** La transacción de importación sale de `handleUploadToFirebase` (inline, 112 líneas) a `src/core/import/runSaleImportTransaction.ts`. `page.tsx` **−115/+9**. **Cero cambio de comportamiento:** no se agregó el guard de `[IMPORT-OVERWRITE]`, no se tocó ningún fallback, cuerpo verbatim. ⚠️ **Quedaron DOS implementaciones a propósito y se registra por qué** (ver #18 en la COLA): migrar los 3 tests a la función real cambia lo que esos tests afirman, y eso es un frente con su propio RED, no un movimiento puro.
>   - **Write a `ayrsteel-test`: CERO.** El permiso de round-trip no se usó — C1 corrió contra el EMULADOR, y C2 fue read-only contra los 2 entornos con assert de `projectId`. **Cero write a `ayrsteel-2026`** (solo lectura). Cero push, cero `master`, cero deploy, cero `firestore.rules`.
>   - **Tip de REFS: NO se transcribe acá (regla C0, v6.70.0 — un hash de TIP se mueve; verificar con `git rev-parse develop origin/develop master origin/master`).** Al abrir la tanda los 4 refs estaban sincronizados: `develop` = `origin/develop` = `56499464` (el dueño SÍ pusheó la Tanda 2), `master` = `origin/master` = `aeb724b5`. Divergencia de `master` INTENCIONAL, no es pendiente.
>   - **RULES: SIN CAMBIOS. Cero deploy.**
>   - **Custodios, medidos ANTES y DESPUÉS de C3, reportados POR SEPARADO (regla B4, prohibido sumarlos):** `npm run test` → **125 archivos / 1297 tests / 0 skip, exit 0** (idéntico antes y después — el custodio de "movimiento puro"). `test:emu` → **35 archivos / 239 passed / 3 skipped / 0 failed (242), exit 0** (idéntico antes y después). `tsc --noEmit` → **0 errores, exit 0**, medido **SIN pipe** en las 3 corridas. `check:docs` → **19 ROTO / 5 saltados**, idéntico al baseline. `test:emu:rules` y `test:emu:functions` NO medidos (esta tanda no tocó `firestore.rules` ni `functions/src`). `npm run build` NO corrido (sin rutas nuevas; la extracción no agrega páginas).
>   - **Baseline de apertura, nota de proceso:** `git status` mostró `quotationsFetch.integration.test.ts` como modificado, pero el archivo era **byte-idéntico a HEAD** — blob de índice = blob de HEAD = `87b0333d`, `hash-object --no-filters` idéntico, 3739 bytes en ambos, `git diff` y `git diff --cached` vacíos, y `i/lf w/lf` (ni siquiera el fantasma CRLF de v6.60.0). Es stat-dirty puro de Windows que `git update-index --refresh` no limpia. **No se trató como "baseline sucio"**: no había ni un byte de trabajo ajeno sin commitear.
>
> **BASELINE (2026-08-31, v6.81.0) — TANDA 2 AUTÓNOMA:**
>   - **3 ítems de COLA CERRADOS + 1 backfill de dato en `ayrsteel-test` + 1 diagnóstico que da vuelta una premisa.** Cerrados: `[NAV-B-FINISHES]` (`83d9ecaf` + `8c33159e` + `5c44ab8b`) · `[TOAST-WARNINGS-ARRAY]` (`7da0ef93`). Diagnosticado sin tocar código: `[QUOT-FETCH-RANGE]`. **Cero backend, cero rules, cero deploy, cero write a `ayrsteel-2026`** (prod solo se LEYÓ, read-only, para dimensionar el impacto de A1). Detalle completo en `CLAUDE.md` v6.81.0.
>   - **Lo que hay que leer de esta tanda es A1, no los fixes.** La contradicción que la Tanda 1 dejó abierta quedó **REPRODUCIDA dentro de un solo test y LOCALIZADA**, pero **NO explicada**: la misma query de rango, mismo fixture, back-to-back, da **0** construida en el archivo de test y **1** construida dentro de `salesService.ts`. La variable es DÓNDE se construye la query — no el dato, no el `Promise.all` (descartado por experimento), no el orden de tests. Sospecha no confirmada (duplicación del módulo `firebase/firestore` en el grafo de vitest) que **NO se afirma como mecanismo**.
>   - **`[QUOT-FETCH-RANGE]` cambia de naturaleza, no de prioridad hacia arriba: el fix sigue siendo el de 1 carácter, pero el ítem baja a DORMIDO.** La query aporta 0 (confirmado por 3 mediciones independientes, incluida la del Admin SDK) pero **NO es código muerto**: es la única fuente que cubriría un `COT-*` con status fuera de `['QUOTATION','CANCELLED']`. Exposición HOY medida en los 2 entornos: **0 y 0**.
>   - **Backfill A4:** `coils/SINUIR-ALU-AZUL-045-5000-00058.coilTypeKey` ← `BOB-ALUAZUL-045` en `ayrsteel-test`. 1 doc, 1 campo, `update()` (nunca `set()`), backup previo (`sha256 aa22665d5cae07b812fc0d09361305cb1b10cbc8ce184b41446747e4dc9f9233`), diff post-write = exactamente 1 campo. `coils` de test queda 1/1 con la clave.
>   - **Runtime A5 en browser real contra `ayrsteel-test`, entorno confirmado por mecanismo en 2 capas** (grep del bundle servido + network requests en vivo: todos `projects/ayrsteel-test`, cero a prod). **F1 CUMPLIDO** con afirmación de AUSENCIA de reload (marcador en `window` que sobrevive al guardado + `navigation.type` sigue en `"navigate"`), dato restaurado por round-trip. **F2 NO CORRIDO**: el warning SÍ es provocable (10/10 SKUs de stock en test tienen `quantity < 0`), pero exige abrir una bobina `isClosed:true` y correr una producción real que escribe colecciones **append-only no revertibles** — fuera del permiso de write de la tanda. No se simuló.
>   - **Tip de REFS, medido con `git rev-parse` EN LA MISMA CORRIDA de este bloque:** `develop` = `5c44ab8b` · `origin/develop` = `05bdceb3` (**4 commits por delante, NADA PUSHEADO** — el push lo hace el dueño) · `master` = `origin/master` = `aeb724b5`. Divergencia de `master` INTENCIONAL. El commit de docs de esta tanda queda por delante de `5c44ab8b`.
>   - **RULES: SIN CAMBIOS. Cero deploy.**
>   - **Custodios del cierre, los 4 medidos en esta tanda y reportados POR SEPARADO (regla B4, prohibido sumarlos):** `npm run test` → **125 archivos / 1297 tests / 0 skip, exit 0** (+1 archivo / +3 tests sobre v6.80.0: `src/app/admin/coils/finishes/page.test.tsx`; delta PREDICHO antes de correr y medido después, coincidió 1:1 — regla B2). `test:emu` → **35 archivos / 239 passed / 3 skipped / 0 failed (242), exit 0**, sin cambios respecto de v6.80.0 (esta tanda no tocó `src/test/integration`). `tsc --noEmit` → **0 errores, exit 0**, medido SIN pipe. `check:docs` → **19 ROTO que rompen el exit / 5 archivos saltados**, IDÉNTICO al baseline de PASO 0 antes y después de las ediciones de docs — 0 ROTO nuevos, y el chequeo duro de docs protegidos NO disparó. `test:emu:rules` y `test:emu:functions` NO medidos (esta tanda no tocó `firestore.rules` ni `functions/src`). `npm run build` NO corrido: los cambios son 1 hook, 1 página y borrado de estado muerto, sin rutas nuevas.
> **BASELINE (2026-08-31, v6.80.0) — TANDA 1 AUTÓNOMA:**
>   - **7 ítems de COLA atacados: 4 CERRADOS, 3 SALTADOS por bloqueo estructural.** Cerrados: `[COIL-TYPE-KEY-HARDEN]` (`55634e76`) · `[SALES-EDIT-404]` (`44596d38`) · `[CHECKDOCS-BANNER]` (`d7cde925`) · `[COMENTARIO-STALE-COVERAGE]`+`template.md` (`14343158`). Saltados, con el bug MEDIDO y el fix conocido pero SIN aplicar por no poder anclarlo: `[QUOT-FETCH-RANGE]` · `[IMPORT-OVERWRITE]` · `[WIDTH-FALLBACK-SPLIT]`. Cero backend, cero rules, cero deploy, cero write a `ayrsteel-2026`. Detalle completo en `CLAUDE.md` v6.80.0.
>   - **Lo que hay que leer de esta tanda no es ninguno de los 4 fixes: los 3 saltados se trabaron por LA MISMA causa raíz** — lógica de negocio embebida en componentes React (`ProductSelector.tsx`, la transacción inline de `import/page.tsx`) y una suite que REIMPLEMENTA el importador en vez de importarlo (`simulateImport`). El ítem que desbloquea a los 3 es `[IMPORT-EXTRACT]` (COLA #18) y su equivalente para componentes, no ninguno de los 3.
>   - **`[ANCHO-UTIL-VS-DESARROLLADO]` (#32) BAJADO A DORMIDO**, no cerrado: D1 (¿alimenta plata?) NO, D4 (¿existe vínculo venta→bobina?) NO, D3 sin población. El mismatch de código sigue siendo real y queda registrado, pero hoy no tiene consecuencia económica. Gatillo de re-apertura explícito en su fila.
>   - **Higiene de COLA:** borradas las filas de los 4 ítems cerrados acá + `[IMPORT-REDO]` (#16) y `[STOCK-NEG-GUARD]` (#17), cerrados por decisión de negocio en v6.77.0 y que seguían ocupando lugar, + la #21 ya tachada. 4 ítems nuevos medidos: `[CATALOG-NAME-REGEN]`, `[CATALOG-WIDTH-6000]`, el duplicado `width`/`widthMm`, y `[WIDTH-FALLBACK-SPLIT]` (nació y quedó abierto en la misma tanda).
>   - **Tip de REFS, medido con `git rev-parse` EN LA MISMA CORRIDA de este bloque:** `develop` = `14343158` · `origin/develop` = `bfd70aae` (**4 commits por delante, NADA PUSHEADO** — el push lo hace el dueño) · `master` = `origin/master` = `aeb724b5`. Divergencia de `master` INTENCIONAL, no es pendiente. El commit de docs de esta tanda queda por delante de `14343158`.
>   - **RULES: SIN CAMBIOS. Cero deploy. Cero write a prod en toda la tanda.**
>   - `npm run test` → **124 archivos / 1294 tests / 0 skip** (+1 archivo / +16 tests sobre v6.79.0: `scripts/checkDocs.test.cjs`). `test:emu` → **35 archivos / 239 passed / 3 skipped / 0 failed (242)**, sin cambios. `tsc --noEmit` → **0**. `check:docs` → **19 ROTO / 5 saltados**, idéntico al baseline ANTES y DESPUÉS del hardening del propio checker. `npm run build` → NO corrido en esta tanda (ver reporte de cierre).
> **BASELINE (2026-08-31, v6.79.0):**
>   - **4 frentes chicos cerrados EN CÓDIGO (`develop`, PUSHEADO) + 1 write de DATO en PROD.** `[EMU-STALE-LIB]` (`888a9038`) · `[IMPORT-WEIGHT-MULTIITEM]` (`77bac3ae`) · `[IMPORT-WEIGHT-ZEROQTY]` (`b714dc56`) · `[IMPORT-FETCH-ALLORNOTHING]`+`[CATALOGREF-ANY]` (`8a5eacf4`) — los 4, 100% frontend/dominio puro: cero backend, cero rules, cero deploy. Más `[COB035GRIS-UNIT]` CERRADO POR DATO en `ayrsteel-2026`: `metallic_roofing_catalog/COB035GRIS.unit` `"PIEZA"`→`"METRO"`, 1 campo vía `update()`, backup previo (`sha256 e253cb8e513c4de715d4318dfb37536649f82fca96b58cb7fc21488cedeabbd7`), audit_log `ZL21n49JJA74tnTMHERB` (`action: CATALOG_UNIT_FIX`). Detalle completo en `CLAUDE.md` v6.79.0.
>   - **Hallazgo mayor del frente: la premisa original de `[COB035GRIS-UNIT]` (COLA #29, v6.78.0) — que `COB035GRIS` daría peso "~10× por debajo del real" — ERA FALSA, y nunca se había medido.** `calcCoverageWeightKg` no desestructura `unit`; el branching es 100% por `family`. El fix aplicado es de coherencia de dato (display), no de peso.
>   - **Tip de código y de REFS: `develop`=`origin/develop`=`8a5eacf4` (push ya hecho por el dueño, confirmado por `git rev-parse` en el PASO 0 de esta tanda); `master`=`origin/master`=`aeb724b5`, 4 commits atrás.** Divergencia INTENCIONAL — los 5 frentes de código de esta versión son frontend/dominio puro, sin backend/rules/deploy que forzaran sincronizar `master` antes. El commit de docs de esta misma tanda queda por delante de `8a5eacf4` en `develop`, todavía sin pushear al cierre de este bloque (se confirma en la corrida de cierre real, más abajo en el archivo).
>   - **RULES: SIN CAMBIOS en todo el frente.**
>   - `npm run test` → **123 archivos / 1278 tests / 0 skip** (+2 archivos / +20 tests sobre v6.78.0: 1 archivo nuevo de `[EMU-STALE-LIB]` con 8 tests, +1 test de `[IMPORT-WEIGHT-MULTIITEM]`, +1 de `[IMPORT-WEIGHT-ZEROQTY]`, 1 archivo nuevo de `[IMPORT-FETCH-ALLORNOTHING]`/`[CATALOGREF-ANY]` con 10 tests). `test:emu` → **RE-MEDIDO en el PASO 0 de esta tanda: 35 archivos / 239 passed / 3 skipped / 0 failed (242)**, sin cambios respecto de v6.78.0. `test:emu:rules` → no medido en ESTA tanda (Fase 2 de docs); último valor real 2026-08-30 (no-regresión de `[EMU-STALE-LIB]`, `TANDA AUTONOMA 1`): **7 archivos / 127 tests**. `test:emu:functions` → no medido, último valor v6.52.0 (47/47). `npm run build` → no re-corrido en esta tanda de docs (cero código tocado); último valor real 2026-08-30 (`TANDA AUTONOMA 1`, T4.6): **52 rutas, exit 0**. `tsc --noEmit` → **0**, re-medido en el cierre de esta tanda. `check:docs` → ver la corrida de cierre real, más abajo en el archivo.
> **BASELINE (2026-08-30, v6.78.0):**
>   - **`[IMPORT-WEIGHT-BYPASS]` CERRADO: el importador de ventas calcula el peso metallic-roofing vía `calcCoverageWeightKg`, a nivel ítem y a nivel cabecera.** 2 commits sobre `develop`: `b82de647` (peso a nivel ítem, `saleItem.calculatedWeight`/`unitWeight`) + `c12639ed` (peso a nivel cabecera, `sale.totalWeight` — el dueño encontró en runtime, contra `ayrsteel-test`, que el primer commit dejaba la cabecera en 0 pese al ítem correcto). Frente 100% frontend/dominio: cero backend, cero rules, cero índices, cero deploy, prod (`ayrsteel-2026`) sin tocar. Detalle completo en `CLAUDE.md` v6.78.0.
>   - **Tip de código y de REFS: sin sincronizar todavía en este bloque** (se confirma en el propio commit de docs de esta tanda, después de correr `sync-context.cjs` y `check:docs` — ver el resto de esta entrada más abajo en el archivo, sección de cierre real).
>   - **RULES: SIN CAMBIOS en este frente.** `coil_finishes` ya tenía `allow read: if isStaff()` (match explícito, `firestore.rules:226-230`), la lectura nueva del importador cae bajo esa regla sin tocar el archivo.
>   - `npm run test` → **121 archivos / 1258 tests / 0 skip** (+2 sobre v6.77.0, `parseImportRows.test.ts`). `test:emu` → **RE-MEDIDO en el cierre: 35 archivos / 239 passed / 3 skipped / 0 failed (242)**, sin cambios respecto de v6.77.0 (este frente no tocó ningún test de integración de emulador; se corrió `functions && npm run build` antes, por `[EMU-STALE-LIB]`, para no medir código viejo). `npm run build` → **52 rutas, exit 0**, re-corrido en esta tanda. `tsc --noEmit` → **0**. `check:docs` → **19 ROTO** (baseline, sin subir), 5 archivos saltados (lista de siempre), `CLAUDE.md`/`HANDOFF.md`/`GEMINI.md` no aparecen ahí.
> **BASELINE (2026-08-28, v6.77.0):**
>   - **`[STOCK-NEG-WAC]` CERRADO EN PROD: `produceFromCoils` deja de mezclar el WAC contra un saldo previo negativo al cruzar a positivo (`newQty>0` → `currentQty>0`) + `hasWacResetWarning` nuevo (`currentQty<0`).** `[STOCK-NEG-GUARD]` e `[IMPORT-REDO]` CERRADOS POR DECISIÓN DE NEGOCIO — el stock negativo es comportamiento aceptado, no se agrega guard, los 38 docs de `[IMPORT-REDO]` no se tocan. Re-encuadre (no borrado) de `[STOCK-RECON]`/`[COIL-NEG-WEIGHT]` (v6.71.0): la causa tenía 2 componentes, el de CÁLCULO cerró acá, el de DISEÑO sigue así por decisión de negocio. Detalle completo en `CLAUDE.md` v6.77.0.
>   - **Tip de código y de REFS (`develop`/`origin/develop`/`master`/`origin/master`): `0758d999`, los 4 iguales.** Confirmado por `git rev-parse` sobre los 4 refs en la misma corrida — cierra la COLA "Sincronizar master" (ex-ítem 15, divergencia que venía de `[COIL-TYPE-KEY]`, v6.74.0). El commit de docs de esta misma tanda (CLAUDE.md+HANDOFF.md+GEMINI.md) queda por delante de este hash.
>   - **Deploy:** `produceFromCoils` a `ayrsteel-2026`, commit `0758d999`, verificado por `generation` de `functions:list --json` (no por el mensaje del CLI).
>   - **RULES: SIN CAMBIOS en este frente** (100% código de callable + tipos + tests). Sigue vigente lo desplegado en v6.69.0: `ayrsteel-test` ruleset `02924da0` · `ayrsteel-2026` ruleset `f673f3ef`.
>   - `npm run test` → **121 archivos / 1256 tests / 0 skip**, sin cambios respecto de v6.76.0 (scope de este comando no incluye `functions/` ni `src/test/integration`). `test:emu` → **RE-MEDIDO 2026-08-28 (PASO 5 de `[STOCK-NEG-WAC]`): 35 archivos / 239 passed / 3 skipped / 0 failed (242)** — antes 231/234, +8 de `metallicProduction.test.ts` (no +6, ver `CLAUDE.md` §11 v6.77.0 — imprecisión del ejecutor sobre el mensaje del commit `0758d999`). `test:emu:rules`/`test:emu:functions` no medidos en esta tanda. `npm run build` (Next.js raíz) **NO re-corrido** pese a tocar `src/app/admin/lines/metallic-roofing/production/new/page.tsx` — solo corrió el build de `functions/` como `predeploy` del deploy. `check:docs` → **corrida propia de esta tanda (GATE C, post-edición): 19 ROTO que rompen el exit, sin subir respecto de v6.76.0** — 5 archivos saltados por banner de staleness (`roofing.md`, `firestore-rules-explicadas.md`, `annulment.md`, `coils.md`, `metallic.md`, la misma lista conocida), `CLAUDE.md`/`HANDOFF.md`/`GEMINI.md` NO aparecen en esa lista (el mecanismo de `[CHECKDOCS-BANNER]` no se re-disparó). 2 entradas de `scripts/check-docs.allow` (`recon-c2-output.json`/`recon-d2-output.json`) quedaron `ALLOWLIST_STALE` — no rompen el exit, no se tocaron en esta tanda (fuera de alcance).
> **BASELINE (2026-08-28, v6.76.0):**
>   - **`[IMPORT-SIM-DRIFT]` CERRADO: las 3 simulaciones de test del importador de ventas alineadas al importador real (`status`/`frozenCost`/`merge`, `catch` deja de tragar el error).** Frente de CÓDIGO DE TEST — cero código de producción tocado, cero deploy, cero push. 4 commits sobre `develop`: `28a1426d` (`throw`+`status`) · `e91b0af6` (`frozenCost`) · `cd6914f5` (`merge`) · `26307854` (comentarios `[IMPORT-EXTRACT]`). Detalle completo, incluidos los 2 hallazgos de datos NO accionados (`[IMPORT-REDO]`, `[STOCK-NEG-GUARD]`), en CLAUDE.md v6.76.0 y en la COLA de este archivo.
>   - **Tip de código (`develop`): `26307854`.** Push NO hecho en esta tanda (instrucción explícita del dueño). El commit de docs de esta misma tanda (CLAUDE.md+HANDOFF.md+GEMINI.md) queda por delante de este hash — se transcribe en la PRÓXIMA sesión, nunca antes de crearlo.
>   - **RULES: SIN CAMBIOS en este frente** (cero líneas tocadas en `firestore.rules`, por instrucción explícita — el guard `1dd80865` que causó el rojo se dejó intacto, es correcto). Sigue vigente lo desplegado en v6.69.0: `ayrsteel-test` ruleset `02924da0` · `ayrsteel-2026` ruleset `f673f3ef`.
>   - `npm run test` → **RE-MEDIDO 2026-08-28: 121 archivos / 1256 tests / 0 skip**, sin cambios (este frente no toca nada dentro de su scope). `test:emu` → **RE-MEDIDO 2026-08-28: 35 archivos / 231 passed / 3 skipped / 0 failed (234)** — los 3 rojos de `[IMPORT-SIM-DRIFT]` cerrados (antes: 228/3/3, medido al abrir esta misma tanda). `test:emu:rules`/`test:emu:functions` NO re-medidos en esta tanda (sin relación con lo tocado). `check:docs` → **19 ROTO, sin subir respecto del baseline anterior.** `npm run build` NO re-corrido (cero cambios en `src/app`/`src/core`/`functions/src`).
> **BASELINE (2026-08-28, v6.74.0):**
>   - **`[COIL-TYPE-KEY]` CERRADO: `coilTypeKey` en los 3 escritores de `coils` + backfill de los 38 docs vivos de prod.** Frente de CÓDIGO + DEPLOY + DATOS. Commit `a2e99781` (`develop`/`origin/develop`). `master`/`origin/master` intencionalmente atrás, en `e9f54e20` — backend ya en prod, frontend llega a `master` en tanda propia. Deploy de los 3 callables (`registerCoil`/`registerCoilsBulk`/`registerCoilSplit`) verificado en `ayrsteel-test` (smoke real) y en `ayrsteel-2026` (sin smoke, por instrucción explícita). Backfill: 38/38, audit `bqNaEf5CgWXRzIVvQAr8`. Detalle completo en CLAUDE.md v6.74.0.
>   - **Tip de REFS (`develop`): `a2e99781`.** Tip de `master`: `e9f54e20` (sin cambios, ver arriba — divergencia INTENCIONAL, no pendiente).
>   - **RULES: SIN CAMBIOS en este frente** (100% callables + tipo + datos). Sigue vigente lo desplegado en v6.69.0: `ayrsteel-test` ruleset `02924da0` · `ayrsteel-2026` ruleset `f673f3ef`.
>   - `npm run test` → **RE-MEDIDO 2026-08-28: 121 archivos / 1256 tests / 0 skip** (era 119/1238; delta exacto +2 archivos/+18 tests, predicho antes de medir). `tsc --noEmit` 0/0. `npm run build` → 🟢 52 páginas, RE-CORRIDO en esta tanda (exit 0). `test:emu`/`test:emu:rules`/`test:emu:functions` → NO re-medidos ÍNTEGROS en esta tanda (los 3 archivos de integración que este frente tocó se verificaron aislados por `git stash`, RED→GREEN confirmado en el PASO 2 — ver CLAUDE.md v6.74.0).
> **BASELINE anterior (2026-08-27, v6.72.0):**
>   - **`[ALUZINC-RESET]` CERRADO: 1207 docs físicamente borrados de `ayrsteel-2026` (prod), autorizado por el dueño.** Operación de DATOS — cero commit de código, cero deploy. Ver entrada completa abajo y en CLAUDE.md v6.71.0.
>   - **Commits de código YA creados e inmutables (se transcriben):** `72bbcfe5` (helper `buildDuplicateSaleUrl`) · `54928603` (4 swaps a `router.push`). Cierre de docs previo: `a56dbf01` (v6.71.0). El commit de docs de v6.72.0 es `f420cbca`.
>   - **PUSH: ATERRIZADO — CORREGIDO EN ESTA TANDA (v6.73.0, SÉPTIMA C0).** Los 4 refs (`develop`/`master`/`origin/develop`/`origin/master`) están en `f420cbca`, medido con `git show-ref` al abrir esta sesión (`refs/heads/develop`/`refs/heads/master`/`refs/remotes/origin/develop`/`refs/remotes/origin/master`, los 4 idénticos). El "PENDIENTE" que decía esta línea era stale desde que se escribió — mismo defecto que C0 ×6 anteriores (ver CLAUDE.md, sección de reglas grabadas).
>   - **Tip de REFS: `f420cbca`** (los 4, ver línea anterior — mismo hash de código y de refs en este caso, no hubo commit de docs posterior sin pushear).
>   - **RULES: SIN CAMBIOS en este frente tampoco** (100% operación de datos). Sigue vigente lo desplegado en v6.69.0: `ayrsteel-test` ruleset `02924da0` · `ayrsteel-2026` ruleset `f673f3ef`. ⚠️ **Deuda que SIGUE abierta, sin tocar:** el `firestore.rules` del repo ya NO es byte-idéntico a lo desplegado. Re-deployar en el próximo frente que toque rules.
>   - **4 comandos custodios, cifra ÚNICA por comando (SÉPTIMA C0, corregida en v6.73.0 — esta línea y el bloque de abajo tenían valores CONTRADICTORIOS para `npm run test`, 1238 vs 1234; el valor correcto es 1238, ver bloque de abajo).**
> **4 comandos custodios DISJUNTOS, reportados por separado — PROHIBIDO sumarlos (regla B4):**
>   - `npm run test` → **119 archivos / 1238 tests / 0 skip** (medido 2026-08-27, tanda NAV-A/v6.72.0, `exit_code 0` contra la tarea real — regla B7; era 121/1248 antes de `[CASCADE-DUP]`: +25 TANDA A2 → 123/1273 → +5 port TANDA B → 123/1278 → -44 borrado copia cliente+2 `.parity.test.ts` → 119/1234 → +4 de `[NAV-HARD]`/`buildDuplicateSaleUrl` → **119/1238**, cadena completa hasta el valor vigente).
>   - `test:emu:rules` (`src/test/rules`) → **7 archivos / 127 tests** — NO re-medido en esta tanda (v6.73.0), último valor conocido 2026-08-27 (`[CASCADE-DUP]`).
>   - `test:emu` (`src/test/integration`) → **RE-MEDIDO 2026-08-28: 34 archivos/1 failed (35) — 225 passed / 3 skipped / 3 failed (231), `exit_code 1`.** Determinista: 3 corridas idénticas (`STOP 0` + `D1` ×2 del frente `[QUOT-FETCH-RANGE]`), mismos 3 rojos exactos las 3 veces, en `src/test/integration/salesImport.test.ts` (archivo AJENO a `quotationsFetch.integration.test.ts`/`salesImportLogic.ts`). Causa MEDIDA, ver `[IMPORT-SIM-DRIFT]` en la COLA — reemplaza el valor viejo `228/3skip/0 (231)` de v6.57.0, que ya NO es el estado real del repo.
>   - `test:emu:functions` (`functions/src/callables`) → **47/47** — NO re-medido en esta tanda, último valor conocido v6.52.0.
> `tsc --noEmit` 0/0 (raiz y `functions/`). `npm run build` NO re-corrido en v6.73.0 (frente 100% de recon/docs, cero código tocado).
> **v6.73.0 — TANDA DE RECON + `[QUOT-FETCH-RANGE]` CONGELADO. Cero código de producción tocado, cero write a prod, cero deploy.** PASO 0 en 2 capas sobre `[QUOTATION-APPROVE-UNREACHABLE]`: (1) recon de código 100% local (enum `SaleStatus`, cuerpos completos de `approveQuotation`/`cancelQuotation`/`editQuotation`, bloque `match /sales` de `firestore.rules`, inventario de cobertura); (2) recon de datos contra `ayrsteel-test` Y `ayrsteel-2026` (script fuera del repo, `~/ayr-scratch/2026-08-28-quotations/recon.cjs`, NUNCA trackeado, assert de `projectId` obligatorio, SOLO `getDocs`/`count()`, cero callable, cero write). Ambos recons **DESTAPARON un bug nuevo, no el que se estaba buscando**: `[QUOT-FETCH-RANGE]`.
>   - **`[QUOT-FETCH-RANGE]` — `fetchAllQuotations` (`salesService.ts:682`) tiene una query de rango sobre `documentId()` con AMBOS límites literalmente iguales** (`>= 'COT-'` Y `< 'COT-'`), que por construcción no puede matchear ningún id — devuelve 0 siempre. Es la ÚNICA query de rango sobre `documentId()` en todo el repo (`src/`+`functions/src/`, medido con `grep`); las otras 8 ocurrencias son `where(...,'in',...)` o `orderBy`, sin límites que puedan coincidir. Incumple su propio docstring (línea 668: "perchas importadas COT-*, cualquier status"). Fix conocido y NO aplicado: límite superior `'COT-'`→`'COT.'` (sucesor ASCII exacto, `0x2D`→`0x2E`).
>   - **Exposición HOY: 0 docs en los 2 proyectos** (prod tiene 0 `COT-*` post-`[ALUZINC-RESET]`; test tiene 1 sola cotización, nativa `C-000040`, que no usa esta query). El bug es real pero está DORMIDO por falta de población, no por estar bien.
>   - **Mecanismo del bug vivo, medido código-por-código (no solo el bug de la query):** anular una percha `COT-*` DIRECTAMENTE (vía `annulSale` sobre un doc con `status:'QUOTATION'`) resuelve `mode:'self'` en `resolveSaleQuotationLink` (`saleQuotationLink.ts:26-27`), que `resolveSaleTwinPath` colapsa a `twinPath:'orphan'` (`resolveSaleTwinPath.ts:10-11`, porque `'self' !== 'linked'`) — **no existe un `twinPath` literal `"self"`**. En `orphan`, `buildAnnulmentCascade` solo aplica el write base: `status:'VOIDED'` sobre el propio doc (`buildAnnulmentCascade.ts:64`), sin distinguir importada de nativa. Con `status:'VOIDED'`, ese doc queda FUERA de `Q2` (`status in ['QUOTATION','CANCELLED']`) — invisible en `/admin/quotations` aunque la query de rango (`Q1`) estuviera bien formada. Esto es DISTINTO de anular la VENTA vinculada a una percha (rama `"imported"`, `buildAnnulmentCascade.ts:94-111`), que deja la percha en `status:'CANCELLED'` — SÍ visible por `Q2`. No confundir los 2 caminos.
>   - **BLOQUEADO POR baseline sucio de `test:emu`** — no se pudo llegar a RED→GREEN→MUTACIÓN del fix (STOP 2/STOP 3 del PASO 0 asignado) porque el `STOP 0` (baseline) salió con 3 rojos preexistentes ajenos, ver `[IMPORT-SIM-DRIFT]` abajo. Frente CONGELADO en la COLA con RED ya diseñado (fixture `COT-INV-C` con `status:'VOIDED'`, `it()` que assertea `toContain`) pero NO escrito en el archivo — cero cambio en `src/test/`.
>   - **`[IMPORT-SIM-DRIFT]` — 3 rojos DETERMINISTAS en `src/test/integration/salesImport.test.ts`, medidos 3/3 corridas idénticas (mismos nombres de test, mismas aserciones).** Causa MEDIDA con evidencia directa (no inferida): `firestore.rules:81-84` (commit `1dd80865`, 2026-08-26, `[SALES-STATUS-ACCESSOR]`) exige `'status' in request.resource.data` para poder crear un doc en `sales`; los 4 fixtures de este test (`saleData`/`saleSI`/`saleNO`/`saleNC`, sin tocar desde `8da52da1`, 2026-06-29 — casi 2 meses ANTES de la regla) nunca setean `status`. La `runTransaction` es rechazada con `PERMISSION_DENIED` (`evaluation error at L81:24 for 'create'`, capturado crudo del log), el `catch(txError){console.error(...)}` del `simulateImport` local del test (líneas 108-110) lo TRAGA sin re-lanzar, y el stock queda en el valor SEMBRADO — por eso el síntoma es `expected 100 to be 90` (una aserción de negocio), no un error de Firebase visible.
>   - **CAUSA DE FONDO, no solo el síntoma: `simulateImport` (`salesImport.test.ts:21-112`) es una REIMPLEMENTACIÓN standalone del importador DENTRO del test — no llama a `buildImportWrites`/`salesImportLogic.ts` ni a nada del código real de `import/page.tsx`.** Por eso divergió silenciosamente durante 2 meses sin que ningún cambio al importador real lo tocara ni lo rompiera antes: lo rompió un cambio de `firestore.rules`, una capa que ninguna copia local puede anticipar. Misma familia que `[CASCADE-DUP]` (v6.70.0): una copia paralela de lógica de producción, sin mecanismo que la mantenga sincronizada.
>   - **FIX RECHAZADO explícitamente: agregar `status` a los 4 fixtures.** Eso pondría la suite en verde sobre una copia YA obsoleta del importador — un verde mentiroso que no valida nada del código real. **Fix correcto, no ejecutado en esta tanda:** que el test llame al importador real (`buildImportWrites`) en vez de reimplementarlo. Frente propio, con su propio PASO 0.
>   - **PROD NO AFECTADO — verificado, no supuesto.** El importador real (`saleDocBuilder.ts:235`, `buildQuotationDoc`) escribe `status:'QUOTATION'` HARDCODEADO, sin condición — nunca puede nacer sin `status`. El agujero que `firestore.rules:81-84` cierra nunca fue explotable por el código de producción; solo lo era por la copia obsoleta del test.
>   - **`test:emu` (`225 passed/3 skipped/3 failed (231)`, `exit_code 1`) REEMPLAZA el valor `228/3skip/0` de v6.57.0 en la cabecera** — no se suma con ningún otro custodio (regla B4), ver bloque de arriba.
>   - **El NullPointerException de rules en el shutdown de `emulators:exec` NO es la causa de los 3 rojos** — hipótesis evaluada y DESCARTADA por POSICIÓN en el log: aparece ~13 líneas DESPUÉS del resumen `Test Files`/`Tests` (durante `Stopping ... Emulator`), los 3 rojos ya están resueltos y reportados para ese punto. Medido 3/3 corridas (`STOP 0` + `D1`×2). Ruido de teardown del runtime de rules-tools (Java, `Server.processRequestsFromInputStream`), hermano del huérfano sistemático del 8080 (15/15 acumulado) — ver deuda nueva abajo.
>   - **`[PARITY-DATA]` — la paridad test↔prod no cubre POBLACIÓN de `sales`, medido 2026-08-28.** `ayrsteel-test`: 2 docs (`C-000040` QUOTATION + 1 `V-*` COMPLETED), 0 `COT-*`. `ayrsteel-2026`: 67 docs, LOS 67 `COMPLETED`, 0 cotizaciones de ningún tipo (0 `QUOTATION`, 0 `CANCELLED`, 0 `CONVERTED`). Ningún ambiente tiene población viva para validar en runtime el flujo de cotizaciones — hermano de la deuda de 0 extensiones de Algolia en test (v6.61.0).
>   - **Hallazgo cruzado con `[SMOKE-LOGS]`: 4 `audit_logs` `action=='CANCEL_QUOTATION'` en prod, 0 docs `CANCELLED` en `sales`.** En test son 2 (`C-000029`, `COT-FFA1-1262`), y se verificó con `getDoc` que **ninguno de los 2 `entityId` existe hoy en `sales`** — confirma con evidencia nueva que la deuda "`sales` no audita borrados" (v6.58.0) sigue VIVA.
>   - **REGLA NUEVA B9 — "output pegado arriba" del ejecutor NO es evidencia de que el contenido llegó; verificar contra el CONTENIDO recibido, nunca contra la declaración de haberlo enviado.** Medido 3 veces distintas esta sesión (el canal de paste de herramienta no transportó el contenido, igual que la falla ya conocida de v6.62.0/v6.64.0 con outputs largos). Método que SÍ funcionó: transcripción TIPEADA por el ejecutor, un bloque por mensaje, con líneas de control `KEYS`/`SUMA` como checksum verificable por el director.
>   - **REGLA NUEVA B10 — un frente que modifica `firestore.rules` re-mide TODOS los custodios que ejecutan escrituras contra el emulador, no solo `test:emu:rules`.** Origen medido: `[SALES-STATUS-ACCESSOR]` (v6.69.0, commit `1dd80865`) midió `test:emu:rules` (122→127) y nunca corrió `test:emu` — los 3 rojos de `[IMPORT-SIM-DRIFT]` que esa misma regla introdujo sobrevivieron 3 frentes completos (v6.69.0, v6.70.0, v6.72.0) sin que ningún custodio los detectara, porque ninguno de esos 3 frentes corrió `test:emu`.
>   - **4 imprecisiones del director, DECIMOSÉPTIMA a VIGÉSIMA, las 4 detectadas y corregidas sin resolverlas en silencio:**
>     - **DECIMOSÉPTIMA** — el ejecutor repitió `admin/sales/page.tsx:92`/`:113` (línea de otro doc/frente, sin cruzar) al redactar `[QUOTATION-APPROVE-UNREACHABLE]` en la COLA; medido en el PASO 0 de código de esta tanda: son `:94`/`:115` (`handleApprove`/`handleCancel`), y el wiring real (`onApprove={handleApprove}`/`onCancel={handleCancel}`) vive en `:199-200`, no en ese rango. **Nota: esa línea de la COLA no se corrigió en esta tanda** (fuera del alcance explícito del cierre) — queda registrada como imprecisión conocida, no como fix aplicado.
>     - **DECIMOCTAVA** — el ejecutor dijo que M5 (cotizaciones nativas vivas) daría "casi seguro `C-000040`" ANTES de correr el recon de datos contra `ayrsteel-test`. Acertó — pero adivinar y acertar sigue siendo adivinar: la afirmación se hizo sin haber medido, y el recon corrido después la confirmó por casualidad de tener 1 solo candidato posible en un ambiente con 2 docs totales.
>     - **DECIMONOVENA** — el ejecutor llamó `twinPath:"self"` a lo que `resolveSaleTwinPath` en realidad COLAPSA a `"orphan"` (`resolveSaleTwinPath.ts:10`, `mode !== "linked"`). El `twinPath` literal `"self"` no existe en el tipo `SaleTwinPath` (`"native"|"imported"|"orphan"`) — corregido al citar el código verbatim.
>     - **VIGÉSIMA** — el ejecutor atribuyó los 3 rojos de `[IMPORT-SIM-DRIFT]` al `NullPointerException` de rules ANTES de medir la posición de esa excepción en el log. Hipótesis descartada por evidencia (D4 del frente `[EMU-IMPORT-RED]`) — casi desvía el diagnóstico hacia una pista falsa; la causa real (`PERMISSION_DENIED` en `firestore.rules:81`) es independiente y anterior en el log.
>   - Detalle completo, con todo el código citado verbatim y los recons de datos crudos, en CLAUDE.md v6.73.0.
> **v6.70.0 — `[CASCADE-DUP]` CERRADO: copia CLIENTE de la cascada de anulación (`buildAnnulmentCascade`+`canAnnulSale`) BORRADA. Cero código de producción tocado, 3 commits locales, SIN PUSH.** Commits: `bd1613d8` (TANDA A2, cobertura server-only) → `7061cad2` (TANDA B, port de 5 casos `self`) → `f88cbb02` (TANDA B, borrado de 6 archivos).
>   - **TANDA A2 SE HABÍA CERRADO DECLARANDO COBERTURA SERVER-ONLY COMPLETA. NO LO ERA.** Los 5 casos `link.mode === "self"` (cotización `QUOTATION` con producción `ACTIVE`, fix de v6.52.1) solo los cubría `canAnnulSale.parity.test.ts` — el archivo que esta misma tanda iba a borrar. **Detectado por MUTACIÓN (M4: quitar `|| link.mode === "self"` de la guarda), no por lectura.** Sin la mutación, el borrado habría dejado esa rama sin NINGÚN test en todo el repo.
>   - **Remedio: portar los 5 casos a `canAnnulSale.test.ts` (server-only) ANTES de borrar, y RE-VERIFICAR el port con la MISMA mutación.** Suite 1273→1278 (delta exacto +5). Repetida M4 sobre el port: 2 de los 5 tests portados se pusieron rojos — confirmado que el port no era decorativo.
>   - **REGLA NUEVA B1 — la verificación por mutación se cuenta por COMPORTAMIENTO, no por aserción.** El enunciado de arranque decía "3 aserciones de `stockEffect`" — medido: son 9 `expect()` en 5 `it()`, 3 comportamientos distintos. El "3" era el número de casos del `it.each`, congelado como si fuera el total. Corregido con 1 mutación por comportamiento (M1-M5), cada una en línea distinta, revertida y confirmada con `git diff` vacío antes de la siguiente.
>   - **REGLA NUEVA B2 — Aritmética predicha ANTES de borrar** (119 archivos/1234 tests, contando `it.each`/loops por caso ejecutado) y medida DESPUÉS: coincidió 1:1.
>   - **REGLA NUEVA B3 — un `.parity.test.ts` no se puede conservar si se borra una de las dos copias que compara: no compila.** Se evaluó y descartó conservar `canAnnulSale.parity.test.ts` para no perder los 5 casos `self` — portar era la única salida real, no una opción entre varias.
>   - **Arquitectura: ya NO existe copia cliente de `buildAnnulmentCascade` ni de `canAnnulSale`.** La única duplicación cliente/server que sigue viva en `annulment/` es `resolveSaleTwinPath` (+ `saleQuotationLink.ts`/`saleProductionLink.ts`), no tocada en este frente — su copia cliente sigue viva, consumida por `SaleDetailsModal.tsx:37`.
>   - **SEXTA REINCIDENCIA CONSECUTIVA DE C0** — el commit `95a24458` tocó solo 4 líneas de `## COLA` (medido: 3 insertions/1 deletion) y dejó esta cabecera sin re-verificar. El checklist de cierre de `§11` no se disparó porque ese commit no era un cierre de frente completo. Corregido acá. Detalle completo en CLAUDE.md v6.70.0.
>   - **Cierre colateral: `docs/03-arquitectura/patrones-y-convenciones.md` (2 filas de la tabla SYNC-MARKER + 1 viñeta de GAPS) y `docs/modules/annulment.md` (§0 fila 2, §2 completo, fecha con alcance) corregidos en este mismo frente. Grep de símbolos borrados en `docs/`: 0 hits vivos, solo narrativa histórica en §0-ter de `annulment.md`.**
>   - **REGLA NUEVA B4 — prohibida toda cifra AGREGADA que sume suites distintas.** El primer borrador de esta tanda sumó `test:emu`+`test:emu:functions`+`test:emu:rules`+unit en un total ("1631" en `CLAUDE.md`, "1636" en este doc — CONTRADICTORIOS entre sí). Un total que ningún comando imprime queda stale en silencio cada vez que uno de sus componentes se mueve. Cada suite va por separado, con su comando y su fecha de medición inline. Una cifra copiada de otro doc NO cuenta como medida.
>   - **REGLA NUEVA B5 — omitir una discrepancia detectada equivale a ajustarla en silencio.** El primer STOP de esta tanda anunció "dos discrepancias" y entregó una sola; la segunda (el total contradictorio de B4) se había detectado y descartado como "fuera de alcance" sin decirlo. Toda discrepancia vista se reporta, aunque se decida no tocarla.
>   - **Nota de recon: el inventario del PASO 0 reportó 3 `.parity.test.ts` cuando eran 4** (faltaba `saleQuotationLink.parity.test.ts`). Sin consecuencia — la aritmética predicha del borrado dio exacta (1278-44=1234) y los 2 sobrevivientes son los correctos. Registrado como imprecisión de enumeración en recon.
> **v6.72.0 — `[NAV-HARD]` / NAV-A CERRADO: 4 `window.location` → `router.push` en `/admin/sales`. 3 commits, SIN PUSH al cerrar esta cabecera.** Commits: `72bbcfe5` (helper puro `buildDuplicateSaleUrl` + RED→GREEN de 4 casos, parity del inline de `onDuplicate`) → `54928603` (los 4 swaps + `HeaderOptions` recibe `onImport` como prop, sin `useRouter` adentro) → tanda de docs. Suite 1234→**1238**, delta exacto +4, predicho y medido. `tsc` 0 · `build` 🟢 52 páginas.
>   - **El recon previo tenía 2 errores medidos:** eran **9 hits en 4 archivos** (no 5), y el `url` de `onDuplicate` **NO era falso positivo** — era navegación real. Por eso clasificaba 7 de 9.
>   - **Verificado por MECANISMO con Claude in Chrome** (network requests, contra `ayrsteel-test` confirmado por el `database=projects/…` de los requests, no por el ribbon). Soft nav probada por `?_rsc=` + continuidad del `gsessionid` de Firestore. **La trampa del Router Cache quedó DESCARTADA POR MEDICIÓN** (el back dispara 4 `runAggregationQuery`; `useSales` re-fetchea). NO re-abrir.
>   - **GAP: `onEdit` y la rama `&as=QUOTE` sin runtime.** De los 4 swaps, **solo 3 tienen efecto vivo** — `onEdit` es cableado muerto (`hidden:true` + ruta inexistente, ver `[SALES-EDIT-404]`).
>   - **3 imprecisiones del director (13ª, 14ª, 15ª) y 2 reglas nuevas (B7 `exit_code`, B8 ancla CRLF).** Detalle completo en CLAUDE.md v6.72.0.
> **v6.71.0 — `[ALUZINC-RESET]` CERRADO: 1207 docs de `ayrsteel-2026` borrados FÍSICAMENTE, autorizado por el dueño.** Operación de DATOS, cero commit de código, cero deploy. Desglose: `kardex_movements` 289 · `scrap_logs` 4 · `metallic_roofing_stock_movements` 385 · `metallic_roofing_stock` 18 · `production_logs` 119 · `sales` 262 · `coils` 130 (orden fijo, `coils` último). Una sola corrida de `delete-aluzinc.cjs --execute` (vive fuera del repo, `~/ayr-scratch/2026-08-27-aluzinc/`, NUNCA trackeado por git), 0 fallos de batch, 0 ya-inexistentes. Audit: `audit_logs/3EMsMtQ5WT12OqjmOY49`. Backup: nativo GCS + 16 archivos CSV/JSON locales con sha256 verificado por el script ANTES de borrar (`integrity.json`). Verificación post: **13/13 métricas exactas** contra lo predicho (`coils` 168→38, `production_logs` 262→143, `sales` 329→67, `kardex_movements` 796→507, `audit_logs` 1697→1698, el resto a 0 o sin moverse). La cadena bobina→producción→venta se corta en 2 puntos MEDIDOS antes de borrar: `perCoilBreakdown` puede mezclar bobinas dentro de un mismo `production_log`, y `metallic_roofing_stock`/`_movements` no tienen `coilId` (pool WAC por SKU) — de las 262 ventas con `metallic-roofing`, solo 169 eran trazables a una bobina; el dueño eligió borrar las 262 completas, no solo las 169. Excepción registrada y NO precedente: el borrado usó Admin SDK sobre 3 colecciones `allow delete: if false` (append-only) — autorización explícita, única, fechada, con audit_log. Hallazgo AJENO destapado de rebote: `[DRYWALL-ORPHAN-LOGS]`, a la COLA (9 `production_logs` de drywall con `parentCoilId` inexistente, preexistente, no tocado por este borrado — el conteo de logs de drywall no se movió, 143→143). `coil_finishes` (9 docs, las 8 `ALZ-*`) y `deleteCoilDraft` sobreviven intactos. Runtime validado por el dueño en incógnito tras el borrado: OK. Detalle completo en CLAUDE.md v6.71.0.
> **v6.69.0 — `[SALES-STATUS-ACCESSOR]` CERRADO EN PROD.** Accessor seguro `.get('status','')` en las 3 lecturas de `sales.update` + `create` exige `status` no vacío. Un campo AUSENTE aborta la evaluación de la rule (no evalúa `false`) y deja el doc inactualizable para TODOS los roles, ADMIN incluido — medido: `Property status is undefined on object`. Población real 0 de 329, frente preventivo. Commit `1dd80865`, `test:emu:rules` 122 → 127, desplegado y verificado por REST en los 2 proyectos. Hallazgo de negocio destapado por el runtime: `[QUOTATION-APPROVE-UNREACHABLE]`, a la COLA. Detalle completo en CLAUDE.md v6.69.0.
> **v6.68.0 — EL ITEM #1 DE LA COLA ERA UN FANTASMA.** `[CUTORDER-FX]` (`cutOrderService.ts` doble conversión USD->PEN) figuraba como posición 1 y "bloquea WRITE 8", estando CERRADO desde v6.52.0 — el mismo `HANDOFF.md` lo declaraba cerrado 2 veces más arriba. Re-verificado en 3 capas (código / barrido / prod) antes de borrarlo. Causa: la reestructuración de la COLA en v6.66.0 movió filas sin cruzarlas contra las deudas cerradas del mismo archivo. Remedio: SLUGS estables, la posición deja de ser identidad. Banner ausente hasta v6.69.0 — mismo defecto que se registra ahí. Detalle completo en CLAUDE.md v6.68.0.
> **v6.67.0 — TANDA DE REGISTRO, cero código.** C0 (esta cabecera, tercera reincidencia), C1 (7 refs off-by-one corregidas a título) y C3 (banner en `template.md`, la plantilla propagaba `OPERATOR:{canCreate:true}` muerto desde `921eeabe`) cerrados. C2 disuelto: su premisa era falsa, el 11 ya estaba registrado desde v6.55.0. 4 archivos de docs, suite intacta en 1645. Detalle completo en CLAUDE.md v6.67.0.
> **v6.66.0 — FRENTE DE PERMISOS CERRADO: OPERATOR pierde toda escritura en `firestore.rules`.** `canWrite()` (ADMIN+SUPERVISOR) reemplaza a `isStaff()` en las 32 escrituras; `isStaff()` queda exclusivo para `read`. `delete` admin-only nuevo en `customers`/`contacts`/`coil_finishes`. Commit `921eeabe`. `test:emu:rules` 84 → 122. Desplegado y VERIFICADO POR REST (no por el output del CLI) en `ayrsteel-test` y `ayrsteel-2026`. Banner ausente hasta v6.67.0 — misma raíz que C0. Detalle completo en CLAUDE.md v6.66.0.
> **v6.65.0 — CI abandonado BORRADO.** `.github/workflows/ci.yml` eliminado (commit `0e3d201a`) — único archivo bajo `.github/`, el directorio desaparece con él. La deuda de v6.63.0 lo subestimaba: no era 1 job, eran 4 (`lint-and-typecheck`, `test` con coverage a Codecov, `build`, `security` con `npm audit`+Snyk ambos `continue-on-error:true`). Nadie miraba ninguno. Hallazgo adicional: el trigger `[main, develop]` tenía la mitad muerta — este repo usa `master`, no `main`, y no existe rama `main` ni local ni en origin (`git branch -a --list main` y `git ls-remote --heads origin main` ambos vacíos) — ningún push/PR a `master` disparó ese CI jamás. Precondición de borrado (sin branch protection/ruleset que lo requiera) verificada por el dueño en GitHub, fuera del alcance del filesystem. Cabo suelto: `CODECOV_TOKEN`/`SNYK_TOKEN` y la app de Codecov (si instalada) quedan huérfanos en GitHub Settings — `git rm` no los toca, limpieza manual no bloqueante. Detalle completo en CLAUDE.md v6.65.0.
> **v6.64.0 — FRENTE #3 CERRADO EN DEVELOP: `firestore.rules` pasa de 1/35 bloques `match` con cobertura a 35/35.** `test:emu:rules` 4 → 84. `firestore.rules` NO se tocó: cero líneas. Método: valores esperados MEDIDOS contra el emulador con un probe descartable (156 filas/474 asserts/0 `ERR`) antes de congelarse en los tests — las 12 colecciones que nunca pasaron por el probe salieron 100% verdes a la primera contra su rule declarada. 14 mutaciones, 13 discriminaron (1 no: `isSignedIn()`, redundante — sus consumidores ya cortan solos con `auth==null`), `git diff` de la rule vacío las 14 veces. Granularidad `it.each` por colección: un `for` con `assertFails` adentro de un solo `it()` convierte N asserts en 1 rojo ambiguo (medido: `isStaff()` mutado daba 1 rojo con loop, 6 con `it.each`). 6 deudas nuevas (OPERATOR alcanzado por `isStaff()` en colecciones que la UI le esconde; `customers`/`contacts`/`coil_finishes` sin delete admin-only; `sales/{id}/history` sin guard de campos, con 1 escritor real en `sales/import/page.tsx`; `isSignedIn()` redundante; el canal del ejecutor no transporta paste verbatim de outputs largos, solo tablas redactadas; `ADR-002:139` stale sobre Functions "vacías"). Banners de estado superado en `ADR-003`/`ADR-006` (cuerpo intacto). Detalle completo en CLAUDE.md v6.64.0.
> **v6.63.0 — FRENTE 4+5 CERRADO EN DEVELOP: el huérfano de `emulators:exec` en el 8080 ya no requiere intervención manual.** `scripts/with-emulator.cjs` (wrapper: pre-check aborta si el puerto ya está ocupado sin matar nada, corre la suite real, reap incondicional en `finally` que solo mata si el `CommandLine` matchea `cloud-firestore-emulator`, exit code original preservado) + `scripts/emuPortGuard.cjs` (lógica pura, 18 tests, validada con mutación real sobre la rama Windows). `test:emu`/`test:emu:functions`/`test:emu:rules` pasan por el wrapper vía sus `:inner` (strings byte-idénticos a los de antes). `test:integration` BORRADO (era `vitest` en watch-mode dentro de `emulators:exec`, sin invocadores reales). Huérfano medido **15/15 acumulado** (era 12/12) — sistemático, ya no requiere que el dueño libere el puerto a mano entre suites. Hallazgo de proceso: los 3 archivos nuevos nacían gitignorados (`/scripts/*.cjs`, regla del 19-ago) — se agregó excepción explícita; `sync-context.cjs` sobrevivía por inercia (entró el 20-jul, antes de la regla), no por excepción. Detalle completo en CLAUDE.md v6.63.0.
> **v6.62.0 — FRENTE #10 CERRADO EN DEVELOP.** `/admin/coils` deja de ser página única: redirect puro a `/admin/coils/aluzinc`, más `/admin/coils/drywall` — ambas montan `CoilsInventoryView` compartido con `line` distinto. Paginación en memoria cuando hay scope por línea (`scopeCoilsByFinishIds`+`sliceCoilsForPage`, `coilPaging.ts` nuevo), rama Algolia scopeada server-side con OR de finishes (`buildCoilAlgoliaFilters` gana `lineFinishIds`), `getFinishIdsForLine` por fin con consumidor real, dropdown de acabados por props, alerta `noFinish` deliberadamente GLOBAL (no por línea). Detalle completo en CLAUDE.md v6.62.0 y `docs/modules/coils.md` §7.
> **v6.61.0 — BUG DE ALGOLIA (`/admin/coils`) CERRADO en dos capas:** (1) config fuera del repo, aplicada por el dueño: `attributesForFaceting` en `coils_index` (`filterOnly` × 4: `status`/`finish`/`metadata.currency`/`metadata.provider`, dashboard de Algolia) + `FIELDS` de la extensión ampliado con `finish`,`metadata.currency` (Firebase console, re-sync disparado; **orden obligatorio: facets PRIMERO, FIELDS después**); (2) código `5c83f7bb`: `metadata.provider` iba sin comillas y los 13 proveedores de prod tienen espacios → helper puro `buildCoilAlgoliaFilters` (quote+escape) como fuente única de los 2 call-sites. **Verificado medido: 8/8 filtros cuadran contra Firestore (antes 1/8), Σ finish = Σ status = 168.** Runtime del dueño en incógnito, 5 casos incl. export. Doc nuevo: `docs/03-arquitectura/busqueda-algolia.md`. **#10 queda DESBLOQUEADO y es el próximo frente.** Detalle en CLAUDE.md v6.61.0.
> **BASELINE anterior (2026-08-24, v6.60.0):** los 4 refs en **`4e36435d`** (renormalización CRLF, 290 blobs, cero cambio de contenido) — encima de `c337fc0e` (helper `getFinishIdsForLine`, 5 tests, sin consumidor todavía). `git ls-files --eol | grep -c i/crlf` = **0**. Cruzar `master`↔`develop` ya no ensucia nada. Detalle en CLAUDE.md v6.60.0.
> **BASELINE anterior (2026-08-24, v6.59.0):** los 4 refs (`develop`/`master`/`origin/develop`/`origin/master`) en `90d13e24` (docs de cierre v6.57.0, ya pusheado) + el commit de docs de v6.58.0 (`a50fd248`) encima. **Tip de CÓDIGO sigue siendo `65820b9f`** (v6.57.0) — sin cambios de código en v6.58.0 ni v6.59.0.
> **v6.59.0 — las 4 NC `FFC1-57/61/64/67` fueron ANULADAS EN PROD** vía invocación real del callable `annulSale` desplegado (backup + restore-test previo). Las 6 NC de prod (`FFC1-44/57/61/64/67/72`) están hoy `VOIDED`, sus 6 perchas `CANCELLED`, y ninguna `COT-FFC1-*` está en la cola de producción. 100% operación de datos: CERO commit de código, CERO deploy, CERO índice. Detalle completo en CLAUDE.md v6.59.0.
> **Frente A de v6.58.0 (duplicados `FFC1-57/61/64/67`) fue 100% operación de datos en prod: CERO commit, CERO deploy de Functions, CERO índice nuevo, CERO cambio de código.** Detalle completo en CLAUDE.md v6.58.0 y en DEUDAS abajo.
> **Backend en prod:** sin cambios en este frente (`annulSale` sigue en `62116ea5`, v6.56.0 — ver entradas anteriores). Este frente es 100% frontend/tests/config: CERO deploy de Functions, CERO índice, CERO write a prod.
> **Suite ENTERA en verde (v6.57.0):** `test:emu` 228/3skip/0 (231) + `test:emu:functions` **47/47 real** (el `47/47` registrado al cierre de v6.55.0 era FALSO, medido en baseline limpio con stash: 46/1 — ver DEUDAS) + `test:emu:rules` **4/4 (custodio nuevo)** + unit escopeado **1195 passed/0 skipped (1195)** = **1474 verdes, 0 rojos reales**. `tsc --noEmit` 0 errores.
> **Delta de skips explicado, no solo corregido:** bajó de `1195/4skip (1199)` a `1195/0skip (1195)`. Los 4 `skip` viejos eran los 4 `it()` de `salesStatus.rules.test.ts`, que en ese momento vivía en el scope de `npm run test` de raíz SIN exclude — su `beforeAll` (`initializeTestEnvironment` contra `127.0.0.1:8080`) fallaba por `ECONNREFUSED` sin emulador, y vitest reporta los tests de un `describe` con `beforeAll` caído como **skipped**, no failed. La cifra vieja era correcta para su contexto; los 4 tests no se perdieron, se mudaron a su custodio propio (`test:emu:rules`), donde corren CON emulador real y pasan 4/4.
> Comando nuevo: **`npm run test:emu:rules`** (corre `src/test/rules` CON emulador vía `vitest.emu.config.ts`; antes ningún comando del repo lo corría en condiciones deterministas — ver DEUDAS cerradas).
> ⚠️ El emulador de `emulators:exec` queda huérfano en el 8080 de forma SISTEMÁTICA, no excepcional — medido 6/6 en v6.57.0 (4 corridas con exit 0, 2 con exit 1). Verificar el puerto 8080 ANTES **y DESPUÉS** de cada corrida con emulador (`test:emu`/`test:emu:functions`/`test:emu:rules`/`test:integration`), no solo antes.
> Preferencias: Prompts Claude Code por defecto. Caveman mode. PASO 0 read-only en cada prompt.
> Preguntar ante duda de negocio. NUNCA cerrar en verde sin RUNTIME (lo corre el USUARIO, no Claude).
> npm run build LOCAL antes de merge a master. Un frente a la vez, confirmar cierre antes de seguir.
> backend en prod antes que master.

## DEUDAS
- **DEUDA NUEVA (v6.73.0) — `emulators:exec` emite un `NullPointerException` del runtime de rules DURANTE EL SHUTDOWN.** Medido 3/3 corridas (2026-08-28, frente `[QUOT-FETCH-RANGE]`/`[EMU-IMPORT-RED]`): `Exception in thread "main" java.lang.NullPointerException: Cannot invoke "String.length()" because "line" is null`, en `com.google.firebase.rules.tools.local.server.Server.processRequestsFromInputStream`, siempre DESPUÉS del resumen `Test Files`/`Tests`, durante la secuencia `Stopping ... Emulator`. Evaluado y DESCARTADO como causa de ningún rojo (por posición en el log, no por suposición) — es ruido de teardown del proceso Java de rules-tools, hermano del huérfano sistemático del 8080 (15/15 acumulado, `[with-emulator.cjs]` ya lo reapea solo). No bloquea nada hoy; anotar si algún día se quiere silenciar el log o investigar el pipe que se cierra abrupto.
- **APRENDIZAJE GRABADO (v6.65.0) — `git checkout` está BLOQUEADO por el clasificador del ejecutor**, mismo bloqueo que `Stop-Process` desde v6.62.0. No es transitorio, no se reintenta. Toda operación de rama va por refs directos: `git push origin src:dst` (el servidor valida el fast-forward) + `git branch -f rama hash` (mueve el puntero sin tocar el working tree). Modo de falla SILENCIOSO: en v6.64.0 un `merge --ff-only` fue no-op porque se operó creyendo estar en otra rama tras un `checkout` bloqueado. `git branch --show-current` va ANTES de cada comando de rama que escriba.
- **CORRECCIÓN DE REGISTRO (v6.65.0) — la cabecera de HANDOFF.md v6.64.0 decía push pendiente y refs en `0380b4f7` cuando ya estaban en `b5dfceb9`.** Segunda vez seguida (mismo error entre v6.63.0→v6.64.0). Regla nueva: la cabecera se escribe o re-verifica DESPUÉS del push, nunca antes y sin retocar.
- **CORRECCIÓN DE REGISTRO (v6.65.0) — cuarta imprecisión de expectativa del director, detectada sin resolverla por cuenta propia:** el PASO 0 de esta sesión pedía "SOLO docs/*.md" en un diff donde 3 de 7 archivos vivían en la raíz (`CLAUDE.md`/`GEMINI.md`/`HANDOFF.md`). Se suma a las 3 de v6.64.0.
- **DEUDA NUEVA (v6.64.0) — `isStaff()` incluye OPERATOR: las rules NO replican `ROUTE_PERMISSIONS`.** OPERATOR puede escribir `sales`, `coils`, `production_logs`, las 4 colecciones `*_stock`, y crear `kardex_movements` directamente vía rules, aunque la UI le esconda varias de esas pantallas — defensa de UNA SOLA CAPA. Diseño FASE 1 conocido desde v6.10, ahora MEDIDO por `collections.rules.test.ts` GRUPO S/T, no supuesto.
- **DEUDA NUEVA (v6.64.0) — `customers`/`contacts`/`coil_finishes` sin `delete` admin-only**, a diferencia de `coils`/`sales`/`production_logs`. Cualquier OPERATOR borra un cliente. Asimetría probablemente no intencional, anclada tal como está (`collections.rules.test.ts` GRUPO T); si se quiere cerrar es decisión de negocio, no fix técnico.
- **DEUDA NUEVA (v6.64.0) — `sales/{id}/history` sin guard de campos** (`allow write: if isStaff()`). Escritor real único: `src/app/admin/sales/import/page.tsx:509-516` (client-side, `tx.set` dentro de la `runTransaction` de re-importación, archiva la venta previa). NO candidata a `if false` — cerrarla rompería la re-importación en prod. Corrige el "INDETERMINADO" que este mismo frente había registrado en su propio recon inicial.
- **DEUDA NUEVA (v6.64.0) — `isSignedIn()` es REDUNDANTE: mutarlo a `true` da 0 rojos en toda la suite de rules.** Sus 3 consumidores (`hasRole`/`isAdmin`/`isOwner`) ya cortan solos con `request.auth==null`. No es peligroso (falla cerrado), es un guard decorativo. Misma familia que el aprendizaje de guards decorativos de v6.51.0.
- **DEUDA NUEVA (v6.64.0) — `docs/adr/ADR-002-firebase-firestore.md:139` afirma que las Cloud Functions están "actualmente vacías".** Falso hoy (~15 callables reales en `functions/src/callables/`). Stale de otro dominio, no tocado en este frente.
- **APRENDIZAJE GRABADO (v6.64.0) — el canal del ejecutor NO transporta paste verbatim de outputs largos** (medido 4 veces: 26 líneas de `firestore.rules`, la matriz de `console.log` del probe, los 6 bloques del CSV, y antes el cuerpo de `useCoils.ts` en v6.62.0). SÍ transporta tablas redactadas por el propio ejecutor. Método que funcionó: persistir a disco + pedir agregación con checksums que impidan que la transformación se desvíe.
- **APRENDIZAJE GRABADO (v6.64.0) — un catch-all permisivo de Firestore rules NO es una red de fondo, anula TODAS las rules del archivo** (semántica OR entre `match` — si cualquiera permite, se permite). Medido: `match /{document=**} { allow read,write: if true }` rompió 14 tests de golpe, incluido un archivo hermano con otro `projectId`.
- **APRENDIZAJE GRABADO (v6.64.0) — un test bajo `test:emu:rules` que reporta por `console.log` es INVISIBLE**, incluso con `stdio:'inherit'` en toda la cadena (`with-emulator.cjs`→`emulators:exec`→`vitest`). Todo diagnóstico estructurado en ese pipeline escribe a disco, nunca `console.log`.
- **CORRECCIÓN DE REGISTRO (v6.64.0) — la cabecera de HANDOFF.md v6.63.0 decía `master`/`origin/*` "con push pendiente" cuando ya estaban los 4 refs en `0380b4f7`.** Detectado y corregido al abrir esta sesión.
- **CORRECCIÓN DE REGISTRO (v6.64.0) — `sales/{id}/history` NO tiene escritores indeterminados, tiene 1 escritor real** (ver deuda nueva arriba). El recon inicial de este mismo frente lo había registrado como "INDETERMINADO" y candidato a cerrar; corregido en su propio PASO 0 antes de escribir el test.
- **CORRECCIÓN DE REGISTRO (v6.64.0) — 3 conteos errados del director durante la sesión, detectados por el ejecutor sin resolverlos por su cuenta:** "21/35" en vez de "18/35" bloques restantes en un punto intermedio; "17 restantes" con lista de solo 12 ítems; "5 archivos" esperados en el `git diff --stat` de cierre cuando eran 6 (`helpers/rulesEnv.ts` no contado aparte de los 4 `.rules.test.ts`).
- ~~**DEUDA NUEVA (v6.63.0) — CI abandonado (`.github/workflows/ci.yml`). DECISIÓN DEL DUEÑO YA TOMADA: SE BORRA.**~~ ✅ **CERRADA (v6.65.0).** Borrado (commit `0e3d201a`) — eran 4 jobs, no 1, y el trigger `[main, develop]` tenía la mitad muerta (no existe rama `main`). Detalle en CLAUDE.md v6.65.0.
- **DEUDA NUEVA (v6.63.0) — `scripts/*.cjs` sin cobertura de `tsc` NI de `eslint`.** `tsconfig.json` excluye `scripts` y su `include` solo lista `.ts`/`.tsx`/`.mts`; `eslint.config.js:12` nunca incluyó `.cjs` en su `files` → 59 `no-undef` de `process`/`console`/`__dirname` en los 3 archivos nuevos, idénticos a los que ya tiene cualquier `.cjs` preexistente (`scripts/check_orphan.cjs`, `test-matrix.cjs`). Preexistente, afecta a TODO `scripts/`, no introducido por este frente.
- **DEUDA NUEVA (v6.63.0) — `/scripts/*.ts` (`.gitignore:72`) tiene la MISMA trampa de opt-out silencioso que `/scripts/*.cjs`.** Hoy sostiene 28 archivos `.ts` trackeados por la misma inercia (agregados antes de que la regla existiera). Cualquier `.ts` de infraestructura nuevo bajo `scripts/` nace ignorado salvo `!` explícito.
- **APRENDIZAJE GRABADO (v6.63.0) — `.gitignore` puede tener reglas de OPT-OUT SILENCIOSO para directorios de infra.** `/scripts/*.cjs`/`/scripts/*.ts` existen para no versionar scripts ad-hoc — correcto para eso, pero como regla ciega por extensión también ignora infra PERMANENTE agregada después. Síntoma peligroso: `git status` limpio, tests locales pasan (el archivo existe en disco), y recién se rompe en un clon nuevo o en CI. Toda infra nueva bajo `scripts/` necesita su `!` explícito con comentario.
- **APRENDIZAJE GRABADO (v6.63.0) — `spawnSync("npm.cmd", args)` sin `shell:true` falla con `EINVAL` en Windows** (`.cmd` es batch, no ejecutable). No se vio leyendo el código — se encontró en el primer experimento con un señuelo, antes de tocar la suite real.
- **APRENDIZAJE GRABADO (v6.63.0) — un wrapper de higiene sobre un test runner tiene una responsabilidad que no puede fallar: preservar el exit code original.** El reap vive en `finally` (corre siempre), pero el exit final se lee del resultado de `npm run`, nunca del reap — si el reap enmascarara ese código, una suite roja pasaría por verde.
- **APRENDIZAJE GRABADO (v6.63.0) — `test:emu` y `test:integration` NO eran duplicados inocuos.** `test:integration` corría `vitest` SIN `run` (watch-mode) dentro del mismo `emulators:exec` — nunca terminaba solo. 0 invocadores reales en todo el repo. Se borró, no se convirtió en custodio.
- **DEUDA NUEVA (v6.62.0) — `fetchInventory:170-171`/`210-211` sigue filtrando `status !== 'VOIDED'` client-side post-fetch cuando hay rango de fechas.** Preexistente, misma familia de patrón que el modo memoria que #10 introdujo (fetch amplio + filtro en memoria) — no se tocó, solo se confirma que sigue ahí.
- **DEUDA NUEVA (v6.62.0) — `TablePagination` de `/admin/coils/{aluzinc,drywall}` sigue sin `mode="cursor"`** pese a que una de sus 2 ramas (sin scope de línea) pagina por cursor Firestore. Cosmético/consistencia, no bloqueante.
- **DEUDA NUEVA (v6.62.0) — el modo memoria no cachea entre páginas de la misma línea:** cada cambio de página re-trae el universo completo de la línea (168 docs hoy, aceptable). Revisar si el volumen de `coils` crece sustancialmente (umbral ya registrado ~1000 docs/línea, v6.60.0).
- **DEUDA NUEVA (v6.62.0) — `useFinishes` ahora se invoca 2 veces en el árbol de una página con línea** (dentro de `useCoils` y dentro de `CoilsInventoryView`). No es bug — el hook es barato y estable — pero es una duplicación de llamada que antes no existía.
- **DEUDA NUEVA (v6.62.0) — `fetchInventory`/`fetchCoilsForExport` siguen con 0 tests propios.** La cobertura nueva de #10 (`useCoils.test.ts`, `coilPaging.test.ts`) ancla la lógica de paginación/scope puros, no el servicio Firestore en sí (queries/Algolia reales).
- **NOTA (v6.62.0) — stock negativo reconfirmado en runtime, no es hallazgo nuevo:** `REPRES-ALZ-AZUL-5002-028-2568-00041` (`-39.09 kg`/`-14 ML`) reapareció durante la validación de paginación de aluzinc de #10. Misma bobina documentada desde v6.32/v6.46 — se suma como confirmación viva a la deuda "#3" (negativos/reconciliación, congelada por falta de conteo físico), no requiere fix de código.
- **APRENDIZAJE GRABADO (v6.62.0) — el contenido largo de `useCoils.ts` no se transmitió por el canal del ejecutor** (falló con `git diff` ×2, `Read` y `sed`; fragmentos cortos sí llegaban) — misma familia que la falla ya conocida de `git rev-parse` con múltiples argumentos. Cuando pase: no insistir con más lecturas del mismo archivo por el mismo canal — pedir que los TESTS demuestren el comportamiento (correrlos y leer su output), no releer el archivo.
- **APRENDIZAJE GRABADO (v6.62.0) — `waitFor` sobre un MOCK se satisface al INVOCARSE, no al RESOLVERSE.** Para confirmar que un hook asentó su resultado hay que esperar sobre su ESTADO, no sobre la invocación del mock — un `toHaveBeenCalledTimes(0)` asserteado demasiado temprano es una garantía falsa (el mock puede llamarse recién después).
- **APRENDIZAJE GRABADO (v6.62.0) — el clasificador del ejecutor bloquea `Stop-Process`: el emulador huérfano del 8080 ya no se limpia en soledad.** Toda sesión con `test:emu*` necesita al dueño disponible para liberar el puerto entre suites, o autorización explícita de kill. Huérfano medido **12/12 acumulado** (era 9/9 en v6.60.0).
- **APRENDIZAJE GRABADO (v6.62.0) — `git add -N` (intent-to-add) de VS Code deja entradas con blob vacío (`e69de29b`) que `git status --porcelain` muestra como `A ` sin ser staging real.** No interfiere con `git add` explícito por filename; sí prohíbe `git commit -a` (comitearía contenido no controlado) y `git stash` hasta resolverlo con un add real o un commit.
- ~~**DEUDA NUEVA (v6.60.0) — ⚠️ BUG VIVO, PRIORITARIO: `/admin/coils` con búsqueda de texto + filtro de acabado devuelve SIEMPRE 0 resultados, en silencio.**~~ ✅ **CERRADA (v6.61.0)** — facets `filterOnly` en `coils_index` + `FIELDS` de la extensión ampliado + fix de provider `5c83f7bb`. 8/8 filtros cuadran contra Firestore, particiones completas. Ver cabecera y CLAUDE.md v6.61.0.
- **DEUDA NUEVA (v6.61.0) — `algoliaClient.ts:37-39` convierte CUALQUIER error de Algolia en "sin resultados".** El catch devuelve `{hits: []}` → `fetchInventory` recibe un resultado válido vacío y el catch de `useCoils.ts:120-123` (que sí setea error visible) nunca se entera: tabla vacía sin toast, indistinguible de "no hay bobinas". Fue lo que hizo invisible el bug de provider; afecta a los 5 índices. Frente propio: propagar el error en vez de tragarlo.
- **DEUDA NUEVA (v6.61.0) — `sales_index` y `customers_index` sin facets** (medido 2026-08-25: `status` 0/191, `sellerId` 0/261, `name` 0/1). Los campos SÍ están en el record (`sales_index` 329/330 con los 8) — falta SOLO declararlos, no re-indexar. `production_logs_index` SÍ los tiene funcionando (250/250, 77/77) **sin registro de cuándo ni por quién** — pregunta abierta. `contacts_index` vacío (0/0), no medible. Frente propio.
- **DEUDA NUEVA (v6.61.0) — record fantasma `SMOKE-EDIT-PROD-Q-NATIVA` en `sales_index`** (330 records vs 329 docs; fixture del frente EDITAR v6.53.0, el delete no llegó al índice). Impacto bajo: `fetchSales` re-lee los docs reales de Firestore. Pariente de los 11 `audit_logs` SMOKE huérfanos (v6.55.0 — 8 `SALE_CREATED` + 3 `PRODUCTION_LOG_CREATED`): los cleanups de smoke no barren Algolia.
- **DEUDA NUEVA (v6.61.0) — la paridad test↔prod NO cubre extensiones ni config de Algolia.** `FIELDS` (Firebase console) y `attributesForFaceting` (dashboard Algolia) viven SOLO en prod, sin fuente versionada; `ayrsteel-test` tiene **0 extensiones** → la búsqueda no existe en test y todo fix de búsqueda se valida directo en prod (pasó en este frente). Frente propio: instalar las 5 extensiones en test. Regla de paridad (CLAUDE.md v6.36) ampliada.
- **DEUDA NUEVA (v6.60.0) — `getFinishIdsForLine` (`src/core/coils/domain/finishCompat.ts`, `c337fc0e`) tiene 0 consumidores; su docstring promete reemplazar el filtro inline de `listAvailableCoils`, pero `coilService.ts:319-322` sigue con el inline.** NO es código muerto: se cablea en #10. Client-only por diseño — ningún callable resuelve finishes por línea (los 4 sitios server leen `coil_finishes` por id). **Sin parity test entre las 2 copias de `finishCompat`** (`finishCompat.test.ts` importa solo la cliente); paridad de `assertCoilFinishCompatible` visual, no anclada.
- **DEUDA NUEVA (v6.60.0) — 164 archivos `i/lf w/crlf`** (blob LF correcto, worktree CRLF). Inocuos para git. **Autor NO identificado, 5 comandos descartados por medición:** `npm run test`, `npm run build`, `test:emu`, `test:emu:functions`, `test:emu:rules` — los 5 dejaron `git status` sin archivos trackeados sucios. El proceso que los escribe con CRLF sigue sin ubicarse. Inocuos mientras el índice esté en LF (git normaliza al leer), pero conviene identificarlo antes de que alguno vuelva a entrar al índice con CRLF.
- **DEUDA NUEVA (v6.60.0) — `core.autocrlf=true` a nivel SYSTEM (`C:/Program Files/Git/etc/gitconfig`).** Neutralizado acá por local `false`. Un clon nuevo en otra máquina sin override local reintroduce el fantasma entero. No es nuestro para cambiar; documentar en onboarding.
- ~~**DEUDA NUEVA (v6.57.0) — 34 de 35 bloques `match` de `firestore.rules` con CERO cobertura ejecutada.**~~ ✅ **CERRADA (v6.64.0).** `firestore.rules` pasa de 1/35 a 35/35 bloques `match` cubiertos, 5 archivos de test (`salesStatus`/`foundation`/`locks`/`sales`/`collections`), 84 tests, `firestore.rules` sin modificar (cero líneas). Detalle en CLAUDE.md v6.64.0.
- **DEUDA NUEVA (v6.57.0) — `emulators:exec` deja el emulador HUÉRFANO en el 8080 de forma SISTEMÁTICA, independiente del exit code.** Medido 6/6 en este frente (4 corridas con exit 0, 2 con exit 1) — corrige el aprendizaje previo (v6.51.0/v6.52.0) que lo registraba como excepción ligada a exit ≠0 o "incluso tras exit 0". Candidato a un pre-check/post-check automático embebido en los scripts `test:emu*` (matar huérfano antes de arrancar, o fallar ruidoso si el puerto ya está ocupado), en vez de depender de que quien corre la suite se acuerde de revisar a mano. Frente propio. **Actualizado v6.60.0: 9/9 acumulado.** Las 3 corridas de esa sesión (`test:emu`, `test:emu:functions`, `test:emu:rules`) dejaron huérfano el 8080, las 3 con exit 0. PIDs 20756 / 8808 / 22728, todos `cloud-firestore-emulator-v1.20.2.jar --project_id ayrsteel-test`, matados a mano tras confirmar el `CommandLine`. Ya no hay duda de que es sistemático e independiente del exit code — el pre-check/post-check automático en los scripts `test:emu*` deja de ser "candidato" y pasa a ser el fix que corresponde.
- **DEUDA NUEVA (v6.57.0) — `test:emu` y `test:integration` son el MISMO comando por 2 nombres.** Ambos corren `vitest ... src/test/integration --config vitest.emu.config.ts` dentro de `emulators:exec` con el mismo set de emuladores; la única diferencia real es que uno pasa `run --no-file-parallelism --maxWorkers 1` y el otro no — sin `run`, vitest entra en watch-mode por default y solo corre una vez porque `emulators:exec` no le da TTY interactivo (comportamiento accidental, no diseñado). Medido: mismo resultado exacto, `228 passed | 3 skipped (231)`, en las 2 corridas de este frente. Decidir si se unifica en un solo script o se borra uno — hoy son 2 nombres para lo mismo con un riesgo latente de que `test:integration` se comporte distinto si alguna vez corre con TTY real.
- ~~**⚠️ DEUDA PRIORITARIA (v6.52.1) — STOCK FANTASMA EN 6 NOTAS DE CRÉDITO**~~ ✅ **CERRADA (v6.54.0).** El fix es el replay inverso del import (`documentType==='NOTA CRÉDITO'` → `ncStockAction`), backend deployado a test y prod, backfill de las 6 NC reales aplicado (`ncStockAction: 'RETURNS_STOCK'`), y **2 de las 6 (`FFC1-44`, `FFC1-72`) ya anuladas y validadas en prod con before/after crudos**. Las otras 4 (`FFC1-57/61/64/67`) quedaron backfilleadas pero **SIN anular** por un bloqueo de duplicado de movimientos — **ese bloqueo se LEVANTÓ en v6.58.0 y las 4 fueron ANULADAS en v6.59.0**. Las 6 NC de prod están hoy `VOIDED`, sus 6 perchas `CANCELLED`. Detalle completo en CLAUDE.md v6.54.0, v6.58.0 y v6.59.0.
- ~~**DEUDA (v6.55.0) — retro-gap de `488455d1`: las 6 perchas `COT-FFC1-*` siguen en la cola de producción de prod.**~~ **CORREGIDA Y CERRADA PARCIALMENTE (v6.56.0) — el "6" mezclaba dos casos distintos, separados ahora:**
  - **`COT-FFC1-44`/`COT-FFC1-72`: retro-gap REAL (venta gemela ya `VOIDED` antes de que `488455d1` existiera/se desplegara) → CERRADO.** Backfill en v6.56.0 les puso `status:'CANCELLED'` — ya salieron de la cola. No eran candidatas a que `annulSale` las recorriera de nuevo (su venta ya está `VOIDED`, no se puede re-anular), por eso el fix tuvo que ir por patch manual.
  - **`COT-FFC1-57/61/64/67`: NO es esta deuda.** En su momento seguían en cola LEGÍTIMAMENTE — sus ventas gemelas (`FFC1-57/61/64/67`) NO estaban `VOIDED` (seguían `COMPLETED`), bloqueadas por el duplicado de movimientos de import. **Ese bloqueo se LEVANTÓ en v6.58.0 y las 4 fueron ANULADAS en v6.59.0** (ver deuda `DUPLICADOS FFC1-57/61/64/67` abajo) — hoy las 4 ventas están `VOIDED`, las 4 perchas `CANCELLED`, y ninguna está en la cola de producción (verificado por barrido: 50 docs en `PRODUCTION_QUEUE_FILTER`, cero `COT-FFC1-*`).
- ~~**DEUDA (v6.55.0) — bug confirmado: `annulledSaleRefs[].annulledAt` queda como STRING literal `"SERVER_TIMESTAMP"`, nunca Timestamp real.**~~ ✅ **CERRADA (v6.56.0, commits `545ee08e`+`62116ea5`).** Fix: `translateCascadeFields` resuelve el placeholder a `Timestamp` concreto vía walk recursivo (objetos+arrays) cuando está dentro de un `ARRAY_UNION` — un sentinel de `FieldValue` es ILEGAL ahí (rechazo client-side del SDK, verificado contra emulador). Path `native` (map) sigue con el sentinel real, cubierto por test de no-regresión. Backfill en prod: `COT-FFC1-44`/`72`, fuente = `voidedAt` de la venta gemela (mismo `serverTimestamp()`, misma transacción, coincide al ms con `audit_logs`). Barrido final: 0/329 con `annulledAt` string. Detalle en `docs/modules/annulment.md` §7.
- ~~**DEUDA (v6.55.0) — `src/test/rules/salesStatus.rules.test.ts`: COBERTURA NO DETERMINISTA, no "sin cobertura ejecutada".**~~ ✅ **CERRADA (v6.57.0, commit `65820b9f`).** Custodio propio y determinista: `test:emu:rules` (`vitest.emu.config.ts`, config hijo dedicado que hereda todo del padre y anula solo las 3 entradas de emulador del `exclude`) corre el archivo SIEMPRE con emulador real — ya no depende de que un huérfano ande vivo por accidente. 4/4 passed, medido en corrida cruda repetida 2 veces. La otra framing más vieja ("sin cobertura ejecutada", CLAUDE.md v6.55.0, nunca sincronizada con esta reclasificación) también queda cerrada — ver CLAUDE.md v6.57.0.
- **DEUDA NUEVA (v6.55.0) — 8 `audit_logs` `SALE_CREATED` huérfanos en prod, `userEmail:"SMOKE"`, apuntando a docs ya borrados** (11 total con 3 `PRODUCTION_LOG_CREATED` también huérfanos). Residuo de smokes previos que no incluyeron su propio audit en el barrido. `audit_logs` es append-only por regla — limpiarlos requiere decisión explícita de negocio.
- **DEUDA NUEVA (v6.55.0) — línea de CORTE (#3): reconciliación de inventario CONGELADA por falta de conteo físico, sin saldo inicial cargado.** Cifras verificadas contra prod (`metallic_roofing_stock_movements`/`COB030ROJO`): `ΣSALIDA=91,861.73` (141 mov.) / `ΣENTRADA=45,264.85` (79 mov.), razón ~2.0×, `+3132` sin explicar en `COB030ROJO`. *(Un conteo anterior sin corregir por los duplicados del 17-ago daba `≈128k`/`≈61k`, mismo ratio — cifra vieja, superada por la de arriba.)*
- ~~**DEUDA (v6.54.0) — DUPLICADOS `FFC1-57/61/64/67`**~~ **PARCIALMENTE CERRADA (v6.58.0). El alcance registrado estaba subestimado ×22.** **Las 4 NC fueron ANULADAS en v6.59.0** (ver entrada de cierre arriba) — el bloqueo que las tenía frenadas ya no aplica a ningún doc.
  - **Los 4 conocidos: CERRADOS.** 4 movimientos fantasma del 13-ago borrados en prod (transacción única, backup con restore-test previo). IDs borrados: `AFeBwYTBCTYhSMO9U7kq` (PL030AZ6MT, qty 2, costPerUnit 37.479797), `YrjJnMEslow7bVIGiroI` / `gisRpAJOjL3K7Hz2ZcIT` / `60g2jx3GDU7tsnZRoANo` (PL030RJ6MT, qty 1 c/u, costPerUnit **-0.00477**, costo NEGATIVO = WAC ya corrupto). Sobreviven los 4 del 17-ago (`costPerUnit: 0`, coherente con `baseCost: 0` del doc). Stock ajustado en la misma transacción, política **avgCost congelado**: `PL030AZ6MT` -6096→**-6098**, totalValue -236430.25→**-236507.82**, avgCost 38.78449 intacto. `PL030RJ6MT` -5001→**-5004**, totalValue -166007.41→**-166106.99**, avgCost 33.194843 intacto. Movimientos totales 385→381. Invariante `ΣENTRADA-ΣSALIDA == doc.quantity` (delta 0) preservada en ambos SKUs post-write. 13/13 asserts.
  - **El universo real NO eran 4 documentos: son 89 grupos duplicados.** Medido sobre el histórico completo agrupando por `(sku, type, quantity, reason)`: **89 grupos / 179 movimientos / 45,244.59 unidades de excedente / 7 SKUs.** Frente A cerró 4 grupos → **quedan 85**. Ver "FRENTE B" abajo.
  - **Cómo agrupar (esto es lo que faltaba y costó una ronda entera):** NO agrupar por `adjustedDocument` — ese campo lo escriben **solo** `writeSaleReversal`/`writeAnnulNCDecrement` (NC), y vale la factura ajustada (`FFA1-*`), no el id de la NC. Una query `where adjustedDocument == '<id de NC>'` devuelve **0 docs** y hace invisibles los ~195 movimientos de venta normal. La clave que sí mide el universo es `(sku, type, quantity, reason)` — `reason` lleva el id de venta (`Venta ${saleId} — ${customerName}` en `writeSaleDecrement`).
  - ⚠️ **Esa clave puede dar falsos positivos:** una venta con 2 líneas del mismo SKU e igual quantity genera 2 movimientos idénticos LEGÍTIMOS. Verificar cada grupo contra los `items[]` de su venta antes de borrar nada. En Frente A se verificó: las 4 NC tienen 1 solo item cada una (0 falsos positivos).
- **(v6.58.0) La causa NO era "re-import sin limpiar" — fue un BORRADO MASIVO DE `sales` SIN AUDITORÍA Y SIN REVERSIÓN DE STOCK.** Medido:
  - `audit_logs` tiene **114 `SALE_CREATED` del 2026-08-13**.
  - Docs de `sales` con `uploadedAt` del 13-ago: **0**. Los 114 entityId existen hoy, todos con `uploadedAt` del 17-ago.
  - `audit_logs` con action DELETE/PURGE/VOID entre 13 y 17-ago: 89 registros, **todos de `coils`/`production_logs`, CERO de `sales`**.
  - ⇒ Entre el 13 y el 17 se borraron 114 documentos de `sales` sin revertir stock y sin dejar rastro. El re-import del 17 encontró el terreno vacío, pasó el guard de dedup (`OMITTED` solo si el doc existe) con `isReplacement: false`, y escribió el segundo juego de movimientos. Los 89 del 13-ago quedaron huérfanos.
- **DEUDA NUEVA (v6.58.0) — `sales` no audita borrados. Agujero VIVO, no histórico.** `coils` tiene `DELETE_COIL_DRAFT`; `sales` no tiene equivalente. Un borrado de ventas hoy no deja registro y no revierte stock — puede volver a pasar mañana. Frente propio. Relacionado: los ~15 scripts destructivos sin trackear en la raíz del repo (deuda v6.52.0).
- **FRENTE B (v6.58.0) — 85 grupos duplicados restantes. FUSIONADO CON el ítem #3/línea de CORTE (ver ## COLA, ítem 6).** No arranca sin conteo físico: el grueso de los duplicados son `SALIDA`, así que corregirlos **sube** el stock — y #3 es exactamente la reconciliación de esos saldos. Son el mismo problema, no dos. Excedente por SKU (Σquantity, medido v6.58.0): `COB030ROJO 30,396.99` · `PL030NT6M 4,930` · `COB030AZUL 3,312.6` · `PL030RJ6MT 3,203` (post-Frente A: 3,200) · `PL030AZ6MT 3,152` (post: 3,150) · `PL030NT515 150` · `PL030NT366M 100`. ⚠️ **9 de los 89 grupos NO siguen el patrón 13+17 y hay que tratarlos aparte:** `FFA1-986 | SALIDA | 720` (2 movimientos con timestamp IDÉNTICO al ms, `2026-08-10T23:23:56.452Z` → doble write en una sola transacción, bug DISTINTO); `COB030AZUL | ENTRADA | 360` y `| 180` (mismo día, minutos de diferencia, `createdBy` humano → probablemente 2 producciones legítimas iguales, NO duplicado); los otros 6: pares 10/11-ago, 14/17-ago, 30-jul/17-ago, verificar 1×1.
- **DEUDA NUEVA (v6.54.0) — NC no-metallic + `RETURNS_STOCK` → skip silencioso, fail-safe latente sin cobertura de test.** Si una NC de otra línea (drywall/roofing/trading/services) tuviera `ncStockAction:'RETURNS_STOCK'`, anularla NO tocaría stock en absoluto, sin avisar (`writeAnnulNCDecrement` solo existe en metallic). Hoy inalcanzable (las 12 NC de prod son 100% metallic), pero mudo si el importador algún día trae una de otra línea con esa marca.
- **DEUDA NUEVA (v6.54.0) — `COB030ROJO`: `totalValue` colapsa a `0` al anular porque `avgCost` ya era `0` antes del fix** (dato preexistente de la familia #3 "negativos + valor no confiable", no introducido por este frente). `writeAnnulNCDecrement` preserva `avgCost` en vez de recalcularlo, así que un `avgCost` corrupto en 0 se propaga silenciosamente. Confirmado en runtime real (`FFC1-44`): `totalValue -323120.97 → 0`. Vale la pena revisar cuando se ataque #3.
- **DEUDA-DOC-STRATEGY-VIVA (v6.54.0)** — `docs/03-arquitectura/patrones-y-convenciones.md` (§1 ×2, resumen ítem 2) y `docs/05-formulas/ventas-igv.md` (F-V3 "Paridad") documentan la strategy backend metallic como "muerta / sin paridad" y `annulSale` como "WRITE 9 pendiente". **Falso desde v6.50.0** (`annulSale` es callable server-side y consume `metallicRoofingStockStrategy`). Agravado en v6.54.0 con la 3ª primitiva `writeAnnulNCDecrement`, sí consumida. Banners puestos en los 2 docs (2026-08-21), cuerpo NO reescrito — frente propio de higiene: verificar consumidores reales de cada método de la strategy, estado real de WRITE 9 (`processSale`/`approveQuotation`/el importador siguen client-side), y la deuda relacionada de `canAnnulSale` cliente sin consumidores (v6.52.1, ver abajo).
- ~~**DEUDA (v6.52.1) — `canAnnulSale` cliente (`src/core/sales/annulment/canAnnulSale.ts`) tiene 0 consumidores.** Existe únicamente como fuente de paridad de la copia server (`grep` confirma: solo su definición + el parity test). Hay que mantenerla en sync a mano en cada cambio — este frente tuvo que tocar las 2 copias por eso. Candidata a borrado si nunca se cablea; evaluar en una limpieza futura junto con el resto del directorio `annulment/` cliente.~~ ✅ **CERRADA (v6.70.0, `[CASCADE-DUP]`, commit `f88cbb02`)** — borrada junto con `buildAnnulmentCascade` cliente y los 2 `.parity.test.ts`. Los 5 casos `self` que solo cubría el parity se portaron a `canAnnulSale.test.ts` server-only antes de borrar.
- **NOTA (v6.52.1) — `resolveSaleTwinPath` devuelve `"orphan"` para `mode: "self"`**, así que anular una cotización emite **un solo write** (el propio doc a `VOIDED`) y no toca ninguna gemela. Es coherente con el diseño, pero significa que una percha importada anulada por este path **no deja rastro en su venta gemela**. No se tocó (fuera del alcance); anotado porque el frente de Editar mira el mismo helper.
- **DEUDA NUEVA (v6.52.0) — `SalesReportTab.tsx` / `StagnantTab.tsx` / `ValuationTab.tsx`: 0 consumidores cada uno.** Hermanos de `CustomersReportTab.tsx` (borrado en el ítem 3). Solo `ProductionTab.tsx` del mismo directorio tiene consumidores (6). No se tocaron porque el ítem nombraba únicamente `CustomersReportTab`. Candidatos al mismo borrado, frente chico.
- **DEUDA NUEVA (v6.52.0) — buscador demo muerto dentro de `AdminShell.tsx:20-28`.** `DEMO_RESULTS` con 3 clientes y 3 SKUs **hardcodeados** ("Constructora Andina S.A.C.", RUC `20512345678`, etc.) para un buscador global **no alcanzable por el usuario**: `setSearchQ` no se llama en ningún lado, `results` no se consume en el JSX, no hay input renderizado. Código muerto (`DEMO_RESULTS`, `searchQ`, `results`, tipos `SearchResult`/`ClienteResult`/`SkuResult`). Sin riesgo de que alguien vea datos falsos; candidato a borrado. Anotado por si el buscador estaba planeado.
- **DEUDA NUEVA (v6.52.0) — 4 copias crudas del fetch de `/api/tipo-cambio`** fuera del alcance del ítem 13: `purchases/new/page.tsx:76`, `sales/import/page.tsx:269`, `BulkUploadCoils.tsx:136`, `ReceiveStripsModal.tsx:66`. Migrar a `fetchTipoCambio` (`src/core/coils/services/tipoCambio.ts`), frente chico.
- **DEUDA NUEVA (v6.52.0) — `strips_stock`: 3 docs con `totalStrips`/`totalWeight` en 0 pero `avgCostPerKg` ≠ 0** (`2.3576`, `2.5346…`, `2.3576`). Residuo de la orden PEN de prueba, **NO** del bug USD del ítem 19 (que nunca corrió en prod). Mismo patrón "stock 0 con WAC residual" que metallic resolvió **en display** en v6.31 (`hasStockPosition`); acá el residuo sigue en el dato.
- **DEUDA NUEVA (v6.52.0) — smoke visual de la franja MODO DESARROLLO NO ejecutado.** Había otro `next dev` **ajeno** (PID 2988, puerto 3000) sosteniendo el lock de `.next/dev`; no se mató (terminar el servidor de otra sesión no corresponde sin pedirlo). Cobertura sustituta: 12 tests anclan render/no-render + `pointer-events`/`z-index`/`fixed`/`aria-hidden`. Falta solo la confirmación estética: `NEXT_PUBLIC_FIREBASE_PROJECT_ID=ayrsteel-test npm run dev`.
- ~~**DEUDA NUEVA (v6.52.0) — blobs con CRLF vs `.gitattributes eol=lf`: 12 archivos dan diff FANTASMA permanente.**~~ ✅ **CERRADA (v6.60.0, `4e36435d`).** Cifra real: **290 blobs** (288 `i/crlf` + 2 `i/mixed`), no 12 — el "12" era solo los stat-dirty de ese momento. 192 de los 288 venían de `7c84b384` (bootstrap 2026-06-21). Commit verificado solo-EOL (`--ignore-cr-at-eol` vacío, insertions==deletions 290/290). `i/crlf` hoy = 0. Texto original: `.gitattributes` declara `* text=auto eol=lf`, pero varios blobs viejos están commiteados con CRLF → git normaliza al leer el working tree y compara contra un blob CRLF ⇒ `git status` los muestra modificados **siendo byte-idénticos**, y `git restore` no los limpia (restaura el mismo blob). Bloqueó el `merge --ff-only` de este batch. Los commits del batch renormalizaron a LF los archivos que tocaron; el resto del repo sigue con el problema latente. Fix de fondo: `git add --renormalize .` en un commit dedicado de higiene (cambia muchos blobs, merece frente propio y aviso al equipo).
- ~~**@types/jest/vi faltante** rompe tsc --noEmit~~ ✅ **CERRADA (v6.52.0, item 9).** Eran **4** archivos, no 3, y **2 problemas distintos**: 36 `TS2304` de config (fix con `vitest-env.d.ts` aditivo, **0 paquetes nuevos**) + 3 de un mock incompleto en `queue/page.test.tsx`. `tsc --noEmit` **39 -> 0**.
- **getPeriodDates exportado** de reportFunctions.ts (para reuso en el hook A3).
- **Fuera-de-calibre en grupos multi-espesor REALES** (modo COLOR con ≥2 espesores): hoy se omite (thicknessMm='VARIOS' escapa el guard). Si se quiere flaggear, evaluar por-log antes de consolidar (booleano hasCalibreWarning). Diferido, no hay data que lo ejercite (todo 0.30).
- **Fallback de monto dispara con ===0:** una venta legítima de monto 0 (NC, muestra) se auto-corregiría a baseCost×qty+profit. Improbable en aluzinc, vigilar.
- **functions-sunat/package-lock.json** aparece modificado sin commitear. ✅ **CAUSA RESUELTA (v6.51.0): lo ensucia el propio `firebase deploy`** — el codebase `sunat` tiene `"predeploy": ["npm --prefix \"$RESOURCE_DIR\" install", ...]` en `firebase.json`, y corre aunque el target del deploy sea otro codebase. NO es modificación externa (así estaba mal registrado). Fix candidato: `npm ci` en ese predeploy. Frente chico aparte, no bloqueante, **SIGUE ABIERTO**. ⚠️ **Separación de causas (v6.60.0):** este archivo tenía 2 causas apiladas — (a) este predeploy, VIVO; (b) el fantasma CRLF (era uno de los 288 blobs `i/crlf`), **CERRADO en `4e36435d`**. Hoy está limpio. Si reaparece sucio, es (a). No confundir con `functions/package-lock.json` (v6.48.6), OTRO archivo sin relación con CRLF.
- **isFulfilled se DESINCRONIZA** si se EDITA una cotización ya cumplida sin pasar por produce/void (el forward solo recalcula en produce/void). Forward-fix: recomputar isFulfilled on-edit. Mismo patrón que la deuda 'Editar Cotización desincroniza'.
- **Data de TEST sin backfill de isFulfilled** → badge test da 0 hasta que se produzca ahí. ACEPTADO (sandbox); paridad es de código/índice/functions, no de data de negocio.
- ~~**test-prod.ts** rompe el type-check~~ ✅ **CERRADA (v6.52.0, item 10).** Estaba gitignoreado pero SI entraba al type-check (`include: **/*.ts`); agregado al `exclude` junto a `scripts/` y `src/test`. Verificado con `tsc --noEmit --listFiles`: 1 hit antes, 0 despues. **NO se borro** (el item lo pedia explicito).
- **Nota proceso:** el ejecutor entregó resumen de tests, no los 16 nombres 1x1; se aceptó por conteo (15+1) + verificación manual por vuelta + runtime real. Reforzar 'pegar corrida con nombres' la próxima.
- **COB030ROJO:** PT en stock NEGATIVO en prod (-2,116.94 m / S/ -16,916.97). Corrupcion preexistente (no la introduce este merge; el fix inventario la MUESTRA en rojo, correcto). Backfill/ajuste manual pendiente — frente aparte.
- **Bobinas con currentWeight NEGATIVO en prod = 27, no 1** (registro anterior solo tenía `REPRES-ALZ-AZUL-5002-028-2568-00041`, -39.09). Familia dominante `IMPORT-*ROJO` (~19 de 27), todas status PROCESSED, ~3,857 kg negativos acumulados. Misma familia de corrupción (bug multicorte). Confirmado en el recon de #4; el export de `/admin/coils` ahora las SURFACEA (marcadas "Peso negativo", fuera del total neto) — antes vivían todas fuera del bucket AVAILABLE, invisibles. Frente propio con decisión de negocio pendiente.
- **BOM (U+FEFF) en commits 271a2819 y 96017cff** (PowerShell Set-Content). Decision Opcion A: NO corregido (inofensivo, solo rompe matcheo estricto de conventional-commits). De e92c396c en adelante limpios.
- ~~**functions-sunat/package-lock.json** con CRLF sin commitear (dejar asi).~~ ✅ **YA NO APLICA (v6.60.0):** renormalizado en `4e36435d`, hoy limpio. Si reaparece sucio es el predeploy de `firebase deploy`, no CRLF (ver deuda de arriba).
- **MetallicProductionHistory.test.tsx** sin trackear — decidir si entra al repo.
- **Backups de prod sueltos en el working tree** (sales_full_prod_backup_*.json, backup_recon_*.json, scripts/ids_to_purge.json): gitignoreados pero AUN en disco dentro del repo — mover a ~/ayr-backups/ a mano (tarea del duenio).
- **guard avgCost>0 puede existir en inventarios de otras líneas — verificar/unificar:** (trading catalog L295, roofing catalog L342, roofing inventory L156/L166).
- **getProducedForQuoteLine:** indexa por SKU → duplica en `/production/new` y `SaleDetailsModal` (la cola ya lo resuelve).
- **BUG SOMBRA ROUTE_PERMISSIONS** ✅ **declaraciones muertas BORRADAS (v6.52.0, ítem 7)** — sombra verificada empíricamente reproduciendo el `.find()` sobre el array real de claves; **NO se reordenó el objeto** (el orden de inserción ES el gate vivo, reordenar cambiaría accesos) y se agregó docblock advirtiendo la trampa, para que una futura subruta no se declare después de su prefijo creyendo que gana por especificidad. El **mecanismo** de sombra sigue existiendo por diseño. Registro original: `/admin/lines` sombrea rutas OPERATOR (`Object.keys().find()`). Decisión: acceso actual OK (OPERATOR fuera) → fix = limpiar declaración muerta, NO reordenar. CONFIRMADO en recon #2: `queue/page.tsx` NO tiene check inline de rol; el gate efectivo de toda la cola/producción metallic es ADMIN+SUPERVISOR heredado de este sombreado de `/admin/lines` (OPERATOR fuera de todo `/admin/lines/*` en la práctica).
- **production_log:** no graba perfil TR4/TR5. CONFIRMADO en recon #2: 8 cotizaciones reales en prod con SKU duplicado TR4/TR5 (mismo `COB030ROJO`); `ProductionLog.sku` es el único vínculo → el producido NO se desagrega por línea, solo por SKU. La vista read-only de #2 lo maneja: solicitado fiel por línea + producido por SKU con nota.
- **SalesHistoryTable (perfil de cliente) — CERRADA en #9-A:** estado BINARIO, pinta VOIDED como "Cotización". Fix: `getSaleStatusBadge()` (5 estados) en `salesDisplayLogic.ts`. Ver CLAUDE.md v6.47.
- **fetchSales param customerDoc:** mismo bug de semántica, código muerto (0 callers).
- **reportFunctions.ts:381:** muestra documentNumber como "Doc" (vacío en ventas POS).
- ~~**CustomersReportTab.tsx** huérfano~~ ✅ **BORRADO (v6.52.0, item 3).** ⚠️ Sus hermanos `SalesReportTab.tsx`/`StagnantTab.tsx`/`ValuationTab.tsx` tambien tienen **0 consumidores** y **siguen vivos** — no se tocaron porque el item nombraba solo `CustomersReportTab`. Candidatos al mismo borrado.
- **La regla "sin costo → profit 0"** ya NO la garantiza el builder para quien pasa totales (consecuencia aceptada de separar forma/valor). Hoy funciona porque el POS no los pasa y el importador tiene guard propio.
- **Query legacy de crmService:** borrable post-backfill.
- **confirmQuotationForProduction** sin guard de rol interno (defensa = UI + isStaff, que incluye OPERATOR). Se cierra en Frente 2.
- **`(input as any).length = deleteField()`** escapa el tipado en ProductModal.
- **`crmService.ts:147`** (`getCustomerProfile`) + líneas `159`/`170` conservan el spread viejo `{id, ...data}` (id parásito posible) — NO se tocaron en el fix de mapeos defensivos de esta sesión.
- **`crmService.ts`/`kardexService.ts` sin tests propios** — se tocaron (fix de mapeo `id`) sin red de seguridad de tests.
- **guard isClosed solo client-side** en `consumeCoil`/`sendToCut` (hermético solo en `produceFromCoils`).
- **Importador de ventas sigue client-side directo** (`src/app/admin/sales/import/page.tsx`, `runTransaction`/`writeBatch` sin pasar por callable). Migra en WRITE 9 (`salesService`).
- **Menú de cotización importada ofrece "Editar Cotización"/"Duplicar Operación" — REFINADA en el recon de #9-A, no es lo que se creía:** "Editar" navega a `/admin/sales/[id]/edit`, ruta que NO EXISTE (404) — no hay desincronización real posible por ahí hoy, es un botón muerto. "Duplicar" navega con `?from=X` pero `new/page.tsx` solo lee `?duplicateId=X` — abre form vacío, no duplica. El riesgo real y LIVE es otro: `cancelQuotation` sí ejecuta sobre perchas importadas sin distinguir origen (ver #9-B, bug vivo confirmado). Detalle en CLAUDE.md v6.47 y el recon crudo de #9-A.
- ~~**Forms de bobina duplicados huérfanos**~~ ✅ **BORRADOS (v6.52.0, item 5):** `src/components/forms/AddCoilForm.tsx`, `src/components/purchases/PurchaseCoilFromXml.tsx`, `src/components/inventory/EditCoilModal.tsx`. Verificado antes de borrar que **todos** los consumidores importan por ruta explicita `@/core/coils/components/...` (`InventoryModals.tsx` los 3, `app/admin/coils/page.tsx` el `EditData`) y que no hay barrel ni alias apuntando a esos directorios. Registro original: sin imports en todo el repo (confirmado por grep en la sesión combo UI #7+#8), solo mencionados en `ROADMAP.md`. Mismo patrón que el barrel muerto `BulkUploadCoils.tsx` ya documentado. No reciben los fixes #7/#8 (viven solo en las rutas LIVE bajo `src/core/coils/components/`). Limpieza aparte, frente chico.
- **Registro de bobina con VALOR 0 bloqueado por `coilEntryFormSchema` (`z.coerce.number().positive()`)** — verificado en la sesión combo UI, es comportamiento correcto (bloquea en submit con mensaje claro), no requiere fix, descartado de este frente.
- ~~**`cutOrderService.ts:172-175` doble-conversión de moneda**~~ ✅ **CERRADA EN PROD (v6.52.0, ítem 19).** Fix: borrado el condicional; el costo de **SERVICIO** (`invoice.gravada * invoice.exchangeRate`) **NO se tocó** (esa conversión SÍ es legítima). **CERO backfill, probado no asumido:** recon read-only de prod dio **1 sola `cut_orders`** (ANULADA, bobina de prueba PEN con `exchangeRate: 1` → la rama buggeada **ni se evaluó**), **0 órdenes ejecutadas contra bobina USD**, `strips_movements` en **0**. Era bomba armada, no dato roto. RED→GREEN con 3 tests de integración nuevos (`src/test/integration/cutOrderCost.integration.test.ts`): RED crudo `expected 34 to be close to 10` (**34/10 = 3.4 exacto**, el factor ES el `exchangeRate`). El 3er test (USD + factura de servicio USD → 10.34 PEN/kg) fija que la conversión legítima **se preserva**. Doc `costeo-drywall.md` F-D5 corregida (documentaba el código buggy **sin flaggearlo**). **Ya NO bloquea WRITE 8.** Registro original:  `materialCostPEN = sentWeight * coil.pricePerKg; if (coil.metadata?.currency==='USD') materialCostPEN *= coil.metadata.exchangeRate;`. `coil.pricePerKg` YA está en PEN (Mundo A, confirmado en el recon de frente #1) — multiplicarlo otra vez por `exchangeRate` infla el costo de material ×TC para toda bobina USD que pase por una orden de corte, alimentando el WAC de `strips_stock`. **NO es teórica: `receiveStrips` (donde vive el bug) está importada y en uso real desde `ReceiveStripsModal.tsx`.** Prod hoy: **54 bobinas con `metadata.currency==='USD'`** (28 AVAILABLE, 20 IN_PROGRESS, 6 PROCESSED) — cualquiera de las 48 no-PROCESSED que pase por recepción de flejes dispara el bug. Documentado también (sin flaggear como bug) en `docs/05-formulas/costeo-drywall.md` F-D5. Arreglar ANTES de WRITE 8 (`cutOrder`, "el monstruo") — no meter el fix a mitad de ese frente, es un bug independiente y anterior.
- ~~**fetch de TC duplicado e inline**~~ ✅ **CERRADA (v6.52.0, item 13).** Los 2 forms LIVE migrados a `fetchTipoCambio`. Se destapó que el de `AddCoilForm` era **bug real**: ante `fallback` no avisaba **nada** y el usuario se quedaba con el TC viejo en silencio (los otros 2 forms sí avisaban, con 2 comportamientos distintos entre sí). Verificado leyendo `api/tipo-cambio/route.ts:42-53` que la API **nunca devuelve numero en fallback**, asi que unificar no rompe la decision v6.20 ("fallback 3.75 muerto"). ⚠️ **Quedan 4 copias crudas del fetch fuera del alcance del item:** `purchases/new/page.tsx:76`, `sales/import/page.tsx:269`, `BulkUploadCoils.tsx:136`, `ReceiveStripsModal.tsx:66`. Registro original: `AddCoilForm.tsx` y `PurchaseCoilFromXml.tsx` tenian su propio `useEffect`+`fetch('/api/tipo-cambio?fecha=...')` inline (con pequeñas diferencias de comportamiento entre ambos ante `fallback`). El frente #1 creó `src/core/coils/services/tipoCambio.ts` (`fetchTipoCambio`, con tests) para el modal de edición pero A PROPÓSITO no tocó los 2 forms de alta (para no re-testear el alta en ese frente). Frente chico: migrar ambos a `fetchTipoCambio` y unificar el comportamiento ante fallback.
- **DEUDA NUEVA — `request as any` en 4 tests nuevos de `coilManagement.integration.test.ts`:** mismo patrón que las 13 pruebas preexistentes del archivo (mock de `CallableRequest` incompleto). Micro-deuda, consistente con el resto del archivo, no aislada.
- ~~**Patrón stale closure `useMemo(handlers, [deps-estables])`**~~ ✅ **CERRADA (v6.52.0, ítem 17): 0 instancias vivas.** Barrido de **los 23 `useMemo` de todo `src/`**, no solo las páginas de línea que nombraba la deuda — `trading`/`roofing`/`services` **no tienen `useMemo` en absoluto** y `drywall` solo `kpis`. Los 3 que memoizan handlers o cierran sobre estado están **todos correctos**: `queue/page.tsx:113` (`[router, quotes]`, el bug de v6.43 ya arreglado), `quotations/page.tsx:76` (`[]` es correcto: cada `render` usa solo su argumento `row` + helpers puros de módulo) y `AdminShell.tsx:83` (`[searchQ]`). El resto son cálculos derivados sin handlers. **No hay fix pendiente por este patrón.**
- **Frente B — combobox buscable en /production/new (recon hecho, sin implementar):** 2 `<select>` nativos extensos: cotización (mismo cap 100, 131 reales → 31 invisibles) y bobina (~88 elegibles, sin cap, sin recorte). NO hay combobox/typeahead reusable ni lib instalada (cero Headless/react-select) → construir uno propio sin deps, filtro client-side sobre opciones ya cargadas. PRESERVAR la lógica de cada `onChange`: cotización resetea SKU/quoteItemPending/rows (cascada); bobina → `handleCoilSelect` (resuelve finish + valida densityFactor). Al implementar B, decidir si el select de cotización sube su cap (reco sí, si no el combobox es ciego a 31).
- **Cobertura faltante — test dedicado a "oculta cumplidas por defecto":** el comportamiento del default `hideCompleted=true` (Frente COLA) no tiene test propio. El fix de `renderiza filas mixtas` lo sacó del camino cambiando el fixture a PARCIAL, pero nadie testea explícitamente que una CUMPLIDA se oculte sin togglear (ni que aparezca al togglear "mostrar cumplidas"). Agregado opcional, baja prioridad.
- **DEUDA NUEVA — STUB FALSO `/admin/sales`:** el botón "Descargar Reporte Excel" (`src/app/admin/sales/page.tsx:135`) hace `toast.success("Generando Excel...")` SIN generar ningún archivo. Miente éxito. Frente chico aparte.
- **DEUDA NUEVA (analítica, archivada) — filtro `weightSnapshot` silencioso:** `runAluzincVentas`/`runAluzincCosto` (`reportFunctions.ts:~581`) descartan ítems sin `weightSnapshot` SIN avisar (sin warning/banner), y el snapshot es peso TEÓRICO puro de catálogo (`calcCoverageWeightKg` sobre dims nominales, nunca real). Puede subreportar kg en el reporte de ventas. No urgente.
- ~~**`InDevelopment.tsx` huérfano**~~ ✅ **BORRADO (v6.52.0, item 2).** 67 lineas, 0 consumidores confirmados por grep.
- **NOTA DE PROCESO:** en #9-A el ejecutor sobreescribió `quotations/page.tsx` (un stub `InDevelopment` de Sprint 6B) con `Write` sin leerlo primero — lo reportó él mismo al notar que el archivo ya existía pese a que el recon inicial había dicho "la ruta no existe" (cierto para el gate/sidebar, falso para el archivo). Contenido final correcto, sin pérdida real (el stub no tenía lógica). Reforzar: `find`/`ls` antes de asumir que un archivo no existe, `Read` siempre antes de `Write` sobre paths dentro de `src/app`.
- ~~**APRENDIZAJE GRABADO — `sidebar.tsx` es JSX HARDCODEADO, NO data-driven:** el menú lateral NO se arma desde ningún array... En #9-A se agregó el ítem "Cotizaciones" a `navItems.ts`... **Para agregar/quitar un link del menú: editar `sidebar.tsx` directamente, nunca `navItems.ts`.**~~ ✅ **SUPERADO (`[DOCS-STALE-SWEEP]` PASO 3, v6.74.0) — las 2 afirmaciones y la instrucción quedaron falsas/inaplicables desde v6.49.0/v6.52.0, no solo desactualizadas.** El sidebar (`Sidebar.tsx`, mayúscula) es **data-driven** vía el array `NAV_GROUPS` desde el swap m2 (v6.49.0, commit `9f080f8a`) — ya NO es JSX hardcodeado ítem-por-ítem. `navItems.ts` no es "módulo muerto que sigue ahí": fue **borrado físicamente** en v6.52.0 (`74576bf6`, ver el ítem "`navItems.ts` candidato a borrado completo" 2 líneas abajo). La instrucción vigente hoy — **editar el array `NAV_GROUPS` en `src/components/layout/Sidebar.tsx`** — ya vive documentada con más detalle en `docs/modules/sidebar.md` §1/§6; este bullet quedó redundante y se retira en vez de reescribirse.
- ~~**`navItems.ts` candidato a borrado completo**~~ ✅ **BORRADO (v6.52.0, item 1).** 149 lineas. Re-verificado con grep antes de borrar: 0 consumidores de `navItems`/`NAV`/`ROUTE_TITLES` fuera del propio archivo.
- ~~**`productionConfirmation.integration.test.ts` #10 falla en aislamiento**~~ ✅ **CERRADA (v6.52.0, item 11), `test:emu` a VERDE TOTAL.** Causa: pedia la app **DEFAULT** de `firebase-admin`, pero `setupIntegrationTest` inicializa una app **NOMBRADA** y nunca la default -> pasaba o fallaba **segun el orden de ejecucion**. Fix: alineado al SDK **cliente**, como sus **9 hermanos del mismo archivo** (que hacen los mismos writes y pasan) — el test no necesita privilegios admin; un `initializeApp` guard habria creado una 2a app al lado de la nombrada solo para este test. Registro original abajo:  `Error: The default Firebase app does not exist` al hacer `(await import('firebase-admin')).firestore()` sin `initializeApp()` previo en ese test específico. Reproducido corriendo el archivo SOLO (sin ninguno de los cambios de #9-A en la cadena de imports) — bug de setup del test, no del código de producción. Frente chico de config.
- **DEUDA CHICA (#9-B.1b) — criterio "cotización sin ítems metallic → No aplica" duplicado:** `LinkedQuotationFulfillment` (`SaleDetailsModal.tsx`) lo inlinea (cross-ref comentado) en vez de importarlo de `quotationsViewLogic.ts:66-72`, para no recomputar `buildQueueRow` dos veces sobre el mismo `quote+logs`. Vigilar drift si cambia el criterio en un solo lado. Menor de lo previsto — el gate externo por ítem eliminó la duplicación del branch `none`.
- ❌ ~~**path `self` (`SelfQuoteFulfillment`, botón PRODUCIR) muerto-en-UI, candidato a borrado**~~ **DEUDA RETIRADA (v6.52.0, ítem 8): la premisa era FALSA, el componente está VIVO.** `SaleDetailsModal` tiene **3 entry points**, no solo `/admin/sales`: `production/new/page.tsx:693` carga sus `quotes` con `statusFilter: "QUOTATION"` (`:87`) y pasa una al modal → `resolveSaleQuotationLink` devuelve `{mode:"self"}` → **`SelfQuoteFulfillment` renderiza, con su botón `PRODUCIR`**. Es el camino vivo del flujo de producción; `QuoteDetailsModalLoader.tsx:72` es el tercer entry point. Además el modo `self` del tipo tiene 2 consumidores fuera del modal (`canAnnulSale.ts:35`, `resolveSaleTwinPath.ts:6`). **Borrarlo habría roto la vista de detalle de cotización del flujo de producción.** No volver a proponer el borrado sin re-verificar los 3 entry points.
- **DEUDA (observada en #9-B.2a, ajena al frente) — submit de venta CONFÍA el `baseCost` del carrito, no lo recomputa server-side:** `processSale`/`createQuotation` (`salesService.ts`) hacen `totalCost += item.quantity * item.baseCost` con el valor que trae el `CartItem`, sin releer `avgCost` de stock al momento de guardar (el `avgCost` que sí se lee ahí solo alimenta el movimiento de stock, no pisa `item.baseCost`). Mismo patrón client-side-confía-en-payload que motiva WRITE 9. Verificado en runtime de test con `V-000067`/COB030AZUL.
- **DEUDA — ~15 scripts destructivos sueltos sin trackear en la raíz del repo** (`azul_dump.json`, `delete_fase1_lote1-4.cjs`, `backup_fase0.cjs`, `post_mortem.cjs`, `verify_fase3.cjs`, `verify_logs.cjs`, `verify_marzo_invoices.cjs`, `dump_recon.txt`, `final_output.json`, `micro_recon.cjs`, `micro_recon2.cjs`). Observados en el recon de #9-B.2/#9-B.2a, no tocados ni investigados. Parecen artefactos de una operación destructiva previa no commiteada — limpieza/triage pendiente (tarea del dueño).
- ~~**`backfillQuoteCost.test.ts`: 4 rojos conocidos**~~ ✅ **CERRADA (v6.52.0, item 12) — y eran 14, no 4.** Dos bugs apilados: (1) **hueco de harness** — `backfillQuoteCost.test.ts` (4) y `__tests__/annulSale.integration.test.ts` (10) son tests de integracion que viven en `functions/src/callables/`, **FUERA de `src/test/integration`**, asi que `test:emu` nunca los corria y un `vitest run` pelado los corria **sin** emulador (de ahi el ADC). **Ningun comando del repo los ejecutaba en condiciones** -> script nuevo **`test:emu:functions`**, agregado en vez de ampliar `test:emu` (su `beforeEach` hace `listCollections()` y **borra TODAS las colecciones**). (2) Con emulador arriba, los 4 seguian rojos con OTRO error (`Only ADMIN can backfill quote costs`): `backfillQuoteCost` es `onCall` **v2** y el test lo invocaba con la forma **v1** `wrapped(data, context)` -> `request.auth` `undefined`. Fix: `wrapped({data, auth})`. **14/14 verdes.** Registro original abajo:  Verificado en corrida aislada: los 4 tests (`RED-A/B/C/D`) mueren en el mismo `beforeEach` con `Could not load the default credentials` (GoogleAuth ADC, falta `FIRESTORE_EMULATOR_HOST`), no por un `expect` de negocio. Confirmado disjunto de los 3 frentes de esta sesión (0 solape de imports, vive en `functions/src/callables/`). Anotar como "4 rojos conocidos" al correr la suite completa para no confundir con regresión nueva. Detalle en `scripts/local/verify-backfill.md`.
- **DEUDA NUEVA (v6.48.5) — COMPROBANTE VINCULADO (`/admin/quotations`) candidato-UX a eliminar, PARKEADO.** Con DOCUMENTO mostrando el id completo con prefijo, esa columna dejó de ser la única forma de distinguir importada/nativa. Decisión de UX pendiente del dueño, no tocar sin OK.
- **DEUDA NUEVA (v6.48.5) — entry-point "duplicar cotización" en `/admin/quotations` sin botón, PARKEADO.** La rama `?as=QUOTE` (#9-B.2a-2) ya funciona; falta el trigger de UI en esa vista. `row.id` (con prefijo) ya listo para un futuro `?duplicateId={row.id}&as=QUOTE`. Reabrir acciones ahí es decisión de negocio (revierte parcial el "sin acciones" de #9-A) — no implementar sin pedido explícito.
- **DEUDA NUEVA (v6.48.5) — font swap Geist→IBM Plex + fluid type (`globals.css`/`layout.tsx`): DESCARTADO.** Cambios sueltos ajenos a los 3 frentes de la sesión, revertidos con `git restore`. Retomar como frente propio con recon aparte si se quiere (impacto visual global).
- **DEUDA NUEVA (v6.48.6) — `annulSale` D1 es client-side → bypasseable.** Endurecer en WRITE 9 (guard server-side).
- **DEUDA NUEVA (v6.48.6) — `annulSale` reversión usa `pricePerKg` VIVO del coil vs `baseCost` CONGELADO en el flujo normal de stock.** Dos mecanismos de costo distintos conviviendo — observación, no bug confirmado.
- **DEUDA NUEVA (v6.48.6) — `annulledSaleRef` write-only (0 lectores).** El path nativo lo sigue escribiendo; candidato a borrado en limpieza aparte.
- **DEUDA NUEVA (v6.48.6) — hardening futuro: denormalizar `hasActiveProduction` en la percha** para bloqueo D1 atómico dentro del `runTransaction` (evita la mini-race de la query pre-txn actual). Toca writers `produceFromCoils`/`voidProductionFromCoils` + backfill. Frente propio.
- **DEUDA NUEVA (v6.48.6) — `functions/package-lock.json` desincronizado bloquea TODO deploy de `functions/`** (Cloud Build `npm ci` aborta). El lock COMMITTEADO está OK (`npm ci` local pasa); es el working tree el que se ensucia externamente. Workaround: `git checkout -- functions/package-lock.json` antes de deployar. Fix de fondo (qué lo ensucia) pendiente, frente chico.
- **DEUDA NUEVA (v6.48.6) — `scrap.ts:150`/`164` mismo patrón `.toMillis()` sin guardia** sobre `createdAt` de `production_logs`/`coils`. NO verificado si hay corrupción ahí (sizing fue específico a `sales.timestamp`). Candidato a extender `toMillisSafe`.
- **DEUDA NUEVA (v6.48.6) — ~11 call-sites de UI cliente con `.toDate()`/`.toMillis()` sobre `sale.timestamp` sin guardia** (`SalesTable.tsx`, `SaleDetailsModal.tsx`, `PrintableTicket.tsx`, `reportFunctions.ts`, `dashboardService.ts`, `MetallicProductionHistory.tsx`, `SalesHistoryTable.tsx`, `useCoilScraps.ts`, ver `scripts/local/recon-void-sizing.md`). Dormidos (prod limpio); referencia defensiva ya existente `getTimestampMillis` (`queueLogic.ts:109`). Hardening opcional.
- **DEUDA NUEVA (v6.48.6) — forense pendiente: qué script de seed/restore de TEST serializó `sales.timestamp` como objeto plano.** Confirmado QUE pasó, no CUÁL script (ver `scripts/local/recon-void500.md`).
- **DEUDA NUEVA (v6.48.6) — `metallic_roofing_stock/COB030AZUL`: `quantity:-567.5`, `totalValue:-4468.16` (negativo).** Visto de pasada en el recon del void 500, no investigado — familia de la deuda de stock negativo ya conocida (v6.46).
- **DEUDA NUEVA (v6.50.0) — `ProductionBlockedAnnulModal` no disparó en un smoke real de browser (prod), cayó a toast fallback.** El bloqueo backend/rules funcionó bien (la venta no cambió de status), es solo la UX del modal/CTA la que no se vio. Investigado a fondo: body HTTP crudo del callable capturado (shape correcto), código fuente de ambos SDKs (`firebase-functions`/`https.js`, `@firebase/functions`/`index.cjs.js`) verificado línea por línea, mecanismo end-to-end correcto, sin bug de código encontrado. Hipótesis: timing de cache de Vercel edge post-deploy (~1 min entre deploy y smoke). No re-verificable (fixture ya borrada). Reintentar en frío con DevTools Network tab si reaparece. Baja prioridad.
- ~~**DEUDA (v6.50.0) — `functions-sunat/package-lock.json` sigue apareciendo modificado sin razón aparente.**~~ ✅ **DIAGNOSTICADA en v6.51.0** — la causa es el predeploy del codebase `sunat` en `firebase.json` (`npm install`), disparado por cualquier `firebase deploy`. Comprobado: tree limpio antes, sucio después. Ya no es misterio; falta el fix (`npm ci`).
- **DEUDA NUEVA (v6.50.0) — migrar `approveQuotation`/`cancelQuotation` a callables server-side** (paridad con `annulSale`, cierra D1 residual client-side). `approveQuotation` además tiene un guard negativo débil que no excluye `VOIDED` explícitamente — la rule nueva lo tapa mitigando el síntoma, no la causa.
- **DEUDA NUEVA (v6.50.0) — considerar paquete compartido para los helpers puros de `annulment`** (hoy duplicados mecánicamente cliente↔server con parity tests como única red de seguridad). Evaluar cuando un 3er/4to dominio necesite el mismo patrón.
- **DEUDA NUEVA (v6.67.0) — 10 refs cruzadas numéricas sin verificar en `CLAUDE.md`** (líneas 163, 169, 183, 203, 276, 303, 347, 800, 802, 803). MEDIDAS-DESCONOCIDAS, no sanas: en la población equivalente de v6.66.0, 5 de 7 estaban off-by-one. Frente propio, read-only.
- **DEUDA NUEVA (v6.67.0) — 7 archivos de `docs/` con hits de staleness semántico NO tocados, con razón declarada 1x1** (ver CLAUDE.md v6.67.0, C3). Ninguno es falso hoy, pero `firestore-rules-explicadas.md` sigue con su cuerpo stale desde Sprint 1 bajo banner, y su reescritura sigue siendo frente propio.

## PARKEADOS (esperando OK explícito del dueño, no implementar sin pedido)
- **COMPROBANTE VINCULADO (`/admin/quotations`):** candidato-UX a eliminar (redundante con DOCUMENTO ya mostrando prefijo). Ver CLAUDE.md v6.48.5.
- **Entry-point "duplicar cotización" desde `/admin/quotations`:** rama `?as=QUOTE` funciona end-to-end, falta el botón/trigger de UI en esa vista. Ver CLAUDE.md v6.48.5.
- **Font swap Geist→IBM Plex + fluid type:** descartado esta sesión, candidato a frente propio con recon aparte si se retoma. Ver CLAUDE.md v6.48.5.

## PENDIENTE INMEDIATO
- **FRENTE "ANNUL NC INVERSO" CERRADO EN PROD (v6.54.0)** — commits `3a448834` (C1 backend) + `cedbb118` (C2 frontend), backend deployado a `ayrsteel-test` Y `ayrsteel-2026` (mismo commit, 421.42 KB ambos). Anular una NOTA DE CRÉDITO ahora es el **replay inverso exacto del import**: si repuso stock (`ncStockAction:'RETURNS_STOCK'`), anularla lo retira; si fue money-only/ausente, no toca nada. Guard de 2 niveles en `sales.ts` (`documentType`→`ncStockAction`), primitiva nueva `writeAnnulNCDecrement` (metallic, server-only, costo congelado), `ncStockAction` PERSISTIDO desde el importador (antes moría, 0/329 docs en prod lo tenían), `stockEffect` + audit NC-aware. `canAnnulSale` intacto (verificado `git diff` vacío en ambas copias).
  - **Runtime validado en 2 capas:** test (2 fixtures orphan sembrados a mano, RETURNS_STOCK + money-only) y **prod real** (`FFC1-44`/`FFC1-72`, path `imported` con percha, before/after crudos: `COB030ROJO -41664.88→-43464.88`, `COB030NATURAL 1200→0`, `PL030NT6M -9346→-9347`).
  - **Backfill de prod:** las 6 NC reales con `ncStockAction:'RETURNS_STOCK'` (campo único, `update()`, backup previo probado restaurable). **2 ya anuladas** (`FFC1-44`/`FFC1-72`); **4 quedan sin anular** — el bloqueo por DUPLICADOS de movimiento se **LEVANTÓ en v6.58.0** (ver deuda), pero la anulación de esas 4 sigue sin correrse a propósito.
  - Suite **1463 verdes / 3 skip / 0 rojos**. `tsc` 0. Backups en `~/ayr-backups/2026-08-21-nc-backfill/`. Ruido de test sin limpiar (a propósito): `COB030AZUL` en `ayrsteel-test` quedó en `-200` (run pre-deploy con código viejo), `PL030NT6M` en `17950` (delta legítimo del smoke correcto).
  - Detalle completo en CLAUDE.md v6.54.0.
- **FRENTE EDITAR CERRADO EN PROD (v6.53.0)** — E1+E2+E3, runtime end-to-end validado por el dueño en `ayrsteel-test` antes del merge. Editar una cotización **NATIVA** en `QUOTATION` es una operación real, server-side, con UI en `/admin/quotations/[id]/edit` y botón gateado en `/admin/quotations`.
  - **E1** (`f80b423d`): dominio puro `buildQuotationDoc`/`buildSaleDoc` + `classifyLine` server-side, 48 casos de parity. Son **4** los bloqueos al import cross-boundary (el 4º nuevo: `catalogImport.ts` importa `xlsx` a nivel de módulo).
  - **E2** (`2ccb5643`): callable `editQuotation`, **desplegado en test Y prod** (414.32 KB ambos). Guards ADMIN-only + origen + producción activa + estado. Smoke real 33/33 test, 6/6 pasivo + 22/22 activo en prod. `updateQuotation` huérfana **borrada** (D6).
  - **E3** (`46cb5ba8`): UI. Ruta sin declarar en `ROUTE_PERMISSIONS` (la cubre el prefijo), `canEditQuotation` como gate de fila, `parseEditError` nuevo, modal renombrado a `ProductionBlockedModal` con copy opcional, y `cartLogic` extraído del POS con parity.
  - **Invariantes T1/T2** y el recompute Q2(b) con sus 3 guards: ver CLAUDE.md v6.53.0.
  - ⚠️ **En prod el universo editable está VACÍO: 0 nativas en `QUOTATION`.** El botón no aparece en ninguna fila hasta que se cree una cotización nativa. Correcto y esperado.
  - Bitácora por sub-frente en `scripts/local/edit-e1.md` · `edit-e2.md` · `edit-e3.md` · `edit-e4-runtime.md` (gitignored).
- **DEUDA NUEVA (v6.53.0) — `IGV_RATE = 0.18` sumó una redeclaración más.** La página de edición la declara igual que `new/page.tsx` (para reconstruir `unitValue` al precargar ítems viejos). Ya eran 6+ en el repo; sigue sin leerse `settings.igvRate`. Deuda vieja, ahora un poco más grande. Ver `docs/05-formulas/ventas-igv.md` F-V2.
- **DEUDA NUEVA (v6.53.0) — la página de edición duplica la búsqueda de cliente de `new/page.tsx`** (Algolia + debounce + click-outside + `fetchClientData`). Se extrajo lo **puro** (`cartLogic`), pero esa parte hace I/O y tiene semántica distinta en cada página (al editar, el estado inicial sale del doc; al duplicar, del maestro), así que se copió adaptada en vez de forzar un hook compartido. Candidata a `useCustomerSearch` si aparece un 3er consumidor.
- **NOTA (v6.53.0) — el rename del test del modal figura como D+A al umbral por defecto de git.** `ProductionBlockedAnnulModal.test.tsx` → `ProductionBlockedModal.test.tsx` es un rename real, pero al agregarle los 3 casos de copy el archivo casi se duplicó (57 → 101 líneas) y la similitud cayó a **39%**, bajo el 50% que git usa por defecto. Se ve como `R039` con `git show -M30%`, y `git log --follow` sigue la historia. **No hay nada roto**: git guarda snapshots e infiere renames al diffear. El componente sí figura `R069`.
- **`fix-annul-self-block` CERRADO EN PROD** (`91fdbb81`, develop==master==origin/develop==origin/master; backend desplegado a test Y prod ANTES del merge). **Dos daños del mismo flujo:**
  - **(a) D4-self** — `annulSale` no bloqueaba una `QUOTATION` con producción ACTIVE. `resolveSaleQuotationLink` clasifica **toda** `QUOTATION` como `mode: "self"` (chequea `status` **antes** de `relatedQuotationId`/`originQuoteId`), y el bloqueo estaba gateado a `mode === "linked"` en **DOS** sitios (`sales.ts:88` la query, `canAnnulSale.ts:39` el bloqueo) — arreglar uno solo no alcanzaba. **#9-B.2b cerró solo `linked`.** Superficie real **medida** en prod: **84 perchas `COT-*` / 108 logs ACTIVE** anulables pre-fix (el smoke pasivo lo probó en positivo Y en negativo sobre las 84, con cero writes). ⚠️ **NO era alcanzable desde la UI** (botón gateado a `COMPLETED`), solo por invocación directa del callable.
  - **(b) STOCK FANTASMA** — el loop de items no tenía gate de status, así que anular una `QUOTATION` corría `writeSaleReversal` e inflaba `metallic_roofing_stock` sobre un doc que **nunca descontó**. Confirmado en código (`createQuotation` no llama a ninguna strategy; el importador atribuye el descuento a la VENTA, no a la percha; `approveQuotation` crea un doc nuevo) y en dato (**0 de 1.178 movimientos** referencian una cotización). Fix = **señal B, allowlist positiva fail-safe**: revierte solo si `status === 'COMPLETED'`.
  - **Helper nuevo `hasActiveProductionForQuote`** (`functions/src/utils/`) — **ya en prod y listo para que Editar lo consuma**. Vive en `utils/` y no en `domain/` porque hace I/O (`domain/` es puro); no inline en `sales.ts` para no acoplar dos entry points.
  - **RED→GREEN** con 4 rojos crudos (`promise resolved "{success:true}" instead of rejecting` · `expected 110 to be 100`, +10 = quantity exacto). Fixtures `F8`/`F9` + `stockBaseline` nuevos (los 7 previos invocaban `annulSale` sobre una VENTA, ninguno sobre `QUOTATION`; y el seeder no sembraba stock, así que el daño ni era observable). +5 casos `self` al parity test.
  - **Smoke real:** test 18/18 · prod 5/5 pasivo (barrido de las 84 reales, cero writes) + 12/12 activo con seed propio; el caso de stock se validó con **footprint cero** usando un SKU inexistente. Data de prod sin cambios verificada (`197/1/130`, 250 logs ACTIVE, 18 docs de stock).
  - **1261 verdes, 3 skipped, 0 rojos** en todo el repo. Detalle completo en CLAUDE.md v6.52.1; bitácora paso a paso en `scripts/local/fix-annul-self-block.md` (gitignored).
  - ⚠️ **Lo que NO cubre:** las 6 notas de crédito `COMPLETED` — ver la deuda prioritaria arriba.
- **BATCH DE LIMPIEZA `chore/batch-cleanup` CERRADO EN PROD** (`b6216e0a`, develop==master==origin/develop==origin/master, merge **LINEAL ff-only** — los 15 commits del batch quedan atómicos y auditables, NO se squashearon). 19 ítems enunciados: **17 ejecutados, 2 SKIP justificados con evidencia, 0 FAIL.** 100% frontend/tests/docs — **cero deploy de Functions, cero índice, cero write a prod**.
  - **Señales antes → después:** `tsc --noEmit` **39 → 0** · `test:emu` `218/3skip/1 failed` → **222/3skip/0 (225)** · `functions/` *(sin comando que la corriera)* → **14/14** · unit **982/14 failed** → **994/994**. **1230 verdes, 3 skipped, 0 rojos en todo el repo.**
  - **Los 2 SKIP:** ítem 6 (`isRoleAllowed`) — 0 hits en todo el repo, se fue con el `sidebar.tsx` viejo en v6.49.0, **nada que borrar**. Ítem 8 (`SelfQuoteFulfillment`) — **premisa falsa, está VIVO** (ver deuda retirada arriba), borrarlo habría roto la vista de detalle de cotización del flujo de producción.
  - **Fixes de comportamiento real (no solo limpieza):** (a) **`cutOrderService` doble conversión USD→PEN** (ítem 19, ver deuda cerrada arriba — RED→GREEN, 3 tests de integración nuevos, cero backfill, desbloquea WRITE 8); (b) **`AddCoilForm` se comía el fallback de TC en silencio** (ítem 13); (c) **`test:emu:functions`**, comando nuevo que expone 14 tests que **ningún comando del repo estaba corriendo en condiciones**.
  - **3 reclasificaciones de deudas mal registradas:** `SelfQuoteFulfillment` no es código muerto · los rojos de `functions/` eran **14, no 4** (causa estructural: viven fuera de `src/test/integration`) · el patrón stale-closure tiene **0 instancias vivas** (barrido de los 23 `useMemo` del repo). Las tres cerradas/retiradas arriba.
  - **Franja MODO DESARROLLO viva** (ítem 15), banner dormido de `AdminShell` **borrado** (ítem 18, 0 callers le pasaban la prop — jamás se renderizó) + `--color-env-emul` huérfano removido. La franja queda como **único** mecanismo de entorno.
  - ⚠️ **Pendiente del dueño: smoke visual de la franja** — es lo único del batch sin confirmar en runtime. Todo lo demás está cubierto por tests.
  - Detalle completo en CLAUDE.md v6.52.0; bitácora ítem por ítem con RED crudos y decisiones en `scripts/local/batch-cleanup.md` (gitignored).
- **#9-B.2c (annulSale swap completo a callable server-side + rules cerradas) CERRADO EN PROD** (commit `204bbada`, develop==master==origin/develop==origin/master). `annulSale` deja de ser client-side y pasa a Cloud Function v2 gen2 (`functions/src/callables/sales.ts`), invocada desde un wrapper thin de `httpsCallable` (`salesService.ts`, ~15 líneas, sin catch/rewrap para preservar `error.details`). Cascada D1/D2/D3 preservada y endurecida: D1 bloqueo por producción activa ahora con `details` estructurados (`{quotationId, activeLogIds}`) + guards nuevos de auth/rol (ADMIN/SUPERVISOR, antes cualquier `isStaff()`); D2 nativo `annulledSaleRef` pasa de string plano a OBJETO (`{saleId,saleNumber,annulledAt,annulledBy}`, cambio de contrato intencional); D2 importado ahora SÍ escribe algo (`annulledSaleRefs` array, antes no tocaba nada — gap cerrado); D3 costos sincronizados nunca se revierten, igual que antes. Dominio puro duplicado mecánicamente en `functions/src/domain/annulment/` (cross-boundary import no viable, `TS6059` + `firebase.json source:"functions"`) con 4 parity tests. `getStockStrategy` portado completo a `functions/src/domain/strategies/` (roofing/trading/services nuevos, drywall extendido aditivamente). `firestore.rules`: `sales.status` cerrado a `VOIDED` solo vía callable + doc `VOIDED` terminal/congelado (preserva `approveQuotation`/`cancelQuotation` client-side, que siguen sin migrar). Hotfix bundleado: `admin.firestore.FieldValue` (namespace, resolvía `undefined` en emulador local) → import modular, mismo patrón que 16 archivos ya existentes; de paso se borró el test obsoleto `annulSaleCascade.integration.test.ts` (aserciones contra el contrato viejo). 88 tests nuevos, `test:emu` final 217/3/1(221) mismo rojo preexistente. Smoke real en `ayrsteel-test` + prod (callable HTTP directo + UI real vía `/admin/sales`). **Deuda: `ProductionBlockedAnnulModal` no disparó en el smoke de browser (cayó a toast), investigado a fondo sin encontrar bug de código — hipótesis timing de cache Vercel edge, sin repro.** Doc de verdad nuevo `docs/modules/annulment.md`. Detalle completo en CLAUDE.md v6.50.0.
- **Sidebar swap a acordeón m2 (panel hundido) CERRADO EN PROD** (commit `9f080f8a`, develop==master==origin/develop==origin/master). `sidebar.tsx` (JSX hardcodeado, acordeón exclusivo desde v6.48.5) reemplazado por `Sidebar.tsx`, DATA-DRIVEN (`NAV_GROUPS: NavEntry[]` — deja obsoleta para este archivo puntual la deuda histórica "sidebar.tsx es JSX hardcodeado" de #9-A, `navItems.ts` sigue muerto aparte). Mismo mecanismo de acordeón exclusivo (`sidebarAccordion.ts`, reusado sin tocar) con piel visual m2. Helper puro nuevo `filterGroupsByRole` (7 tests). Portado `getProductionQueueCount`+`useEffect` (badge Cola live intacto). Colapsado con popover flotante local (`absolute left-full`, sin Radix/portal), validado en runtime que NO clippea en md (768-1023px). Ancho 260px/72px (proto original 272px, preservado 260px por contrato con `AdminShell` — deuda de duplicación de ancho registrada). Validado antes en preview standalone (`/sidebar-preview`, rama `feat/sidebar-accordion-preview`, nunca mergeada — descartada). `sidebar.test.ts` (shadow test) borrado con `sidebar.tsx`; `Sidebar.test.ts` nuevo 20/20 GREEN. Suite completa 892/896, 4 rojos preexistentes ajenos. Runtime validado por el dueño en preview de rama → develop → prod (breakpoints, roles, auto-open por ruta, badge Cola, popover sin clip). Frontend-puro. Doc de verdad nuevo `docs/modules/sidebar.md`. Detalle completo en CLAUDE.md v6.49.0.
- **#9-B.2b (annulSale bloquea anulación con producción viva + limpia twin nativo) CERRADO EN PROD** (merge `a7e06d9c`, develop==master==origin). D1: bloquea (no cascadea) si la percha vinculada (`resolveSaleQuotationLink`, ambos campos) tiene producción `ACTIVE` (`hasActiveProduction`, query pre-txn — Firestore txn no corre queries). D2 nativo: limpia `convertedToId` con `deleteField()`. D2 importado: intacto (`annulledSaleRef` dropeado del plan, era write-only). D3: `costSource` intacto. 10+ tests RED→GREEN, runtime end-to-end (bloqueo + release) probado. Frontend/client-side. Detalle completo en CLAUDE.md v6.48.6.
- **Fix void 500 (`toMillisSafe`) CERRADO EN PROD** (commit `5f17db81`, backend deployado a prod ANTES del merge a master). El guard `laterSales` de `voidProductionFromCoils` y su gemelo en `revertProductionLog` crasheaban (`TypeError`, 500 crudo) si `sale.timestamp` era un objeto plano en vez de `Timestamp`. Helper puro `toMillisSafe` (`functions/src/domain/timestamps.ts`), consumido por ambos guards, degrada a bloqueo conservador (`failed-precondition`) en vez de crashear. Prod 0/328 corruptos → preventivo. Smoke TEST verde (destranca `FFA1-1290` + cierra el void→anular de #9-B.2b). Detalle completo en CLAUDE.md v6.48.6, recon crudo en `scripts/local/recon-void500.md`/`recon-void-sizing.md`, deploy en `scripts/local/deploy-void.md`.
- **#9-B.2a-2 (intent de duplicar por status del origen) CERRADO EN PROD** (merge `6a1dc3a6`, develop==master==origin). Al duplicar desde `/admin/sales`, el intent (COTIZAR/VENDER) se deriva del `status` del origen y pre-selecciona el botón: `COMPLETED`→VENDER, `QUOTATION`→COTIZAR, vía `&as=SALE|QUOTE`. NUNCA auto-submit, solo énfasis visual — ambos botones siempre clickeables. Helpers puros `duplicateIntentFromStatus`/`parseDuplicateIntent` (`salesDisplayLogic.ts`), `onDuplicate(saleId,status)` firma extendida en `SalesTable.tsx`. Rama QUOTE mitad-muerta-en-UI desde #9-A (sin entry point), validada por URL directa. 10 tests RED→GREEN, build verde. Frontend-puro. Detalle completo en CLAUDE.md v6.48.5.
- **Sidebar en acordeón exclusivo CERRADO EN PROD** (merge `2393a70d`, develop==master==origin). Los 6 grupos top pasan a acordeón EXCLUSIVO (abrir uno cierra el anterior). Estado `openGroup` en `sidebar.tsx`, sync a ruta activa. Helper puro nuevo `sidebarAccordion.ts` (`nextOpenGroup`/`resolveInitialOpenGroup`/`shouldShowGroupItems`), 13 tests RED→GREEN. Colapsado (icon-rail) → acordeón inerte, todos los ítems visibles. INVARIANTE: `LineGroup`/`openLines` (acordeón de líneas, no-exclusivo) es mecanismo separado, byte-idéntico sin tocar. Frontend-puro. Detalle completo en CLAUDE.md v6.48.5.
- **Quotations: columna DOCUMENTO con id completo CERRADO EN PROD** (merge `a794d23f`, develop==master==origin). `/admin/quotations` mostraba el número pelado (`formatQuoteDisplayId(row.id)`) — para importadas idéntico al comprobante real, sin marca de que era cotización. Fix: celda renderiza `row.id` crudo con prefijo (`COT-BBV1-238`/`C-000020`). Subtítulo gris redundante eliminado (repetía el mismo dato que ya alimentaba "Comprobante Vinculado", confirmado con docs reales de prod). Búsqueda sin gap (row.id ya en `searchFields`, substring case-insensitive). Cosmético, frontend-puro, sin test nuevo (JSX sin lógica pura). Detalle completo en CLAUDE.md v6.48.5.
- **#9-B.2a (reactivar botón Duplicar) CERRADO EN PROD** (develop==master==origin). Dos bugs reales corregidos en el mismo commit: (a) mismatch de param `?from=` → `?duplicateId=` en el origen (`/admin/sales/page.tsx`); (b) bug de semántica en `loadDuplicate` (`new/page.tsx`) — usaba `data.documentNumber` (comprobante) para el campo RUC/DNI del form, corregido a `data.customerDocument` (semántica v6.28). Gating nuevo `canDuplicate(status)` (`salesDisplayLogic.ts`), allowlist `["COMPLETED","QUOTATION"]`, sin gate de rol, sin excluir importadas (Duplicar solo lee, no muta). Verificado en runtime de test que `baseCost=0` en el duplicado es fiel al WAC vivo del SKU, no un gap del flujo. 6 tests nuevos RED→GREEN, 16/16 el archivo, build verde. Frontend-puro. Detalle completo en CLAUDE.md v6.48.4.
- **#9-B.1b (Estado de Producción en el detalle de venta) CERRADO EN PROD** (merge `da6ffbc4`, develop==master==origin). Helper puro `resolveSaleQuotationLink` (`saleProductionLink.ts`, 9 tests): 3 modos self/linked/none, precedencia `relatedQuotationId` > `originQuoteId`. `SaleDetailsModal`: gate único al tope por ítem metallic (`sale.items?.some(businessLine==='metallic-roofing')`, NO `sale.businessLines`); adentro, modo `self` reusa el componente original sin cambios (`SelfQuoteFulfillment`), modo `linked` es nuevo y read-only estricto (`LinkedQuotationFulfillment`, reusa `buildQueueRow`+`buildQuoteDetailView`, degrada con gracia si la percha fue purgada), modo `none` = "No aplica". 48/48 suite unit GREEN, build local verde. Frontend-puro, cero backend/índice/callable. Detalle completo en CLAUDE.md v6.48.3.
- **#9-B.1 (guard cancelQuotation importadas + separación TOTAL de ventas/cotizaciones) CERRADO EN PROD** (merge `5a9e5504`, develop==master==origin). Guard en `cancelQuotation` (`salesService.ts`, dentro de la txn): `if (data.relatedSaleId || data.metadata?.isQuotation) throw` — bloquea cancelar perchas importadas (74/130 con producción real, `isFulfilled:true`); nativas siguen igual. UI (`SalesTable.tsx`): "Cancelar" oculto en importadas (`hidden: isImportedQuotation`), "Editar"/"Duplicar" ocultos SIEMPRE (`hidden:true` — ambos eran botones muertos, no arreglados, solo tapados: 404 y mismatch de param respectivamente). Dropdown (`SalesFilters.tsx`) sin las opciones de cotización — solo "Todas"/"Ventas Cerradas". Parte E (búsqueda de texto): el filtro Algolia server-side por status se REVERTÍA (rompía la búsqueda entera, `status` no facetable) y se RESOLVIÓ distinto — filtro CLIENT-SIDE sobre los docs Firestore reales ya traídos (`filterSalesExcludingQuotations`), sin depender del facet. Contador corregido en 2 campos (`totalCount` y `listTotalCount`, ambos a `sales.length`). Separación completa por las 3 vías: dropdown + guard + búsqueda. 8 tests unit + 3 integración GREEN. Detalle completo en CLAUDE.md v6.48.2.
- **#9-A (separar ventas/cotizaciones en vistas) CERRADO EN PROD** (merge `9b03e678`, develop==master==origin). `/admin/sales` filtra `where('status','in',['COMPLETED','VOIDED'])` vía `buildListStatusFilter` (whitelist robusta, índice `{status,timestamp}` ya declarado, cero índice nuevo — antes `['COMPLETED','QUOTATION','CONVERTED']`, mezclaba las 130 perchas `COT-*`). Nueva ruta `/admin/quotations` (reescribe un stub `InDevelopment` de Sprint 6B, 0 consumidores reales): `fetchAllQuotations()` trae TODAS las cotizaciones (range-query `documentId()` para `COT-*` + `status in ['QUOTATION','CANCELLED']` para nativas `C-*`, dedup por id, sin `limit`). Helper puro `quotationsViewLogic.ts` (`getQuotationOrigin`/`getQuotationStateLabel`/`getProductionStateLabel`/`buildQuotationRow`, reusa `buildQueueRow` tal cual). Vista: Documento · Cliente · Fecha · Estado de Cotización · Estado de Producción · Origen · Comprobante Vinculado, SIN acciones. Fix bug binario `SalesHistoryTable.tsx` (`getSaleStatusBadge`, 5 estados). 30 tests unit + 3 integración emulador GREEN. Runtime validado por el dueño: sales 197 sin COT-*, quotations 131 (130 importadas + `C-000020` nativa cancelada) con los 3 estados de producción distinguibles. Detalle completo en CLAUDE.md v6.47.
- **#4 (Excel `/admin/coils` — todas las bobinas) CERRADO EN PROD** (merge `2b9bfd5a`, develop==master==origin). `fetchAvailableCoilsForExport()` (hardcodeada a `status=="AVAILABLE"`, sacaba 65 de 168 bobinas) reemplazada por `fetchCoilsForExport(filters)`: con `statusFilter==="ALL"` cero `where` de status (robusto a cualquier status presente/futuro/undefined — la versión intermedia con whitelist `where("status","in",[5 valores])` ya demostró ser frágil, le faltaban `SOLD`/`SPLIT_PARENT`); `finish`/`currency`/`provider` a cliente en modo ALL para no exigir un índice compuesto inexistente (los 7 índices de `coils` en `firestore.indexes.json` llevan `status` como campo líder). Nuevo helper puro `src/core/coils/coilExportLogic.ts` (`buildCoilExportRows`/`buildCoilExportSummary`, 8 tests). Dos hojas XLSX (Bobinas + Resumen con bruto/neto/negativas/aviso de búsqueda). Frontend-puro. Runtime validado por el dueño: ALL→168, AVAILABLE→65, ALL+GALV cuadra sin error de índice. Detalle en CLAUDE.md v6.46.
- **Fix test `renderiza filas mixtas` CERRADO EN PROD** (`875d1785`). Bug del test (expectativa vs default `hideCompleted` de `3f5adb2f`, no de la app). Fixture FFA1-102 a PARCIAL (10/20). Suite a verde total (page.test.tsx 8/8, queueLogic 20/20). Detalle en CLAUDE.md v6.45.
- **#2 (Ver cotización read-only) CERRADO EN PROD** (`9b04f3ca`, develop==master==origin). Vista `QuoteDetailsReadOnly` reemplaza `SaleDetailsModal` en la cola metallic (read-only, cero escritura). Helper puro `buildQuoteDetailView` + 7 tests. TR4/TR5 mismo SKU: solicitado fiel por línea + producido agrupado por SKU con nota. Incluye FIX del stale closure preexistente (`[router]`→`[router,quotes]`) que dejó "Ver cotización" muerto desde `f67cec9a`. Detalle en CLAUDE.md v6.43.
- **Frente A (buscador cola) CERRADO EN PROD** (`bb970782`, develop==master==origin). N+1 muerto (`getAllActiveFulfillmentLogs`+`bucketLogsBySourceId`+lookup O(1)) + cap 100→500 (constante `QUEUE_FETCH_CAP`, banner incluido) + `searchFields` activado (cliente + N° con/sin `COT-`). `buildQueueRow` ya era puro. Runtime validado (2ª página ahora buscable, carga más rápida). Detalle en CLAUDE.md v6.44. Sigue: Frente B (combobox /production/new), luego #4 según orden de prioridad.
- **Combo UI #7+#8 CERRADO EN PROD** (develop 5fca6ed6 / master 2995e0b3). Spinner+wheel muerto y paste numérico sanitizado en Peso/Ancho/Espesor/Valor de los 3 forms LIVE de bobina (`AddCoilForm.tsx`, `PurchaseCoilFromXml.tsx`, `EditCoilModal.tsx`). Helper puro `src/core/coils/utils/numericInput.ts` con 8 tests Vitest (RED→GREEN documentado). Frontend-puro, sin backend/índice tocado.
- **Frente #1 (editar bobina USD→SOLES) CERRADO EN PROD** (master `d086344d`; callable `updateCoil` ya deployado a prod+test con paridad y smoke OK antes del merge). `updatePayload` ahora persiste `metadata.currency`/`exchangeRate`/`originalCurrencyValue` (antes los ignoraba en silencio pese a que el cliente ya los mandaba). Autofill de TC por fecha de factura en el modal de edición, disparado solo por acción de usuario (nunca en mount). `pricePerKg` recomputado con el helper canónico `coilPricing.ts` en los 3 puntos que lo tocan (Valor Total, toggle moneda, autofill). Detalle completo en CLAUDE.md v6.42.

## COLA
> Reestructurada en v6.66.0 (dos sistemas de numeración colapsados en lista plana).
> **v6.68.0: cada ítem tiene SLUG estable.** La posición es solo orden de display —
> un ítem se cierra BORRANDO su fila, y un slug que reaparece se detecta con un grep.
> Motivo: en v6.66.0 la migración preservó un ítem YA CERRADO en la posición 1 y
> sobrevivió 2 versiones porque nada lo identificaba salvo estar primero.
> Los ítems cerrados NO se re-listan — su registro vive en la entrada de versión.

1. **`[QUOTATION-APPROVE-UNREACHABLE]` — VIVA, coordenadas REESCRITAS (C2, v6.82.0: 3ª vez que se mueven; de acá en más se cita POR NOMBRE, regla v6.67.0).** No hay camino en la UI para APROBAR ni CANCELAR una cotización NATIVA. `approveQuotation` y `cancelQuotation` siguen client-side en `salesService.ts` y siguen wireados desde `admin/sales/page.tsx` vía `onApprove={handleApprove}` / `onCancel={handleCancel}` a `SalesTable.tsx`; sus botones existen dentro de `if (sale.status === "QUOTATION")` — pero `buildListStatusFilter` (`salesAggregateLogic.ts`) devuelve `["COMPLETED", "VOIDED"]`, así que ese bloque nunca se evalúa verdadero. *(La fila citaba `:92`/`:113`; v6.73.0 midió `:94`/`:115`; hoy los handlers están en `:82`/`:104` y el wiring en `:199-200`. Ninguna coordenada sobrevivió 2 tandas.)* ⚠️ **Y `/admin/quotations` ya NO "solo ofrece EDITAR":** el botón de editar sigue, pero la acción de duplicar quedó sin entry point ahí y `[SALES-EDIT-404]` borró el de `/admin/sales` en v6.80.0 — re-verificar la superficie real de acciones al abrir el frente, no confiar en esta frase. **Alcance corregido por el dueño en runtime:** las cotizaciones IMPORTADAS ya nacen con comprobante vinculado y nunca necesitaron aprobación — el corte afecta SOLO a las NATIVAS (ej. `C-000040` en test: VIGENTE, PENDIENTE, comprobante `—`, sin salida). Probablemente pendiente de re-wirear tras la separación de páginas ventas/cotizaciones. **Funcionalidad de negocio ausente, no registro sucio — PRIORIDAD DEL DUEÑO.** ✅ **REGLA DE NEGOCIO (2026-08-31, confirmada por el dueño): una cotización se aprueba CUANDO EL CLIENTE ACEPTA — es un evento real de negocio que el sistema hoy no puede disparar para nativas, NO un candidato a borrado de código muerto.** No re-clasificar como deuda a eliminar.
   - **RECON DE ALCANCE, read-only, TANDA 5 (2026-08-31) — coordenadas SOBREVIVIERON esta vez (1ª desde v6.73.0): `:82`/`:104`/`:199-200` re-verificadas contra código, sin corrimiento.** `buildListStatusFilter("ALL")` (`salesAggregateLogic.ts:40-45`) devuelve `["COMPLETED", "VOIDED"]` — confirmado, código verbatim. `SalesTable.tsx` gatea el botón Aprobar/Cancelar en `if (sale.status === "QUOTATION")` (`:286`), pero en `/admin/sales` ese status nunca llega a una fila. `admin/quotations/page.tsx` (la página que SÍ lista `status==="QUOTATION"`) tiene **0 acciones de aprobar/cancelar** — grepeado, cero hits de `approve`/`cancel` en ese archivo; el único gate de acción ahí es `canEditQuotation` (`quotationsViewLogic.ts:108-111`, `origin==="NATIVA" && quotationStatus==="QUOTATION"`), que habilita EDITAR pero no aprobar/cancelar. **Respuesta a (a): inalcanzable por LAS DOS vías** — por UI (ninguna pantalla ofrece el botón a una fila NATIVA en QUOTATION) y por lógica (la única lista que podría alimentar el botón ya filtra ese status antes de llegar a la tabla). **Respuesta a (b): SÍ hay un consumidor que ya lee el estado y no lo completa** — `canEditQuotation` lee `origin`+`quotationStatus` y ofrece Editar, pero la transición aprobar/cancelar no tiene ningún gate análogo en esa página; el estado se ve ("Vigente") y se puede editar, pero nunca se puede cerrar. **Población medida, LOS 2 ENTORNOS, 2026-08-31:** `ayrsteel-2026` (solo lectura, assert `projectId`) = **19** `sales.status==="QUOTATION"`, **0 NATIVAS** (las 19 son importadas, `relatedSaleId`||`metadata.isQuotation`); `ayrsteel-test` = **24** `status==="QUOTATION"`, **1 NATIVA** (`C-000040`), 23 importadas. El corte de la fila afecta HOY a 1 doc de prueba en test y a 0 docs en prod — dormido en prod, no incendio. Script fuera del repo: *scripts/local/recon_quotation_approve.cjs* (gitignored). **NADA de código tocado — recon puro, para que el próximo frente ataque con el alcance ya decidido.**
   - **RECON T0, TANDA 6 (2026-08-31) — 2 GATES DE PARADA, LOS 2 DISPARADOS. Cero código, se reporta y no se implementa.** Decisiones de negocio ya tomadas por el dueño para cuando se ataque: (1) aprobar = solo marcar aceptación del cliente, SIN mover stock; (2) el entry point vive en `/admin/quotations`, no en `/admin/sales`.
     - **T0.1 — `approveQuotation` (`salesService.ts:249-365`) SÍ toca stock, directo, citado verbatim.** Dentro de la misma `runTransaction`: por ítem-bobina, `transaction.update(doc(db,'coils',item.sku), {status:'SOLD',...})` (`:303-308`) + `transaction.set(.... 'kardex_movements'...)` (`:309-320`); por ítem-no-bobina, `strategy.writeSaleDecrement(...)` (`:330-342`, comentario del propio código: `// Stock negativo permitido — ADR-005`). Además crea un doc `sales` nuevo completo con `status:'COMPLETED'` (`:348-355`). **Gate disparado — código actual escribe stock, no coincide con la decisión (1).**
     - **T0.2 — enum `SaleStatus` (`src/types/index.ts:180`) citado verbatim: `"QUOTATION" | "COMPLETED" | "CANCELLED" | "CONVERTED" | "VOIDED"`.** Ninguno significa "aceptada por el cliente, sin stock movido": `COMPLETED` es venta concretada (mueve stock); `CONVERTED` es el estado que la PROPIA cotización adopta después de que `approveQuotation` ya generó la venta — post-hecho, no pre-aceptación. **RAMA B — no existe el valor.** Un enum nuevo toca el guard de `status` de `firestore.rules` (`:101-104`), los filtros de lista (`buildListStatusFilter`/`buildAggregateStatusFilter`) y todo lo que agrupe por estado — frente propio, no detalle de implementación. **Gate disparado.**
     - **T0.3 — superficie real de `/admin/quotations` hoy, citada verbatim (`page.tsx:157-171`): 1 sola acción, `Editar` (gateada por `canEditQuotation`).** Cero botones de aprobar/cancelar en el árbol de columnas — confirma la fila de arriba, sin corrimiento.
     - **T0.4 — SÍ hay harness capaz de sostener un RED, a nivel lógica.** `quotationsViewLogic.ts` ya tiene `quotationsViewLogic.test.ts` (mismo patrón que ancla `canEditQuotation` hoy) — un futuro `canApproveQuotation` puro tendría dónde vivir su RED. `page.tsx` no tiene test de componente (mismo motivo documentado en otros lados: renderizar la página completa en test es riesgo de OOM). No aplica el bloqueo de `[WIDTH-FALLBACK-SPLIT]` (harness de componente) — este frente NO está bloqueado por falta de harness, está bloqueado por las 2 decisiones de diseño de arriba.
     - **T0.5 — `firestore.rules:101-104` citado verbatim: `allow update: if canWrite() && fieldsUnchanged(['totalAmount','subtotal','igv','exchangeRate','currency','items','paymentType']) && !(...VOIDED...) && !(...VOIDED...)`.** `status` NO está en la lista protegida — un update que cambie SOLO `status` (sin tocar los 7 campos financieros) pasaría este guard. Irrelevante mientras no exista el valor de destino (T0.2, RAMA B).
     - **CONCLUSIÓN: el frente NO es "re-wirear la UI a lo que ya hay" (alcance chico) — es diseñar un estado nuevo Y un callable/transacción nueva que NO mueva stock, ambos fuera del alcance de esta tanda.** Queda con su propio PASO 0 para cuando el dueño priorice: (a) nombre y semántica del nuevo status (o de un campo aditivo tipo `clientAccepted:boolean` que no toque el enum, alternativa más barata a evaluar); (b) si necesita callable server-side (patrón `editQuotation`) o alcanza con transacción cliente; (c) qué pasa con `firestore.rules` si el nuevo campo necesita protección propia.
   - ✅ **CERRADO EN SU TRAMO DE CÓDIGO (v6.86.0, TANDA 7). Las 3 preguntas del PASO 0 quedaron respondidas por el dueño y ejecutadas.** (a) **campo ADITIVO**, no status nuevo — `clientAccepted` (booleano) + `clientAcceptedAt` (timestamp), el enum `SaleStatus` no se toca; (b) **transacción cliente**, no callable — espeja la forma de `cancelQuotation`, que ya vivía client-side; (c) **`firestore.rules` NO necesitó cambio**, y eso está MEDIDO, no derivado.
     - **U0 (el gate) — `src/test/rules/salesClientAccepted.rules.test.ts`, custodio `test:emu:rules`.** La premisa que T0.5 derivó por lectura ahora se mide: un update que agrega el par aditivo sin tocar ningún campo existente **PASA** el guard de `firestore.rules:101-104` (coordenada re-verificada antes de citarla). 3 controles negativos: modificar `totalAmount` existente → RECHAZA; **AGREGAR `subtotal`, un financiero que el doc no tenía → RECHAZA** (par discriminante exacto contra `clientAccepted`: la única diferencia entre las 2 claves es el NOMBRE); colar `totalAmount` dentro del mismo update del par → RECHAZA. Fixture con el BUILDER REAL (B19), y no es cosmético: el doc que produce `buildQuotationDoc` **no tiene 5 de los 7 campos protegidos**.
     - **U1 (el escritor) — `markQuotationAccepted` (`salesService.ts`), test `quotationClientAccepted.integration.test.ts`.** RED crudo `TypeError: markQuotationAccepted is not a function` ×3 → GREEN 4/4. **El assert que ES el frente:** aceptar no mueve stock — se cuentan `metallic_roofing_stock`, `metallic_roofing_stock_movements`, `kardex_movements` y `coils` ANTES y DESPUÉS y se comparan, con el doc de stock sembrado POBLADO para que el cero no sea cierto por accidente sobre una colección vacía; más `status` intacto en `QUOTATION`. Guard de idempotencia (re-aceptar pisaría el timestamp original, que es el dato de auditoría). Audit propio `QUOTATION_CLIENT_ACCEPTED`. **`approveQuotation` NO se tocó** (decisión 5).
     - **U2 (la UI) — `canAcceptQuotation` (`quotationsViewLogic.ts`) + botón en `/admin/quotations`.** RED contra la capa de lógica (13 failed/24 passed → 37/37), no contra la página React. El gate espeja exactamente lo que el escritor acepta (NATIVA + QUOTATION + no aceptada). `buildQuotationRow` PROPAGA el flag — sin eso el botón reaparece sobre una cotización ya aceptada. **El copy del diálogo dice explícitamente que aceptar no descuenta stock ni genera venta**: confundir "aceptada" con "vendida" es la confusión más cara posible acá, porque el otro camino sí hace las dos cosas.
     - **LO QUE NO ENTRÓ, declarado:** la aceptación no dispara ningún flujo posterior (no convierte a venta, no notifica, no aparece como filtro en la vista). Es solo el registro del evento. Convertir una cotización aceptada en venta sigue siendo `approveQuotation`, con todo lo que eso implica (crea venta `COMPLETED`, descuenta stock, kardex, bobina `SOLD`) y con su UI todavía inalcanzable desde `/admin/sales` — **ese tramo NO se cerró y no se tocó**.
2. **`[SMOKE-LOGS]`** — 11 `audit_logs` con `userEmail == "SMOKE"` (8 `SALE_CREATED` + 3 `PRODUCTION_LOG_CREATED`, medido 2026-08-26 vs prod) apuntando a docs borrados. DECISIÓN DE NEGOCIO: `audit_logs` es append-only por regla (`update/delete: if false`). **Pariente medido en v6.68.0:** la única `cut_orders` de prod (`sKK7cvOkfJEQ6bLuDm9u`, ANULADA) referencia la bobina `TEST-002-1`, que ya no existe en `coils` — misma familia de referencia colgante, inerte, se decide junto. **Evidencia de v6.73.0, RE-MEDIDA en C2 (v6.82.0) — la parte de PROD se confirma exacta, la de TEST quedó MUERTA:** en `ayrsteel-2026` hay **4** `audit_logs` `action=='CANCEL_QUOTATION'` (`C-000019`, `COT-FFA1-1255`, `COT-FFA1-1250`, `C-000020`), **ninguno de los 4 existe hoy en `sales`**, y hay **0** docs `sales` con `status:'CANCELLED'` — 0/0, idéntico a lo registrado. ⚠️ En `ayrsteel-test` la afirmación "ninguno de los 2 `entityId` existe hoy" **YA NO ES CIERTA**: son 2 (`C-000029`, `COT-FFA1-1262`) y **`COT-FFA1-1262` SÍ existe**, con 2 docs `CANCELLED` en esa colección. El conteo de logs `SMOKE` en test es **9** (7 `SALE_CREATED` + 2 `PRODUCTION_LOG_CREATED`), no comparable con los 11 de prod. La conclusión de fondo — "`sales` no audita borrados" (v6.58.0) — sigue VIVA y sostenida por la evidencia de PROD, que es la que importa.
3. **`[DRYWALL-ORPHAN-LOGS]`** — 9 `production_logs` de drywall con `parentCoilId: "F001-13039-8"`, una bobina que NO existe en `coils` hoy. Destapado de rebote en la verificación post-borrado de `[ALUZINC-RESET]` (P3.3a) — **preexistente, NO causado por ese borrado**: `"F001-13039-8"` no está en `ids-frozen.json` (no es una de las 130 bobinas aluzinc borradas), y el conteo de `production_logs` de drywall no se movió (143 antes, 143 después). Sin PASO 0, sin diseño, sin investigar más allá de este hecho. IDs de los 9 logs: `81cp77L3ik6AuYX153N9`, `9qspJivd89b1IJSmw7M1`, `GDdbA2zkhv8mvHqBveOk`, `NLJVbjKTAHJdNtApPBSS`, `Udysojgwe308QeH1qnau` (+ 4 más, mismo `parentCoilId`, ver reporte del STOP B de `[ALUZINC-RESET]` para la lista completa).
6. **`[E2E-HARNESS]`** — Playwright NO es dependencia del repo, sigue sin correr en CI. **El bloqueante declarado (fixture de auth + contra qué entorno correr) quedó RESUELTO EN LA PRÁCTICA en v6.79.0 (T6, vía Playwright MCP, no la librería instalada).** Método: login real con credenciales de `demo@cliente.com` contra `ayrsteel-test` (nada de emulador ni token sembrado); el ENTORNO se confirma POR MECANISMO ANTES de tocar nada — primero por HTTP, grepeando el chunk JS servido por el dev server en busca de `ayrsteel-test`/`ayrsteel-2026` inlineados (sin abrir browser, sin auth), y después en vivo leyendo `database=projects%2F<id>%2F...` en los network requests reales de Firestore durante el login. Limpieza post-corrida verificada por snapshot antes/después (stock restaurado exacto, docs de `sales` borrados por id explícito, `audit_logs` huérfanos preservados a propósito por ser append-only). **Sigue sin ser dependencia del repo y sigue sin CI** — el método es manual, repetible, pero no automatizado. Ver v6.79.0 en `CLAUDE.md` para el detalle completo.
7. **`[TYPE-SCALE]` — VIVA, cifra actualizada (C2, v6.82.0: 959 → **960**).** `globals.css` es Tailwind v4 con `@theme inline` y SIN escala de `font-size` declarada (**0 ocurrencias de `--text-` en el archivo, re-verificado**): las **960** llamadas a `text-xs/sm/base/lg` resuelven contra los defaults de Tailwind. Declarar `--text-*` en `@theme` achica todo el ERP sin tocar un solo `.tsx`. Preferencia de tamaño por usuario en `localStorage` — **decisión del dueño: NO Firestore**, para no tocar `users` ni rules ni disparar el re-deploy de rules pendiente (ver deuda de rules en la cabecera). Va en RAMA con preview de Vercel; **la crea el dueño** (`git checkout` bloqueado para el ejecutor).
8. **`[TABLE-CELL]` — VIVA, 2 cifras REESCRITAS y la "DUDA ABIERTA" YA ESTABA RESUELTA (C2, v6.82.0).** Re-medido: **47** archivos con `<table>`/`<thead>`/`TablePagination` (la fila decía 44), **20** con `truncate`/`line-clamp` (✓ exacto), y **64** `title=` nativos (la fila decía **~129** — sobreestimado al doble). Objetivo: celda monolínea con elipsis + tooltip. Componente compartido PRIMERO, migración por lotes después. Acá SÍ se corre `/impeccable init` (`PRODUCT.md`/`DESIGN.md` siguen sin existir, verificado). ⚠️ Hover no existe en touch: definir fallback por tap si se usa en tablet en planta. Va en RAMA. ✅ **La "DUDA ABIERTA" sobre `PRODUCT.md`/`DESIGN.md` vs la fuente única quedó RESUELTA en v6.75.0** — la regla de jerarquía documental está grabada en `CLAUDE.md` §11, con el texto verbatim del banner que esos archivos deben llevar **en el mismo commit que los cree**. La fila la seguía listando como pendiente de OK del dueño: ya no lo está.
9. ~~**`[QUOT-FETCH-RANGE]`**~~ ✅ **BORRADO — BUG FANTASMA, NUNCA EXISTIÓ (v6.82.0, C1). NO RE-ABRIR.** Ver el bloque de cierre al pie de esta lista. Número conservado, fila vaciada. *(texto histórico refutado, conservado abajo solo por trazabilidad)*
   - ⚠️ **TODO LO QUE SIGUE ES FALSO.** Los 2 límites de la query NO son iguales: el superior lleva un **U+F8FF invisible** (`charcodes [67,79,84,45,63743]`, length 5) que es el sentinela canónico de prefijo de Firestore. La query devuelve los `COT-*` correctamente — medido E1→1, E5→2 exactos. Todas las mediciones previas que dieron 0 se hicieron sobre una query **reconstruida a mano sin ese carácter**, no sobre la real.
10. **`[PARITY-DATA]` — VIVA en el fondo, NÚMEROS REESCRITOS (C2, v6.82.0): los de la versión anterior estaban obsoletos en los DOS entornos.** La paridad test↔prod no cubre POBLACIÓN. **Re-medido 2026-08-31, read-only con assert de `projectId`:** `ayrsteel-2026` = **105 docs** en `sales` (86 `COMPLETED` + 19 `QUOTATION`, **19 con prefijo `COT-`**); `ayrsteel-test` = **52 docs** (24 `COMPLETED` + 24 `QUOTATION` + 2 `CANCELLED` + 2 `VOIDED`, **25 con prefijo `COT-`**). ⚠️ **La versión anterior de esta fila decía "prod = 67 docs, TODOS COMPLETED, 0 cotizaciones" y "test = 2 docs, 0 `COT-*`" — las 4 cifras están muertas.** Lo que NO cambió es la conclusión: los 2 entornos siguen con poblaciones incomparables entre sí, así que ningún fixture de cotizaciones vale en los dos lados. Hermano de las 0 extensiones de Algolia en test (v6.61.0). **AMPLIADO en v6.79.0 con el diff de `metallic_roofing_catalog` de `[COB035GRIS-UNIT]` (recon de los DOS entornos, `ayrsteel-2026` y `ayrsteel-test`):** `coil_finishes` usa **VOCABULARIO DE IDS DISTINTO por entorno completo** (`ALZ-*` en prod, `ALU-*` en test) — diverge en **51 de 53** docs de catálogo que comparten SKU, solo por el campo `finish`. Ningún fixture que siembre un `finish` id vale en los dos lados a la vez. **RE-CONFIRMADO en C2 (v6.82.0), esta parte NO cambió:** 9 finishes en cada entorno, prod 8×`ALZ-*` + `GALV`, test 8×`ALU-*` + `GALV`; y el prefijo de `finish` en `metallic_roofing_catalog` es **54/54 `ALZ` en prod** y **55/55 `ALU` en test** — cero solapamiento. Además: 54 docs en prod vs 55 en test (`SMOKE-TEST-COB040AZUL`, basura de smoke en test, sin equivalente en prod); `COB040VERD` (solo prod) vs `COB040VERDE` (solo test) — MISMO producto con id distinto entre entornos, ver `[CATALOG-DUP-VERDE]`. Scripts + dumps crudos fuera del repo, en *~/ayr-scratch/2026-08-31-catalog-parity/* — *recon.cjs*, *diff.cjs*, *dump-ayrsteel-2026.json*, *dump-ayrsteel-test.json*, *diff-result.json*.
14. **Re-verificar las 18 discrepancias restantes del `REPORTE.md`** (`~/ayr-scratch/2026-08-27-estado-actual/`, fuera del repo). La discrepancia #1 de ese reporte salió FALSA al re-medirse (ver `[DOCS-CHECKER]`/`[DOCS-STALE-SWEEP]`); las otras 18 no tienen ninguna garantía de estar bien — el checker automático (`scripts/check-docs.cjs`) solo cubre citas `archivo:línea`/paths/comandos npm/hashes de commit, que es apenas **1 de las 19** categorías de discrepancia que ese reporte original enumeraba. Frente propio de re-verificación 1x1, sin atajos automáticos disponibles para el resto.
15. **Sincronizar `master`** — hoy en `e9f54e20`, **4 commits atrás** de `develop`/`origin/develop` (`9fe6ae0f` al momento de este registro). Divergencia intencional desde `[COIL-TYPE-KEY]` (backend ya en prod antes que el frontend llegara a `master`) — pendiente decidir cuándo cerrar la brecha, no es un olvido. ✅ **CERRADO (2026-08-28, `[STOCK-NEG-WAC]`, v6.77.0):** `master`/`origin/master` alcanzaron a `develop`/`origin/develop` en `0758d999`, los 4 refs confirmados por `git rev-parse`. Número de ítem conservado, no re-listar en otra posición.
18. **`[IMPORT-EXTRACT]` — ✅ TRAMO DE CÓDIGO CERRADO (v6.82.0, C3). ⚠️ NO se cierra la fila: los tests siguen reimplementando, y ese tramo queda ABIERTO a propósito. ✅ MAPA DE MIGRACIÓN COMPLETO en v6.87.0 (TANDA 8, G1) — ver el bloque al pie de este ítem.**
    - **HECHO:** la transacción salió de `handleUploadToFirebase` (inline, 112 líneas) a **`src/core/import/runSaleImportTransaction.ts`**, exportando `runSaleImportTransaction(tx, {db, sale, userUid, userEmail})` y el tipo `SaleImportTxResult`. `page.tsx` **−115/+9** (incluye el borrado de 3 imports que quedaron huérfanos: `getStockStrategy`, `buildImportWrites`, `DocumentSnapshot`). **MOVIMIENTO PURO, cuerpo verbatim:** no se agregó el guard de #12, no se tocó ningún fallback, y `userEmail` se tipó `string | null` a propósito para no convertir en `undefined` el `null` que Firebase Auth puede dar (cambiaría lo que se persiste en `metadata.uploadedBy`). Custodios: `tsc` 0 y las 2 suites con números IDÉNTICOS al baseline.
    - **LO QUE FALTA, declarado y no disimulado: quedaron DOS implementaciones.** Los 3 tests (`salesImport.test.ts` y `salesReimport.test.ts` con `simulateImport`, `salesImportQuotation.test.ts` con `simulateImportWithLogic`) **siguen sin llamar a la función real**. **Motivo, no excusa:** migrarlos NO es un movimiento puro — sus copias ya divergieron tanto que cambiarían lo que los tests afirman. Medido en C2: `simulateImport` **no maneja `VOIDED`/`isReplacement`** (cualquier doc existente → `OMITTED`), **no archiva a `history`**, **no escribe `SALE_REPLACED`**, **no llama `buildImportWrites`** (arma el doc a mano con otro shape: `skus`, `metadata.isHistorical`), **no crea la percha `COT-*`** y **no escribe `SALE_IMPORTED_WITH_MANUAL_NC_ACTION`**. Lo único idéntico es el bloque de stock. Migrarlos es un frente con RED propio.
    - **Qué desbloqueó igual:** `[IMPORT-OVERWRITE]` (#12) ya es atacable — su RED se escribe contra la función nueva, sin tocar `simulateImport`. `[WIDTH-FALLBACK-SPLIT]` (#43) **NO** se desbloqueó: su bloqueante es el harness de `ProductSelector.tsx`, un componente React, que es un problema distinto.
    - **✅ CONFIRMADO EN v6.83.0: el desbloqueo era real — PERO SOLO PARA LA MITAD DE ABORTO.** `[IMPORT-OVERWRITE]` (#12) se cerró llamando a `runSaleImportTransaction` desde un test de integración nuevo (`salesImportOverwrite.integration.test.ts`), sin tocar ninguna de las 3 copias. **Fila #12 BORRADA de la COLA — pero el cierre de #12 fue SOLO de la rama de aborto** (percha `CANCELLED` / con producción activa). La otra mitad de la decisión de v6.76.0 (archivar a `history` y pisar, percha `QUOTATION` sin producción) quedó fuera a propósito y vive como **`[IMPORT-PERCHA-ARCHIVE]` (#47)** — no leer el cierre de #12 como "el importador ya no pisa perchas en ningún caso".
    - **⚠️ LA DIVERGENCIA CRECIÓ, y se registra en vez de unificarse (era el contrato de v6.83.0/E2.5).** El guard de la percha vive AHORA solo en la función real: `simulateImportWithLogic` (`salesImportQuotation.test.ts`) sigue haciendo `tx.set(quoteRef, quotationDoc)` a ciegas, y las 2 copias de `simulateImport` ni siquiera crean percha. **Punto de divergencia nuevo, el 7º**, que se suma a los 6 ya medidos en C2. No se unificó: sigue siendo un frente con RED propio, y ahora con MÁS superficie que antes, no menos.
    - **✅ `[IMPORT-PERCHA-ARCHIVE]` (#47) CERRADO en v6.84.0, y la divergencia CRECIÓ otra vez — 8º punto.** El archivado de la percha `QUOTATION` sin producción (a `history` + audit `QUOTATION_REPLACED`, antes de pisar) se agregó SOLO en `runSaleImportTransaction.ts`. `simulateImport`/`simulateImportWithLogic` siguen sin crear percha en absoluto, así que ni siquiera podrían tener este bug — la divergencia no se cerró, se registra como el 8º punto medido.
    - *(contexto original)* Consecuencia medida en `[IMPORT-SIM-DRIFT]` (cerrado en v6.76.0): 2 de los 3 archivos divergieron en silencio durante 2 meses. Mismo patrón ya aplicado a `buildImportWrites`/`parseImportRows` — este era el tercer tramo.
    - **✅ MAPA COMPLETO (v6.87.0, TANDA 8, G1) — read-only, cero código, entregado para diseñar la migración, NO para ejecutarla en esta tanda.** Los 8 puntos de esta fila, re-verificados 1x1 contra el código real (no transcritos de acá):

      | # | Punto | `runSaleImportTransaction.ts` (REAL) | `simulateImport` (`salesImport.test.ts`) | `simulateImport` (`salesReimport.test.ts`) | `simulateImportWithLogic` (`salesImportQuotation.test.ts`) |
      |---|---|---|---|---|---|
      | 1 | VOIDED→`isReplacement` | SÍ, `:102-111` | **NO**, `:35` — solo `if (exists) return OMITTED`, ciego al status | SÍ, `:33-42` | SÍ, `:32-41` |
      | 2 | Archiva venta a `history` | SÍ, `:137-146` (`archivedByUserId` **y** `archivedByUserEmail`) | **NO** | SÍ pero INCOMPLETO, `:61-69` — solo `archivedByUserId` | **NO** — computa `isReplacement` (`:30-37`) pero nunca lo usa para archivar (0 hits de "history" en el archivo) |
      | 3 | Audit `SALE_REPLACED` | SÍ, `:148-158` (con `userEmail`) | **NO** | SÍ pero INCOMPLETO, `:71-80` — sin `userEmail` | **NO** |
      | 4 | Construye vía `buildImportWrites` (builder real) | SÍ, `:168` | **NO** — doc a mano por spread, `:57-67` (`skus`, `metadata.isHistorical`) | **NO** — doc a mano por spread, `:84-95` | SÍ, `:51` — pero `metadata.uploadedBy` HARDCODEADO a `"test@example.com"`, no parametrizado |
      | 5 | Crea percha `COT-*` | SÍ (vía `quotationDoc`), `:226-228` | **NO** — sin lógica de percha | **NO** — sin lógica de percha | SÍ, `:53-56`, vía builder real (ver punto 7 para el guard que le falta) |
      | 6 | Audit `SALE_IMPORTED_WITH_MANUAL_NC_ACTION` | SÍ, `:230-239` | **NO** | **NO** | **NO** |
      | 7 | Guard percha preexistente (CANCELLED/ACTIVE_PRODUCTION → throw, aborta TODO) | SÍ, `:188-199` (lee `existingQuoteSnap` en fase de lecturas, `:135`) | N/A (no crea percha) | N/A (no crea percha) | **NO** — `tx.set(quoteRef, quotationDoc)` ciego, `:55`, sin `tx.get` previo |
      | 8 | Archiva percha a `history` (QUOTATION sin producción activa) | SÍ, `:201-222` | N/A | N/A | **NO** |

      Los 8 siguen VIVOS — ninguno murió al re-medirlos. 3 sub-divergencias nuevas, no distinguidas en versiones previas de esta fila: punto 2 y 3 de `salesReimport.test.ts` están PRESENTES pero INCOMPLETOS (falta `userEmail` en los 2 lugares donde la función real lo escribe); punto 4 de `salesImportQuotation.test.ts` usa el builder real pero con un valor hardcodeado en vez del parámetro real.
    - **Clasificación de los 12 `it()` de los 3 archivos (v6.87.0, TANDA 8, G1.2/G1.3), conteo crudo al pie:**
      - **Balde A — PORTABLE (9/9 de los activos, 0 MUERTO, 0 DECISIÓN):** los 3 `it()` de `salesImport.test.ts` (idempotencia COMPLETED, NC RETURNS_STOCK/MONEY_ONLY, NC Fase 2 SUNAT) + el `it()` activo #1 de `salesReimport.test.ts` (doble-corrida COMPLETED) + los 5 `it()` de `salesImportQuotation.test.ts` (#13-#17: atomicidad, idempotencia, no-toca-counter, timestamp histórico, sin campo `id`). **Por qué son portable pese a la divergencia de arriba:** cada fixture cae en uno de 2 casos donde la copia y la función real COINCIDEN — (a) doc ya `status:'COMPLETED'` → las 2 devuelven `"OMITTED"` (mecanismos distintos: exists-ciego vs chequeo de status, mismo resultado en ESTE caso); (b) BD fresca sin percha preexistente → el guard/archive de percha (puntos 7/8) no tiene sobre qué actuar, y donde se usa `buildImportWrites` (`simulateImportWithLogic`) produce el mismo doc que la función real. **Portable ≠ probado**: ninguno de estos 9 ejercita los puntos 1-3/6-8 bajo la función real — esa cobertura vive en `salesImportOverwrite.integration.test.ts`/`salesImportPerchaArchive.integration.test.ts`, no acá.
      - **3 `it.skip()` de `salesReimport.test.ts` (#2/#3/#4) — FUERA de A/B/C, no forzados a ninguna balde.** Bloqueados por WRITE 9 (callable server-side pendiente; `firestore.rules:101-104` protege `items`/`totalAmount`/`igv` contra `update` cliente), motivo AJENO a esta divergencia. Migrarlos a la función real no los destraba solos: el `tx.set(saleRef, saleDoc)` de la rama `isReplacement` de la función real, con items distintos sobre un doc VOIDED existente, es la misma forma de escritura que la regla bloquearía.
    - **Consumidores (grep, conteo crudo por término y archivo, B20):** `simulateImport` son **DOS funciones homónimas, no una compartida** — `salesImport.test.ts`: 1 definición + 3 invocaciones (4 hits) · `salesReimport.test.ts`: 1 definición + 8 invocaciones (9 hits, copia con más lógica). `simulateImportWithLogic`: 1 definición + 6 invocaciones (7 hits), todo en `salesImportQuotation.test.ts`. **0 hits de ambos nombres en `functions/src/`, `scripts/`, `docs/`** — el único consumidor de cada copia es su propio archivo de test. Los 2 hits de "simulateImport" en `salesImportOverwrite.integration.test.ts`/`salesImportPerchaArchive.integration.test.ts` son el docstring que EXPLICA la extracción, no una llamada.
    - **Costo estimado en RED (G1.5):** **0 REDs nuevos OBLIGATORIOS** para migrar el balde A — es un swap valor-preservado (misma aserción, corrida contra la función real, verificable por regresión al correr la suite; no aplica B1 porque no se afirma comportamiento nuevo). Prudencial, no exigido: 1 mutación de ancla por archivo migrado (3 total) para confirmar que cada test sigue discriminando post-swap, mismo patrón que `salesImportOverwrite.integration.test.ts` ya aplicó. **Migrar los puntos 2/3/7/8 (history/audit/guard/archive de percha) SÍ exige RED nuevo propio** — ningún test del balde A los cubre hoy.
    - **Preguntas de negocio destapadas (balde C): NINGUNA.** 0 casos — los 9 activos son A por la razón estructural de arriba, los 3 skipped están bloqueados por un guard de rules ajeno a esta migración, no por una decisión de diseño pendiente.
19. **`[STRATEGY-DUP]`** — `metallicRoofingStockStrategy` (y sus 3 hermanas `roofing`/`trading`/`drywall`) están duplicadas byte-a-byte entre `src/core/sales/strategies/index.ts` (cliente) y `functions/src/domain/strategies/*.ts` (server), mismo patrón "dominio puro + parity" ya usado en otros lados del repo — pero sin `.parity.test.ts` que las ancle (deuda ya registrada, `CLAUDE.md` §10 "Sin parity test entre las 2 copias de `finishCompat`" es un caso hermano, no este). **Hallazgo nuevo en `[IMPORT-SIM-DRIFT]`:** la copia CLIENTE de `writeProductionIncrement` para `metallic-roofing` (`strategies/index.ts:378-393`) está MUERTA — su único caller en `src/` es `coilConsumptionService.ts:110`, y el único consumidor de ESE archivo es `src/modules/drywall/services/productionService.ts` (drywall, no metallic-roofing). La producción real de metallic-roofing (`produceFromCoils`) usa exclusivamente la copia SERVER (`functions/src/callables/production.ts:271`). Candidato a borrado (o a documentar como código muerto intencional) en un frente propio de limpieza — no tocado en `[IMPORT-SIM-DRIFT]`, que fue solo diagnóstico. **Medido en `[STOCK-NEG-WAC]` (v6.77.0), sin especular:** `writeProductionIncrement` de `roofingStockStrategy` y de `tradingStockStrategy` TAMBIÉN tienen 0 callers — código muerto en las 3, no solo en metallic-roofing. Consistente con que `roofing`/`trading` son líneas de compra-venta sin producción propia (§1): la función nunca tenía por qué tener consumidor ahí. No agrega alcance nuevo al candidato de borrado ya registrado arriba.
23. **`[WAC-TRIPLE-DUP]`** — las 3 implementaciones de WAC del repo (`produceFromCoils` en `functions/src/callables/production.ts`, `calculateWeightedAverageCost` en `drywallProduction.ts`, `adjustStock` en `stockAdjustmentService.ts`), comparadas en el PASO 0 de `[STOCK-NEG-WAC]` (v6.77.0) para decidir el discriminante correcto, no tienen ningún `.parity.test.ts` que las ancle entre sí — mismo patrón de deuda ya registrado para otros dominios duplicados del repo (`finishCompat`, `annulment` antes de `[CASCADE-DUP]`). Un drift futuro en cualquiera de las 3 (ej. otro fix de borde aplicado a una sola) puede volver a divergir sin que ningún test lo note. Frente propio, con su propio PASO 0 para decidir si conviene converger a un solo helper puro o solo ganar el parity test sobre las 3 copias existentes.
24. **`[COST-FALLBACK-TEORICO]` — VIVA, coordenada corregida (C2, v6.82.0: `:270` → **`:304`**).** Candidato a revisar junto con `[IMPORT-REDO]`/`[IMPORT-EXTRACT]`: el fallback de costo del importador de ventas (`parseImportRows.ts:304`, expresión verbatim `stockInfo?.lastCostPerPiece || stockInfo?.avgCost || 0`, ya registrado en `CLAUDE.md` §11 v6.76.0) no distingue "SKU sin ningún costo cargado todavía" de "SKU con costo real igual a cero" — las 2 situaciones producen el mismo `baseCost:0` en el doc de venta, sin ninguna marca que las diferencie después. Visto de pasada durante el recon de `[IMPORT-COST-ZERO]`/`[STOCK-NEG-WAC]`, sin investigar más allá de la sospecha — no tiene PASO 0 propio todavía.
30. **`[IMPORT-WEIGHT-BACKFILL]`** — las ventas históricas importadas con peso 0 (antes de `[IMPORT-WEIGHT-BYPASS]`, v6.78.0) quedan tal cual — nadie las recalculó retroactivamente. Decisión de NEGOCIO, no técnica (mismo patrón que `[IMPORT-REDO]`). No re-proponer sin pedido explícito del dueño.
32. **`[ANCHO-UTIL-VS-DESARROLLADO]` — DORMIDO, no candidato. Bajado de rango en v6.80.0 por el PASO 0 de v6.79.0, que midió las 3 cosas que lo sostenían y las 3 dieron NO.**
    - **D1 — ¿el peso de una venta alimenta plata? NO.** Barrido de `totalWeight`/`calculatedWeight`/`unitWeight` sobre `src/` **y** `functions/src/`, clasificado con el código a la vista: **0 consumidores de COSTO o PRECIO** (las 3 fórmulas del builder canónico son `profit = (priceOrValue - baseCost) * quantity`, `totalAmount += unitPrice * quantity`, `totalCost += baseCost * quantity` — ninguna incluye peso; `totalWeight` es un acumulador paralelo). **0 consumidores de STOCK** (`writeSaleDecrement`/`writeSaleReversal` desestructuran `{ sku, quantity, newBalance, saleId, customerName, sellerId }`, sin ningún parámetro de peso, en las 2 strategies). El único consumidor no-display es `ventaKg` en `costeoAluzincLogic.ts`, que llega a `registry.ts` como columna `'Kg Vendidos'` `format:'number'` — y el `costoPorKg` del MISMO reporte divide por `pesoProduccionKg` (de `perCoilBreakdown`), **no** por `ventaKg`. `aluzincDetalleLogic.ts`: 0 hits de los 3 campos. `functions-sunat/src/`: 0 hits de peso — el comprobante electrónico SUNAT no lo lleva. El resto es display.
    - **D4 — ¿existe vínculo venta→bobina? NO.** Confirmado leyendo docs reales de prod: `metallic_roofing_stock` (14 docs) tiene campos `[avgCost, lastUpdate, productName, quantity, sku, totalValue]`, **`coilId` en 0 de 14**; `metallic_roofing_stock_movements` (26 docs) tiene `[businessLine, costPerUnit, createdAt, createdBy, quantity, reason, sku, type]`, **`coilId` en 0 de 26**; y un ítem metallic de venta real (`BBV1-347`) no tiene `coilId`/`parentCoilId`/`coilIds`/`productionLogId`/`lotId`, con `weightSnapshot: null`. Al importar una venta **no hay forma de saber de qué bobina salió el producto** — el peso teórico del catálogo es lo único disponible.
    - **D3 — sin población.** `ayrsteel-2026`: 38 coils, `masterWidth = 1200` en 38/38, **0 aluzinc**; `production_logs` 143 totales, **0 con `line == 'metallic-roofing'`**, 0 con `perCoilBreakdown`. Ningún ancho implícito de la hoja (`1138.75`/`1150.36`/`1159.06`/`1220.00`) matea; el único a menos de 25 mm es `1200` vs `1220`.
    - **El mismatch de código SIGUE siendo real y queda registrado:** VENTA usa el `widthMm` estático del catálogo (`coverageWeightCalc.ts`: `pesoKg = metrosTotales * thicknessMm * widthMm * densityFactor`), PRODUCCIÓN usa el ancho de la bobina real (`coilProduction.ts`: `theoreticalWeight = mlFromCoil * coil.thicknessMm * coil.masterWidth * coil.coilDensityFactor`). Misma fórmula, fuente de ancho distinta. Pero con D1=NO y D4=NO, hoy **no tiene consecuencia económica ni es arreglable en el importador**.
    - **GATILLO DE RE-APERTURA, explícito:** cuando entren las bobinas de agosto a `ayrsteel-2026`, medir el `masterWidth` real de las aluzinc contra el `widthMm 1220` del catálogo. Si divergen, el frente vuelve a la mesa **como pregunta de negocio** (peso teórico del producto vs peso real despachado), no como bug de código. Hasta entonces: DORMIDO.

33. **`[CATALOG-WEIGHT-INPUTS]`** — REGISTRADO, NO IMPLEMENTAR. Guard de **1 sola clase** (la que el director había llamado "Clase B" se disolvió con la medición de `[COB035GRIS-UNIT]`, v6.79.0 — `unit` es inerte en el cálculo de peso, no hay mismatch de unidad que atrapar). Clase única: **inputs no resolubles** — `finish` que no matea ningún id en `finishRef`, o `densityFactor`/`widthMm`/`thickness` nulos o cero → peso 0 SILENCIOSO en `calcCoverageWeightKg` (`pesoKg: null` que cae a `catalogWeight` = 0, sin flag). Caso medido: `PL030VERD6000` en `ayrsteel-test` (`finish:"ALUZINC"`, inexistente en `coil_finishes` de ese entorno) — corregido por el dueño el 2026-08-31 (`family COBERTURA→PLANCHA`, `finish→ALU-VERDE-RAL-6035`). En `ayrsteel-2026` ese mismo doc estaba SANO (`family:PLANCHA`, `finish:"ALZ-VERDE-6035"`, existente). Alcance SOLO metallic-roofing. Es FLAG, NO bloqueo — `[IMPORT-WEIGHT-BYPASS]` (v6.78.0) eligió a propósito degradar al comportamiento previo para no regresionar las otras 3 líneas de negocio.
34. **`[CATALOG-LINT]`** — lo que antes se pensaba como guard de import ("mismatch de unidad") pasa a ser un CHEQUEO DE INTEGRIDAD DE CATÁLOGO, fuera del camino de peso — porque `[COB035GRIS-UNIT]` (v6.79.0) probó que estas incoherencias NO afectan el peso calculado (`unit` inerte), así que un flag en tiempo de import sería ruido sin efecto real; el lugar correcto es un chequeo aparte sobre el catálogo, no el importador. Candidatos a chequear: `family COBERTURA` con `unit != METRO` · `family PLANCHA` con `unit != PIEZA` · `unit PIEZA` con `length` nulo o cero (una pieza sin largo es incoherente, no se puede vender/pesar) · `finish` inexistente en `coil_finishes` · `family` que no matea el prefijo del id (ej. `PL*` que no sea `PLANCHA`, como pasaba con `PL030VERD6000` en test antes del fix del dueño). Sin PASO 0 todavía.
35. **`[CATALOG-DUP-VERDE]`** — `COB040VERD` (solo en `ayrsteel-2026`) y `COB040VERDE` (solo en `ayrsteel-test`) NO son divergencia de entornos: **verificado contra la hoja de costeo real del dueño** (`Costeo_Producto_PerfilesAyR jean.xlsx`) — tiene AMBAS filas por separado, con valores IDÉNTICOS (`3.904 kg/m`, Metro, `Prepintado|Verde|0.40`). El duplicado está en el MAESTRO de costeo; cada entorno importó una fila distinta de ese mismo duplicado. Si una factura real trae el id que el entorno de destino no tiene, `parseImportRows` da `UNRECOGNIZED_PRODUCT` y saltea la línea — degrada RUIDOSO (fila marcada, visible en el resumen de import), pero la venta entra INCOMPLETA. El arreglo empieza por la hoja del dueño, no por Firestore.
36. **`[CATALOG-DENSITY-DENORM]`** — `densityFactor` vive DUPLICADO entre `metallic_roofing_catalog` y `coil_finishes`. Nulo en `ayrsteel-2026` para `COB035GRIS` y `PL030VERD6000` (2/54 docs, medido). **Decisión del dueño (2026-08-31): NO se rellena.** Desde `[IMPORT-WEIGHT-BYPASS]` (v6.78.0) el importador ya lo lee de `coil_finishes` — fuente única por regla de la casa (§11: "Densidad: UNA por acabado, heredada vía lookup, nunca hardcodeada"). Si `reporte-aluzinc-detalle` está roto para esos 2 SKUs (lee `product.densityFactor` del catálogo, `aluzincDetalleLogic.ts:213`), el defecto es del CONSUMIDOR, no del dato: debe leer `coil_finishes` como ya hace el importador. Fix correcto = BORRAR el campo del catálogo y migrar sus lectores, no llenarlo. Frente propio con PASO 0 (grep de lectores primero).
38. **`[E2E-COVERAGE-GAP]`** — T6 (v6.79.0, ver `CLAUDE.md`) validó comportamiento YA EXISTENTE (`[IMPORT-WEIGHT-BYPASS]`, v6.78.0), no lo construido en esa misma tanda. Faltan escenarios E2E para: caída de referencia crítica (`[IMPORT-FETCH-ALLORNOTHING]`, cerrado en código pero sin E2E) y para el guard de `[CATALOG-WEIGHT-INPUTS]` cuando se implemente.
39. **`[IMPORT-HEADER-WEIGHT-LEGACY]`** — `COT-FFA1-1281`/`FFA1-1281` en `ayrsteel-test` tienen `item.calculatedWeight 5246.976` (correcto a nivel ítem) y `sale.totalWeight` en 0 (cabecera), con `uploadedAt` **2026-08-30T21:20:30.077Z** (medido en D2.1 de `[COB035GRIS-UNIT]`, v6.79.0 — no 2026-08-29 como se creyó al principio). **CONFIRMADO por fecha:** el import cayó en la ventana `[b82de647 2026-08-29T12:43:23Z .. c12639ed 2026-08-30T21:47:51Z]` de `[IMPORT-WEIGHT-BYPASS]` — apenas **27m21s antes** de que el fix de cabecera se commiteara. Es el artefacto exacto de correr el importador en esa ventana: el ítem ya tenía el peso bien (`b82de647` aplicado), la cabecera todavía no (`c12639ed` sin aplicar). Legado de esa ventana, NO regresión de v6.79.0. Decisión de negocio pendiente si se recalcula (mismo patrón que `[IMPORT-WEIGHT-BACKFILL]`).
40. **`[CATALOG-NAME-REGEN]` — ⬇️ DEGRADADO A COSMÉTICO en v6.81.0.** El T0 de la TANDA 1 AUTÓNOMA dio **NO**: el `1.220` horneado en los `displayName` de prod **no llega a ningún documento que vea el cliente** (único template de impresión del repo no renderiza `productName`, y además tiene 0 consumidores). Sin impacto comercial demostrable, el ítem deja de ser candidato de frente y queda como limpieza de dato a resolver junto con `[ANCHO-UTIL-VS-DESARROLLADO]`. **Gatillo de re-apertura:** que aparezca un documento impreso o exportado hacia el cliente que SÍ renderice `productName`. — los `displayName` de `metallic_roofing_catalog` DIVERGEN entre entornos, y prod perdió información comercial en el camino. Medido 2026-08-31: en `ayrsteel-test` **14 SKUs** conservan la descripción comercial original con el ancho ÚTIL adentro del string (ej. `PL030AZ6MT` → `"COBERTURA ALUZINC RA-4 0.30 X 1.05 X 6000 AZUL RAL 5002"`); en `ayrsteel-2026` los mismos SKUs tienen el nombre REGENERADO a formato normalizado que hornea el `widthMm` del catálogo (`"PLANCHA ALZ-AZUL-5002 0.3MM X 1.220 X 6.00MT"`). Los dos entornos tienen `widthMm = 1220`; lo que difiere es que prod ya no conserva el `1.05` en ningún campo. **Atenuante medido en la TANDA 1 AUTÓNOMA (T0): ese `1.220` NO llega a ningún papel que vea el cliente.** `PrintableTicket.tsx` es el único template de impresión del repo, no renderiza `productName` (su celda de descripción es `Perfil {item.sku}`), y además tiene 0 consumidores desde v6.40.1. Los 2 únicos renders de `productName` son `SaleDetailsModal.tsx` (modal interno) y el payload de `quotations/[id]/edit`. Sin PASO 0 propio; se decide junto con `[ANCHO-UTIL-VS-DESARROLLADO]`, del que es evidencia.
41. **`[CATALOG-WIDTH-6000]`** — `PL030VERD6000` en `ayrsteel-test` tiene hoy `widthMm: 6000`, que es el LARGO en el campo de ANCHO. Su `displayName` es `"PLANCHA ALU-VERDE-RAL-6035 0.3MM X 6.000 X 1.05MT"` — ancho y largo en orden invertido respecto de `ayrsteel-2026`, que tiene `widthMm: 1050` y `"...0.3MM X 1.050 X 6.00MT"`. Medido 2026-08-31: en el recon anterior ese campo estaba en `null` y cambió con la corrección del dueño del 2026-08-31 (que sí arregló `family` y `finish`). Es dato de sandbox, no de negocio, pero mientras esté así ese SKU calcula peso con un ancho de 6 metros. Prod NO tiene el problema.
42. **`width` / `widthMm` DUPLICADOS en `metallic_roofing_catalog`, sin dueño único.** Medido sobre los 54 docs de `ayrsteel-2026`: existen los DOS campos y son redundantes entre sí (`width: 1.22` ↔ `widthMm: 1220`, `width: 1.05` ↔ `widthMm: 1050`). No hay ningún otro campo de ancho en todo el catálogo (`usefulWidth`/`anchoUtil`/`coverageWidth` NO existen — barrido de todas las claves de los 54 docs). Nadie declara cuál es la fuente y cuál el derivado, así que una corrección aplicada a uno solo los desincroniza en silencio. Misma familia que `[CATALOG-DENSITY-DENORM]`: se decide junto, con un PASO 0 que grepee lectores de cada uno antes de tocar nada.
43. **`[WIDTH-FALLBACK-SPLIT]` — 🔒 SIGUE BLOQUEADO POR FALTA DE HARNESS DE COMPONENTE. ⚠️ C3 (v6.82.0) NO lo desbloqueó** — su bloqueante es `ProductSelector.tsx` (componente React), distinto del de #12. Mismo patrón: el cambio es de 2 caracteres, lo caro es poder anclarlo. **No tomar antes de resolver el harness.** — REGISTRADO Y SALTADO EN LA MISMA TANDA (TANDA 1 AUTÓNOMA, v6.80.0). Los 2 fallbacks de ancho DIVERGEN entre los 2 caminos de venta, **re-verificado en C2**: el importador usa `widthMm: productInfo?.widthMm ?? 0` (`parseImportRows.ts:294`) y el carrito `widthMm: product.widthMm ?? 1200` (`ProductSelector.tsx:502` y `:512` — la llamada a `calcCoverageWeightKg` y el `weightSnapshot` que se PERSISTE). ⚠️ **CORRECCIÓN C2: la fila afirmaba que esas eran "las únicas 2 ocurrencias de `1200` en `src/`" — FALSO, hay 187 ocurrencias del literal.** Lo correcto es que son las únicas 2 de **`?? 1200`** (el grep original midió el patrón con el operador, no el literal suelto). Lo que sí sigue en pie: `1200` **no es un valor presente en el catálogo** (medido en prod: 53 docs en `1220` + 1 en `1050`). **DECISIÓN YA TOMADA por el dueño: unificar los DOS a `?? 0`** — 0 es un error visible, 1200 es un error plausible que nadie detecta. NO agregar flag ni warning (eso es `[CATALOG-WEIGHT-INPUTS]`, registrado como NO IMPLEMENTAR). **Bloqueante:** los 2 `?? 1200` viven inline en `ProductSelector.tsx`, que arrastra 5 servicios de catálogo, `useFinishes`, `useConfirm` y Firestore — montar ese harness de componente para cambiar `1200` por `0` es un frente propio. Hoy latente: todos los docs de catálogo tienen `widthMm`, así que el fallback no se ejerce.

> **NOTA (recon 2026-08-27) — el frente "modal vs página" quedó DISUELTO.** 20 modales + 4 páginas dedicadas, y las 4 son las complejas (`production/new`, `purchases/new`, `quotations/[id]/edit`, `sales/new`). La convención de `CLAUDE.md` (§5, "Patrón de forms") ya se cumple — no hay nada que migrar. La sensación de "form que recarga" es `[NAV-HARD]` (arriba), no un problema de arquitectura de forms.

44. ~~**`[QUOT-TEST-MIENTE]`**~~ ✅ **BORRADO — LA PREMISA ERA FALSA (v6.82.0, C1). NO RE-ABRIR.** La fila afirmaba que el test "pasa CON la query rota" y que el comentario de su 3er caso (`:104`) era "falso a nivel backend". **Las dos cosas son incorrectas: la query nunca estuvo rota** (ver bloque de cierre de #9 al pie), así que el test no miente — mide una query que anda, y el comentario que dice que un `COT-*` QUOTATION "matchea ambas queries" es **CORRECTO**, confirmado en runtime.
    - **Lo único que sobrevive es una observación de COBERTURA, mucho más chica y de prioridad BAJA:** los 4 fixtures del archivo no incluyen ningún `COT-*` con status fuera de `['QUOTATION','CANCELLED']`, así que la suite no distingue el aporte de la query de rango del de la query de status. No es un test que mienta; es un test que no cubre un caso. Si alguien toca ese archivo, agregar un fixture `COT-*` VOIDED cierra el hueco en 3 líneas. **No amerita frente propio.**
45. **`[PRINT-DOC-CLIENTE]` — PREGUNTA DE NEGOCIO PARA EL DUEÑO, sin responder por cuenta propia. VIVA, y C2 (v6.82.0) la REFUERZA con una corrección de mecanismo.** Medido en T0 (v6.80.0) y **re-verificado en C2**: `src/components/sales/PrintableTicket.tsx` tiene **0 consumidores** ✓ y su celda de descripción es `Perfil {item.sku}` (`:208`) ✓ — no renderiza el nombre comercial del producto. ⚠️ **CORRECCIÓN: la fila lo llamaba "el único template de impresión de todo el repo", citando un grep de `window.print`/`@media print` con 1 hit. Hoy ese grep da 0 archivos en TODO `src/` — el propio `PrintableTicket.tsx` no contiene ninguno de los dos.** O sea que el repo **no tiene ningún mecanismo de impresión en absoluto**, lo que hace la conclusión de esta fila más fuerte, no más débil. Los 2 exports con jsPDF son reportes internos (`aluzinc-detalle`, `bobinas-supervisor`), no documentos de cliente. **Consecuencia, dicha sin rodeos: hoy el ERP no emite ningún papel para el cliente desde esa vía.** ❓ **PREGUNTA: ¿el cliente recibe hoy algún documento generado por el sistema (ticket, cotización impresa, comprobante), o todo sale por fuera del ERP?** La respuesta decide 3 cosas de un saque: si `[CATALOG-NAME-REGEN]` (#40) vuelve a subir de prioridad, si el template huérfano se borra o se cablea, y si hace falta un frente de documento de cliente. **No implementar nada hasta tener la respuesta.**
47. ~~**`[IMPORT-PERCHA-ARCHIVE]`**~~ ✅ **CERRADO (v6.84.0, TANDA 5). Número conservado, fila NO borrada (regla v6.68.0 B17/B18 — se documenta el cierre en su lugar).** La decisión registrada en v6.76.0 tenía dos mitades: (a) percha `CANCELLED` / con producción activa → **abortar la transacción entera** (cerrado en v6.83.0, `[IMPORT-OVERWRITE]`); (b) percha `QUOTATION` **sin** producción → **archivar a `history` y pisar, igual que `saleRef`** (cerrado ACÁ). Guard verbatim citado antes de tocar (`runSaleImportTransaction.ts:181-193` de v6.83.0), `firestore.rules:107-110` (`sales/{id}/history`, `allow write: if canWrite()`, sin field guard) verificado antes de escribir. RED contra el MÓDULO REAL (`salesImportPerchaArchive.integration.test.ts`, fixture sembrado con `buildImportWrites`, no a mano): `expected +0 to be 1` (history vacío) → GREEN 2/2. Control positivo (criterio GRUPO K, v6.64.0): sin percha previa, no se archiva nada — verifica que el guard no archiva SIEMPRE. 2 mutaciones, las 2 discriminan igual (**1 failed / 4 passed** cada una — el único rojo es siempre el caso archivado, el control y los 3 tests de `[IMPORT-OVERWRITE]` quedan verdes bajo ambas): M1 (saltear el archivado con `if (false && ...)`) y M2 (invertir la condición a `!== "QUOTATION"`). Escribe `sales/COT-{doc}/history/{auto}` + `audit_logs` `QUOTATION_REPLACED` (acción PROPIA, NO reusa `SALE_REPLACED` — reusarla haría 2 `SALE_REPLACED` por 1 sola operación de re-importación y descuadraría cualquier conteo de reemplazos de venta). `simulateImport`/`simulateImportWithLogic` NO se tocaron — la divergencia con la función real creció a un 8º punto (ver #18). Delta predicho antes de correr y medido después: `test:emu` 36/242/3skip (245) → **37/244/3skip (247)**, coincide 1:1; `npm run test` sin cambios (126/1313), integration fuera de su scope. `git diff` vacío en las 2 mutaciones antes de commitear. Vive en `src/core/import/runSaleImportTransaction.ts`. **✅ E3.1 (remediación, misma tanda): el audit `QUOTATION_REPLACED` fue verificado independientemente del archivado.** M3 (saltear SOLO la escritura del audit, dejando `history` intacto) → **1 failed/4 passed**, rojo exacto en `auditSnap.size` (no en `historySnap.size`) — el assert discrimina el audit por sí solo, no solo como efecto colateral del archivado. Revertida, `git diff` vacío.

48. **`[PERCHA-RESOLVE-SIN-UI]` — NUEVO (v6.86.0, TANDA 7 / U3). El mensaje del guard le pide al operador algo que la UI no le permite hacer. CERO código en esta tanda: se registra medido y se deja.**
    - **El mensaje, verbatim** (`runSaleImportTransaction.ts:58`): *"La percha de producción {quoteId} ya existe y está CANCELADA. Re-importar la pisaría en silencio: se aborta la importación de este comprobante. **Resolvé la percha antes de re-importar.**"*
    - **Medido POST-U2, con las 2 acciones que hoy existen en `/admin/quotations`:** sobre una percha `CANCELLED` + `IMPORTADA`, `canEditQuotation` da **false** (falla por origen Y por estado) y `canAcceptQuotation` da **false** (ídem). **Cero acciones ofrecidas.** El operador lee "resolvé la percha" y no tiene ningún botón que resuelva nada.
    - **Confirmado en runtime, no teórico:** el dueño corrió el importador en `ayrsteel-test` y el guard disparó sobre `COT-FFA1-1291` y `COT-FFA1-1292`, las 2 `CANCELLED` (2 filas en ERROR, TOTAL 25 / IMPORTADAS 2 / OMITIDAS 21 / ERRORES 2). La verificación V1 midió que **el aborto no escribió nada** — el guard hace lo suyo bien; lo que falta es la salida para el operador.
    - **Alcance a decidir por el dueño, NO elegido acá:** (a) una acción de "reactivar percha" (`CANCELLED` → `QUOTATION`) en `/admin/quotations`, que es un cambio de `status` y por lo tanto toca el eje que `[QUOTATION-APPROVE-UNREACHABLE]` deliberadamente no tocó; (b) o cambiar el mensaje para que pida algo que sí se puede hacer hoy (ej. anular la venta gemela, que es lo que en la práctica dejó esas 2 perchas en `CANCELLED`). Son 2 frentes distintos con costos distintos. **No implementar sin decisión.**

49. **`[RULES-GUARD-CAMPO-FANTASMA]` — NUEVO (v6.86.1, TANDA 7.1 / R3.3). La denylist financiera de `firestore.rules` protege 3 campos que NINGÚN escritor de `sales` escribe, y uno de ellos no existe en el repo entero. CERO código, cero edición de `firestore.rules` en esta tanda.**
    - **El guard, verbatim** (`firestore.rules:102`, coordenada re-verificada en esta tanda, sin corrimiento desde U0): `fieldsUnchanged(['totalAmount', 'subtotal', 'igv', 'exchangeRate', 'currency', 'items', 'paymentType'])`.
    - **Origen del hallazgo:** medido de rebote en U0.2 (v6.86.0), donde AGREGAR `subtotal` —un campo que el builder canónico no escribe— fue justamente el control que hizo **RECHAZAR** y le dio sentido a U0.1. Esta tanda lo formaliza con el conteo por campo.
    - **Tabla campo × escrito, medida sobre los escritores REALES de `sales`** (`saleDocBuilder.ts`, `salesService.ts`, `salesImportLogic.ts`, `runSaleImportTransaction.ts`, `functions/src/callables/editQuotation.ts`, `functions/src/callables/sales.ts`):

      | campo de la denylist | ¿lo escribe un escritor de `sales`? | evidencia |
      |---|---|---|
      | `totalAmount` | **SÍ** | `saleDocBuilder.ts:221` (`buildSaleDoc`) y `:246` (`buildQuotationDoc`) |
      | `items` | **SÍ** | `saleDocBuilder.ts:220` y `:245` |
      | `currency` | **SÍ**, solo el importador | `salesImportLogic.ts:40`, `:63`, `:87` |
      | `exchangeRate` | **SÍ**, solo el importador | `salesImportLogic.ts:41` (`sale.exchangeRateApplied \|\| 1` — ojo: `:64`/`:88` escriben `exchangeRateApplied`, que es un campo DISTINTO y NO está en la denylist) |
      | `subtotal` | **NO** | 0 hits en los 6 escritores |
      | `igv` | **NO** | 0 hits en los 6 escritores |
      | `paymentType` | **NO, y además no existe en el repo** | ver conteo por árbol abajo |

    - **Conteo CRUDO por término y por árbol (B20), excluyendo `.test.`/`.spec.`:**

      | término | `src` | `functions/src` | `scripts` |
      |---|---|---|---|
      | `totalAmount` | 95 | 16 | 5 |
      | `items` | 1179 | 30 | 24 |
      | `currency` | 208 | 21 | 0 |
      | `exchangeRate` | 144 | 23 | 0 |
      | `igv` | 45 | 0 | 0 |
      | `subtotal` | 15 | 0 | 0 |
      | `paymentType` | **0** | **0** | **0** |

    - **`paymentType` es el caso fuerte: 0 hits en los TRES árboles de código.** Con tests incluidos aparece 7 veces y las 7 son o fixtures a mano (`operatorWrites.rules.test.ts:55`, `sales.rules.test.ts:40`, `salesStatusAccessor.rules.test.ts:53`, los 3 con `paymentType: "CONTADO"`) o citas de la propia rule en comentarios/recon. **Ningún doc de `sales` de producción tiene ese campo** — la rule protege un campo fantasma, y 3 fixtures de test lo siembran a mano, que es exactamente la trampa que B19 describe.
    - **Severidad: BAJA, y por qué.** El guard falla CERRADO: proteger un campo inexistente no abre nada, solo hace que la denylist describa un shape de doc que no es el que producción escribe. El riesgo real es de LECTURA — alguien que use la denylist como referencia del shape financiero canónico va a asumir que `subtotal`/`igv`/`paymentType` existen. Ya pasó: los 3 fixtures a mano de arriba.
    - **NO se toca `firestore.rules` acá.** Quitar campos de una denylist es relajar un guard de seguridad: es decisión del dueño, con su propio RED (un test que pruebe que quitarlos no habilita ninguna escritura hoy bloqueada), no una limpieza cosmética.

> **VERIFICACIÓN DE COLA — C2, 2026-08-31 (v6.82.0). 29 ítems vivos, un grep/medición por fila.**
> Motivo: 3 premisas habían muerto al contacto en 2 tandas seguidas
> (`[COIL-TYPE-KEY-HARDEN]`, `[QUOT-FETCH-RANGE]`, `[TOAST-WARNINGS-ARRAY]`).
> **Resultado: 2 MUERTAS · 12 MUTADAS · 15 VIVAS. Ninguna fila sobrevivió por inercia.**
> Las MUTADAS y MUERTAS están reescritas arriba, cada una con la medición que la decide.
> Las 15 VIVAS quedan selladas acá con la evidencia que las sostiene:
>
> | # | slug | evidencia que la sostiene (2026-08-31) |
> |---|---|---|
> | 3 | `[DRYWALL-ORPHAN-LOGS]` | prod: **9** `production_logs` con `parentCoilId F001-13039-8`, y ese doc **no existe** en `coils`. Exacto. |
> | 6 | `[E2E-HARNESS]` | **0 hits** de `playwright` en `package.json` — sigue sin ser dependencia. |
> | 14 | 18 discrepancias del `REPORTE.md` | el directorio existe fuera del repo, sin re-verificar 1x1. |
> | 19 | `[STRATEGY-DUP]` | duplicación cliente/server intacta; `writeProductionIncrement` de `roofing`/`trading`/`services` sigue sin caller propio. |
> | 23 | `[WAC-TRIPLE-DUP]` | **9** archivos `.parity.test.ts` en el repo, **ninguno de WAC**. |
> | 30 | `[IMPORT-WEIGHT-BACKFILL]` | sostenida por dato real: `FFA1-1281` en test sigue con `totalWeight: 0`. Decisión de negocio, no técnica. |
> | 32 | `[ANCHO-UTIL-VS-DESARROLLADO]` | fórmulas divergentes confirmadas (`coverageWeightCalc` usa `widthMm` de catálogo, `coilProduction.ts:45` usa `coil.masterWidth`). **Gatillo NO disparado:** prod tiene 38 coils, **38/38 `GALV`**, `masterWidth` 1200 en 38/38, **0 aluzinc**. Sigue DORMIDO. |
> | 33 | `[CATALOG-WEIGHT-INPUTS]` | degradación silenciosa confirmada: `pesoKg: number \| null`, y `parseImportRows.ts:299` (`if (pesoKg !== null)`) cae a `catalogWeight` (`:279`) sin flag. |
> | 34 | `[CATALOG-LINT]` | propuesta sin PASO 0; los campos que chequearía existen. |
> | 35 | `[CATALOG-DUP-VERDE]` | exacto: prod tiene `COB040VERD`, test tiene `COB040VERDE`, cada uno solo en su entorno. |
> | 36 | `[CATALOG-DENSITY-DENORM]` | prod: **2/54** docs con `densityFactor` nulo ✓; `aluzincDetalleLogic.ts:213` sigue leyendo `product.densityFactor` del catálogo ✓. |
> | 38 | `[E2E-COVERAGE-GAP]` | meta-ítem, sin cambios. |
> | 39 | `[IMPORT-HEADER-WEIGHT-LEGACY]` | exacto en test: `COT-FFA1-1281` y `FFA1-1281` con `totalWeight: 0` y `calculatedWeight` de ítem **5246.976**. |
> | 41 | `[CATALOG-WIDTH-6000]` | exacto: `PL030VERD6000` en test con `widthMm: 6000`; prod no lo tiene. |
> | 42 | `width`/`widthMm` duplicados | prod: `widthMm` 1220×53 + 1050×1, 0 ausentes; `usefulWidth`/`anchoUtil`/`coverageWidth` **no existen** en `src/` ni `functions/src/`. |

> **CERRADO Y BORRADO DE ESTA LISTA — `[QUOT-FETCH-RANGE]` (#9) y `[QUOT-TEST-MIENTE]` (#44)**
> (v6.82.0, C1, READ-ONLY, cero código de producción tocado):
> **NUNCA FUE UN BUG.** Instrumentada la función REAL y **impresos** los límites en runtime:
> el inferior es `'COT-'` (`charcodes [67,79,84,45]`, length 4); el superior es
> `'COT-'` (`charcodes [67,79,84,45,` **`63743`** `]`, length **5**), `BOUNDS_IDENTICAL: false`.
> `63743` = `0xF8FF` = **U+F8FF**, el sentinela canónico de prefijo de Firestore. Medido
> contra el emulador: E1 (1 doc `COT-VOID-1` VOIDED, fuente de status aportando 0) → la
> query de rango devuelve **1**; E5 (2 `COT-*` + `INV-A`/`V-000099`/`Z-999`) → devuelve
> **exactamente los 2 `COT-*`**. El carácter está en el blob de HEAD (`efa3bf`), entró en
> `e1dd90c6` (el commit original del Frente #9-A) y es la **única** ocurrencia de U+F8FF
> en todo `src/` + `functions/src/`. **La query nació correcta y nunca se rompió.**
> **Por qué sobrevivió 4 tandas:** U+F8FF no se ve al leer el fuente, no se ve en `grep`
> (que imprime la línea como `'COT-'`), y **tampoco en `JSON.stringify`** — que lo
> devuelve como `"COT-"` por ser un carácter imprimible del rango Private Use Area.
> Solo lo delatan **charcodes** y **length**. Todas las mediciones previas que dieron 0 se
> hicieron sobre una query **reconstruida a mano sin ese carácter** — no era la misma query.
> La "ANOMALÍA ABIERTA" de v6.81.0 queda EXPLICADA por esto, y su sospecha (duplicación
> del módulo `firebase/firestore` en el grafo de vitest) queda **REFUTADA**. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[NAV-B-FINISHES]`** (v6.81.0, `83d9ecaf` + `8c33159e` + `5c44ab8b`):
> `useFinishes` gana `refetch` (aditivo, estable por `onlyActive`) y los 2
> `window.location.reload()` de la página de acabados pasan a `await refetch()`;
> grep post-cambio: 0 `window.location` en el archivo. También se fue el
> `"use client"` duplicado que la fila listaba como colateral. El bloqueante que
> la fila declaraba ("hook COMPARTIDO") se midió antes de tocar: 15 consumidores,
> **ninguno depende del remount**, así que agregar `refetch` no rompe a nadie.
> RED→GREEN 2 rojos por el mecanismo → 3/3, + 2 mutaciones que discriminan.
> Verificado además en browser real contra `ayrsteel-test` (F1), afirmando la
> AUSENCIA de reload por marcador en `window`, no solo que el dato aparece.
> NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[TOAST-WARNINGS-ARRAY]`** (v6.81.0, `7da0ef93`):
> se borró el estado muerto `lastResult`/`setLastResult` (declarado, nunca leído;
> 2 ocurrencias en todo `src/`, las 2 en ese archivo). ⚠️ **El cierre REFUTA la
> premisa con la que el ítem se promovió:** los warnings NO eran invisibles —
> `production/new/page.tsx:393-412` ya cubre las 4 combinaciones con toasts de
> 6000ms desde v6.77.0 — y **no existe ningún array de warnings**: el retorno del
> callable son DOS BOOLEANOS (`hasNegativeCoilWarning`, `hasWacResetWarning`), o
> sea que el nombre del ítem nombraba una estructura inexistente. No se inventó un
> warning ni se cableó un array que no existe. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[COIL-TYPE-KEY-HARDEN]`** (v6.80.0, `55634e76`):
> `coilTypeKey` obligatorio en `Coil`. ⚠️ El cierre CORRIGE 2 premisas de esta misma
> fila: (1) los 3 archivos que citaba como consumidores a migrar NO referencian el
> campo — hay 3 escritores, 1 tipo, 1 helper y **CERO lectores**; (2) el cambio es
> **INERTE**: `tsc --noEmit` da 0 antes y después, porque todo `Coil` sale de
> `{ ...d.data() } as Coil` y una aserción saltea el chequeo. `ayrsteel-test` tiene
> 1 bobina sin el campo. Detalle en `CLAUDE.md` v6.80.0. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[SALES-EDIT-404]`** (v6.80.0, `44596d38`):
> opción (a) del dueño: borrado, no re-apuntado. Se fueron el ítem del menú, la prop
> `onEdit`, su destructuring, el handler de `admin/sales/page.tsx`, el fixture del
> test y el import huérfano del icono `Edit`. La ruta unificada de documento sigue
> siendo frente propio del dueño. `docs/modules/ventas.md` §14 corregido. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[CHECKDOCS-BANNER]`** (v6.80.0, `d7cde925`):
> `BANNER_TRIGGER` ahora exige además una MARCA explícita (`⚠` al inicio o
> `ÚLTIMA VERIFICACIÓN`), y un doc protegido en la lista de saltados pasa a ser
> ERROR DURO con exit != 0. 2 mutaciones, 3 failed/13 passed cada una, revertidas.
> No-regresión medida antes y después sobre el repo real: **19 ROTO / 5 saltados**,
> los mismos 5. 16 tests nuevos en `scripts/checkDocs.test.cjs`. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[COMENTARIO-STALE-COVERAGE]`** (v6.80.0,
> `14343158`): el docstring de `toProductKind` decía "usa family+unit"; la firma real
> recibe solo `family`. Corregido junto con el checklist de `template.md`, que citaba
> un `ACTIVE_MODULES` y un context que no existen (el mecanismo real es el registry).
> NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[EMU-STALE-LIB]`** (v6.79.0, commit `888a9038`):
> guard por MTIME en `scripts/emuStaleGuard.cjs` (2 puros, 8 tests) cableado en
> `with-emulator.cjs`. NO build incondicional — aborta con mensaje accionable si
> `functions/lib` es más viejo que `functions/src` Y el comando interno arranca el
> emulador de functions (`test:emu:rules` corre `--only firestore`, se saltea a
> propósito). Mutación `>`→`<`: 2 failed/6 passed. No-regresión: `touch
> functions/src/index.ts` aborta exit 1 nombrando `cd functions && npm run build`;
> `test:emu:rules` con el mismo lib stale NO aborta. Detalle en `CLAUDE.md` v6.79.0.

> **CERRADO Y BORRADO DE ESTA LISTA — `[IMPORT-WEIGHT-MULTIITEM]`** (v6.79.0, `77bac3ae`):
> cabecera con 2 ítems metallic distintos, derivado a mano `351.36+175.68=527.04`,
> predicho VERDE y salió VERDE — caracterización, no se forzó un rojo que la
> aritmética no daba. Mutación `+=`→`=`: 1 failed/5 passed, los 2 tests mono-ítem
> quedaron VERDES (ciegos al defecto). Detalle en `CLAUDE.md` v6.79.0.

> **CERRADO Y BORRADO DE ESTA LISTA — `[IMPORT-WEIGHT-ZEROQTY]`** (v6.79.0, `b714dc56`):
> ALCANZABLE, confirmado por grep — los 3 `return;` del loop no miran `CANTIDAD`.
> Mecanismo: `calcCoverageWeightKg` con `quantity 0` da `pesoKg:0` (no `null`); sin
> el guard `cantidad>0`, `0/0=NaN` se filtraba al doc. Mutación M3: 1 failed/6 passed.
> Detalle en `CLAUDE.md` v6.79.0.

> **CERRADO Y BORRADO DE ESTA LISTA — `[IMPORT-FETCH-ALLORNOTHING]` + `[CATALOGREF-ANY]`**
> (v6.79.0, `8a5eacf4`): **corrección de encuadre — el `Promise.all` NO era inseguro, era
> fail-SAFE** (una caída rechazaba el lote entero, los refs quedaban vacíos, y
> `handleFileUpload` ya bloqueaba con `catalogRef.length===0`). El defecto real era de
> DIAGNOSTICABILIDAD: ahora `Promise.allSettled` + toast que NOMBRA cada colección
> caída. Las 10 colecciones son CRÍTICAS (medido contra `parseImportRows`: 5 catálogos
> → peso, 4 stocks → costo, `coil_finishes` → densityFactor) — se conservó el corte
> total. `as any` fuera, spread `...d.data()` antes de `sku`/`businessLine` para que
> `d.id` gane sobre un campo denormalizado. 2 puros extraídos a
> `src/core/import/referenceRefs.ts`. Mutación M-T5 (spread revertido): 2 failed/8
> passed. Mutación M-T4 (`rejected`→`fulfilled`): 5 failed/5 passed. Detalle en
> `CLAUDE.md` v6.79.0.

> **CERRADO Y BORRADO DE ESTA LISTA — `[COB035GRIS-UNIT]`** (v6.79.0): **la premisa
> original (esta misma fila) ERA FALSA, medido, no una premisa mal verificada.**
> `calcCoverageWeightKg` (`coverageWeightCalc.ts:46`) desestructura
> `{family, quantity, thicknessMm, widthMm, densityFactor, lengthM}` — `unit` NUNCA
> se lee. El branching es 100% por `family`. Con `PIEZA` o con `METRO`, `COB035GRIS`
> daba SIEMPRE el mismo número — jamás hubo un peso "~10× por debajo del real".
> Fix aplicado, de COHERENCIA DE DATO (no de peso): `unit` `"PIEZA"`→`"METRO"` en
> `ayrsteel-2026` (1 campo, `update()`, backup `sha256 e253cb8e...`, audit_log
> `ZL21n49JJA74tnTMHERB` action `CATALOG_UNIT_FIX`). `ayrsteel-test` ya decía
> `METRO`, no se tocó. Ver `[CATALOG-LINT]` para el chequeo que reemplaza esta
> preocupación, y `B14` en `CLAUDE.md` §11 para la lección de fondo (parámetro
> pasado ≠ parámetro leído).

> **CERRADO Y BORRADO DE ESTA LISTA — `[CUTORDER-FX]`** (ex-posición 1 hasta v6.67.0):
> `cutOrderService.ts` doble conversión USD→PEN. Estaba cerrado desde **v6.52.0**
> y esta lista lo arrastró igual. Re-verificado en v6.68.0 en las 3 capas. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[SALES-STATUS-ACCESSOR]`** (v6.69.0):
> fix desplegado y verificado por REST en los DOS proyectos. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[CASCADE-DUP]`** (v6.70.0, ex-posición 1):
> copia cliente de `buildAnnulmentCascade`/`canAnnulSale` borrada, 5 casos `self`
> portados a cobertura server-only ANTES de borrar (detectado por mutación que el
> parity era su único hogar). Commits `bd1613d8`/`7061cad2`/`f88cbb02`, sin push.
> Detalle completo en la entrada v6.70.0 y en CLAUDE.md v6.70.0. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[NAV-HARD]`** (v6.72.0, ex-posición 4):
> 4 de 9 `window.location` swapeados a `router.push` en `/admin/sales` (solo 3 con
> efecto vivo: `onEdit` es cableado muerto). 2 legítimos en `layout.tsx`, 1 cerrado
> por análisis en `settings` (inalcanzable en prod), 2 derivados a
> `[NAV-B-FINISHES]`. El recon previo tenía 2 errores (9/4 no 9/5; el `url` de
> `onDuplicate` NO era falso positivo) — corregidos y medidos. La trampa del Router
> Cache quedó DESCARTADA POR MEDICIÓN. NO re-abrir.
> **CERRADO Y BORRADO DE ESTA LISTA — `[COIL-NEG-WEIGHT]`** (v6.71.0, ex-posición 3):
> CERRADO POR `[ALUZINC-RESET]` 2026-08-27, no por un fix. Las 27 bobinas con peso
> negativo eran 100% aluzinc y fueron borradas físicamente. MEDIDO post-borrado: 0
> bobinas con `currentWeight < 0` sobre las 38 GALV sobrevivientes. ⚠️ La CAUSA del
> peso negativo NO se corrigió — si reaparece tras cargar agosto, es el mismo bug,
> no uno nuevo. **Matiz v6.77.0, agregado sin editar lo de arriba:** esa causa tenía
> 2 componentes — cálculo (WAC mal calculado al cruzar a positivo, CERRADO por
> `[STOCK-NEG-WAC]`) y diseño (arrancar en negativo en sí, aceptado por decisión de
> negocio, ver cierre de `[STOCK-NEG-GUARD]` arriba). Si reaparece, puede ser el
> componente de diseño operando tal como fue decidido, no necesariamente una regresión.

> **CERRADO Y BORRADO DE ESTA LISTA — `[STOCK-RECON]`** (v6.71.0, ex-posición 4):
> CERRADO POR `[ALUZINC-RESET]` 2026-08-27, no por un fix. Los 7 SKUs con excedente
> (`COB030ROJO`/`PL030NT6M`/`COB030AZUL`/`PL030RJ6MT`/`PL030AZ6MT`/`PL030NT515`/
> `PL030NT366M`) eran 100% metallic-roofing y su colección entera fue borrada
> físicamente. MEDIDO post-borrado: 0 docs en `metallic_roofing_stock` (P3.2 de
> `[ALUZINC-RESET]`), 0 con cantidad negativa. ⚠️ La CAUSA del excedente NO se
> corrigió — si reaparece tras cargar agosto, es el mismo bug, no uno nuevo.
> **Matiz v6.77.0, agregado sin editar lo de arriba:** esa causa tenía 2 componentes —
> cálculo (WAC mal calculado al cruzar a positivo, CERRADO por `[STOCK-NEG-WAC]`) y
> diseño (arrancar en negativo en sí, aceptado por decisión de negocio, ver cierre de
> `[STOCK-NEG-GUARD]` arriba). Si reaparece, puede ser el componente de diseño
> operando tal como fue decidido, no necesariamente una regresión.

> **CERRADO Y BORRADO DE ESTA LISTA — `[ALUZINC-RESET]`** (v6.71.0, ex-posición 1):
> 1207 docs borrados físicamente de `ayrsteel-2026`, autorizado por el dueño, una
> sola corrida, 0 fallos de batch, 13/13 métricas post-borrado exactas. Los 3
> bloqueantes registrados aquí se resolvieron así: (i) kardex append-only → excepción
> explícita fechada y autorizada, NO precedente; (ii) ventas con SUNAT → premisa del
> dueño al abrir el frente ("SUNAT no implementado, 0 comprobantes, sin implicancia
> fiscal"), NO re-verificada de forma independiente en el PASO 0 — queda como premisa
> aceptada, no como medición propia; (iii) precedente del borrado
> sin auditoría de 13-17 ago → evitado con congelado de IDs + backup verificado por
> sha256 + pre-check de conteos antes de cada paso. Detalle completo en la entrada
> v6.71.0 y en CLAUDE.md v6.71.0. NO re-abrir.

> **CERRADO Y BORRADO DE ESTA LISTA — `[IMPORT-SIM-DRIFT]`** (v6.76.0, ex-posición 9):
> los 3 rojos deterministas de `salesImport.test.ts` cerrados en 4 commits, un cambio
> por corrida, predicción escrita antes de cada una (`28a1426d`/`e91b0af6`/`cd6914f5`/
> `26307854`). Causa: el guard `firestore.rules:81-84` (`1dd80865`, 2026-08-26) es
> correcto; la simulación de test nunca seteaba `status` y su `catch` se tragaba el
> `PERMISSION_DENIED`. `test:emu` completo re-medido en 0 rojos (35 archivos/231
> passed/3 skipped/0 failed). Destapó 2 hallazgos de datos, en ese momento abiertos como
> frentes nuevos: `[IMPORT-REDO]` y `[STOCK-NEG-GUARD]`. ⚠️ **Ambos marcados CERRADOS en
> v6.77.0, EN SU PROPIO LUGAR de la lista numerada** (ítems 16 y 17 arriba — no se borran,
> conservan su número, con fecha y motivo de cierre). Detalle completo en CLAUDE.md
> v6.76.0. NO re-abrir.

## HORIZONTE (candidatos próximo frente)
- ~~**NC no es orden de producción — candidato, SIN recon hecho.**~~ ✅ **RESUELTO FORWARD (v6.55.0, commit `608e8d08`) — RETRO PENDIENTE.** El gate nuevo `createsProductionPercha` (allowlist `documentType in [FACTURA,BOLETA]`) hace que ninguna NC (ni POS) vuelva a crear percha desde ahora. En su momento era forward-only: **verificado en prod (read-only, 2026-08-22)** que las 6 perchas `COT-FFC1-*` que ya existían de antes SEGUÍAN calificando 6/6 para `PRODUCTION_QUEUE_FILTER` (`status:QUOTATION`, `productionStatus:CONFIRMED`, `businessLines:[metallic-roofing]`). ✅ **RETRO CERRADO (v6.56.0 + v6.59.0): hoy es 0/6.** `COT-FFC1-44`/`COT-FFC1-72` quedaron `CANCELLED` por backfill manual en v6.56.0 (la cascada `488455d1` no había corrido sobre ellas porque su venta gemela ya estaba `VOIDED` antes de que el fix existiera). `COT-FFC1-57/61/64/67` quedaron `CANCELLED` en v6.59.0, al anular sus 4 NC gemelas vía el callable real (el duplicado que las bloqueaba se había resuelto en v6.58.0). Barrido de cola re-corrido (v6.59.0): 50 docs en `PRODUCTION_QUEUE_FILTER`, ninguno `COT-FFC1-*`. Ver CLAUDE.md v6.55.0/v6.56.0/v6.58.0/v6.59.0 y `docs/modules/annulment.md` §0-ter.
- **Recon de permisos de import de NC — parkeado, sin investigar.** Quién puede decidir `ncStockAction` en el preview del importador (hoy sin gate de rol visto en este frente) y si eso debería estar restringido dado que ahora ese campo determina un movimiento real de stock en la anulación, no solo en el import. Candidato a recon antes de tocar el importador de nuevo.
- **Slice 3 DIFERIDO:** capa de costo variable/conversión.
- **Frente 2 rol VENDEDOR:** recon hecho (isStaff, ROUTE_PERMISSIONS único guard vivo, isRoleAllowed/mod.routes.roles son código muerto, sellerId en prod es NOMBRE no email → hace falta sellerUid forward-only, agregados scopeados).
- **Reporte por RAL** (frente aparte).
- **PT inflado** por producciones contra cotizaciones importadas (venta histórica no descuenta stock, producción sí suma) → ajuste manual al cerrar el período. Ver `docs/modules/ventas.md` §9.
- **forward-fix** `coilWeightConsumedKg` en `consumeCoil` (drywall, mata `approximateWeight` de logs nuevos).
- **WRITE 8 cutOrder** (monstruo prorrateo, modelo flejes real + lote-por-bobina). ✅ **DESTRABADO en v6.52.0:** el bug de doble conversión USD→PEN que había que arreglar **antes** de este frente (para no meterlo a mitad del monstruo) ya está cerrado, con 3 tests de integración que anclan el costo de material y el de servicio por separado. ⚠️ **PRECONDICIÓN MEDIDA (v6.68.0):** `strips_stock` en prod tiene **3 docs con `totalStrips=0`/`totalWeight=0` pero `avgCostPerKg` residual** (`2.3576`, `2.5346`, `2.3576`) — residuo de la orden PEN de prueba. Es el estado inicial que va a leer el primer `receiveStrips` real: verificar cómo se comporta el WAC ante cantidad CERO con costo residual ANTES de escribir el prorrateo, no después. `strips_movements` en 0, `cut_orders` en 1 (ANULADA).
- **WRITE 9 salesService**.
- **Drawer piezas/bobina en PLANCHA — forward-fix del writer:** `produceFromCoils` hoy graba `piecesCount`/`pieceLengthM` en `perCoilBreakdown` solo para COBERTURA. Plancha muestra '—' en esas 2 columnas. Si se quiere desglose en plancha, es cambio de backend + backfill opcional (frente aparte).

## PENDIENTES OPERATIVOS

- **Smoke check Algolia en prod:** `/admin/sales` con búsqueda de texto activa → verificar que las 3 tarjetas de dinero se ocultan y el pie de tabla queda coherente (mensaje "Totales no disponibles en búsqueda por texto"). Test (`ayrsteel-test`) no tiene Algolia, solo verificable en prod.

## ESTADO FRENTE B (Cola fase 2)
**CERRADO.** Fase 1 + Fase 2 isFulfilled terminados. (Commits 3f5adb2f, 0ea8eb85, 4b7a17de, fbfc5b1a).

## PENDIENTES DIFERIDOS
- Componente `PrintableTicket` (src/components/sales/) queda en repo sin importadores; cablear ruta `/admin/sales/[id]/print` si aparece use case POS térmico. Botón en UI removido el 2026-08-14.

## REGLAS GRABADAS (aprendidas)
- **(v6.60.0) `eol=lf` NO convierte en checkout.** Git vuelca el blob tal cual: blob CRLF → worktree CRLF. La normalización ocurre solo al LEER (filtro `clean`). De ahí el bucle: `status` compara worktree-normalizado contra blob-crudo y siempre da "modificado". Medido: los archivos "sucios" tenían mtime igual al segundo del `checkout` del reflog — los escribió git.
- **(v6.60.0) Cuatro herramientas NO limpian el fantasma CRLF** — medido 1×1: `git restore`, `git checkout -- <file>`, `git checkout -f <branch>`, `git stash push -- <files>`. Todas reescriben desde el mismo blob CRLF. **La vía que sí funciona es operar sobre REFS:** `git push origin <local>:<remota>` (ff en el servidor, sin filtro de EOL) y `git branch -f <rama> <hash>` (sin checkout). Destrabó un `merge --ff-only` que había abortado 4 veces.
- **(v6.60.0) `hash-object --no-filters` y `git status` pueden discrepar legítimamente.** Uno compara bytes crudos, el otro aplica `clean`. Ambas lecturas son correctas — miden cosas distintas. Diagnóstico concluyente: `git ls-files --eol` (`i/` vs `w/`) + `git diff --numstat` (insertions == deletions == cantidad de CRLF). `git add --renormalize --dry-run` lista TODO el repo, no sirve para dimensionar.
- **(v6.60.0) `core.autocrlf` se lee en los 3 niveles** (system/global/local). Acá: system `true`, global `false`, local `false` → efectivo `false`. Mirar solo local+global dio diagnóstico incompleto. El system en `true` es bomba para un clon nuevo sin override local.
- **(v6.60.0) El límite de 30 disyunciones de Firestore es sobre el PRODUCTO de los `in`.** `status IN [4] AND finish IN [8]` = 32 → `INVALID_ARGUMENT: Too many disjunctions`. Medido: 2×8=16 pasa, 4×3=12 pasa, 4×8=32 rompe. Un `!=` cuenta como filtro de RANGO: con otro `in` + `orderBy` cae en "range and inequality filters on multiple fields" y exige índice compuesto.
- **(v6.58.0) La clave de agrupación decide el universo — verificar QUÉ WRITERS escriben el campo antes de dimensionar por él.** Agrupar por `adjustedDocument` (que solo pobla el writer de NC) subestimó el daño ×22: 4 documentos en vez de 89 grupos, 8 movimientos en vez de 179. Los 195 movimientos con el campo ausente — todas las ventas normales — quedaron fuera de la medición sin que nada lo señalara (el grupo `<CAMPO AUSENTE>` se lee como ruido). Un campo que solo escribe un writer no puede medir el universo de todos.
- **(v6.58.0) Al corregir un movimiento de kardex, la política de costo debe espejar la primitiva viva, no reinventar la aritmética.** Había 2 fórmulas válidas: revertir la mezcla WAC (`totalValue -= Σqty×costPerUnit`, recalculando `avgCost`) vs congelar `avgCost` y recomputar `totalValue = quantity × avgCost`. Se eligió la 2ª porque es lo que hace `writeAnnulNCDecrement`, la primitiva que correrá sobre estos mismos SKUs al anular las NC — con la otra fórmula el estado hubiera quedado inconsistente con lo que el código produce. Además, des-mezclar un WAC sobre balance negativo con `costPerUnit` negativo propaga la corrupción en vez de aislarla. Invariante a preservar siempre: `totalValue == quantity × avgCost` y `ΣENTRADA - ΣSALIDA == doc.quantity`.
- **(v6.58.0) Borrado físico > movimiento compensatorio cuando el movimiento NO es un evento de negocio.** El kardex es append-only para eventos reales; estos 4 movimientos eran artefactos de docs borrados sin revertir, no devoluciones. Un compensatorio habría registrado una devolución que nunca ocurrió y dejado 12 movimientos donde hubo 8.
- **(v6.54.0) Confirmar que un deploy ATERRIZÓ antes de correr runtime, no asumirlo por el flujo de commits.** Esta sesión saltó del commit directo al seed/runtime sin ejecutar `firebase deploy`, y el primer runtime en test corrió contra el código VIEJO (pre-fix) — se disfrazó de "el fix no funciona" hasta comparar `firebase-functions-hash` contra una función de control no tocada. Regla: tras "commit → deploy → runtime", verificar el deploy con evidencia (`functions:list --json`, hash/generation, o el `Successful update operation` crudo de la CLI) ANTES de sembrar o invocar nada.
- **(v6.54.0) Un veredicto "MISMATCH" de un script de verificación puede ser un bug del propio script, no del dato.** El restore-test de un backup dio `deepEqual:false` en los 6 docs; el diff campo-por-campo mostró que la función `normalize()` de comparación truncaba `_nanoseconds` al leer el JSON crudo (comparaba solo `_seconds*1000`), mientras el dato restaurado (Timestamp real) sí traía la precisión completa. El backup era correcto; el comparador estaba mal. Regla: ante un mismatch, diffear campo por campo antes de aceptar el veredicto.
- **(v6.54.0) Una observación de campo tipo "X dio SIN EFECTO" no es evidencia de que un fix ya esté andando si el deploy no aterrizó.** El caso "GGA1-005 dio SIN EFECTO" que motivó arrancar el runtime era un misread: con el código VIEJO desplegado (sin el guard NC), un money-only también debería haber sumado igual que cualquier otro. La explicación real nunca se investigó a fondo. No tratar una observación aislada como confirmación sin volver a mirar el mecanismo que la explicaría.
- **(v6.53.0) Antes de deployar un callable con dependencias NUEVAS, verificar que los `.js` esten en `functions/lib/`.** `tsc --noEmit` verde NO garantiza que el artefacto tenga todos los modulos: puede convivir con un `MODULE_NOT_FOUND` que revienta recien en runtime. Un `ls functions/lib/<ruta>.js` de 2 segundos lo descarta.
- **(v6.53.0) Un clasificador de errores atado a la PRESENCIA de un campo se rompe cuando el callable crece.** `parseAnnulError` decide `production-block` solo por `details.quotationId` — correcto para `annulSale` (un solo guard lo emite), incorrecto al reusarlo para `editQuotation` (dos guards lo emiten). El discriminante tiene que ser el campo que **solo** el caso objetivo produce (`activeLogIds` no vacio), no uno compartido con sus hermanos.
- **(v6.53.0) El builder canonico NO es idempotente en las flags** (`itemFlags` arranca de `rawItem.flags`). Nunca hacer build -> recomputar -> build: una flag `sin costo` de la 1a pasada sobrevive a la 2a aunque el costo ya sea > 0. Resolver los valores ANTES y hacer una sola pasada.
- **(v6.53.0) Escribir en `sales/` de prod dispara `onSaleCreated`** (trigger `onDocumentCreated`, `index.ts:224`), que escribe un `audit_logs` `SALE_CREATED`. Solo eso — no toca stock. Todo smoke que siembre en `sales` tiene que contar esos audit logs en su cleanup o quedan huerfanos.
- **(v6.53.0) Al refactorizar codigo LIVE sin tests, el RED de no-regresion se escribe como PARITY contra una copia textual del original.** Se copian las formulas viejas al test, se assertea igualdad, y recien con eso en verde se borra el inline. Es lo que se hizo con `computeCartTotals`/`addItemToCart` sobre el POS (`new/page.tsx`, sin tests propios).
- **(v6.52.1) Al cerrar un guard sobre un discriminante, enumerar TODOS sus valores y decidir explícitamente qué pasa con cada uno.** #9-B.2b cerró el bloqueo por producción activa para `mode: "linked"` y dejó `mode: "self"` abierto durante varias versiones, con el agujero **sin cobertura en ninguna capa** (ni el parity test ni los tests de integración tenían un solo caso `QUOTATION`). No alcanza con arreglar la rama que motivó el ticket.
- **(v6.52.1) `resolveSaleQuotationLink` clasifica como `mode: "self"` a TODA cotización, nativa E IMPORTADA** — el chequeo `status === "QUOTATION"` va primero y corta antes de mirar `relatedQuotationId`/`originQuoteId`. "Nativa vs importada" **no** decide el modo. Por eso el encuadre de este frente como "bug del path nativo" era incorrecto: en prod hay 0 nativas en `QUOTATION` y el 100% de la exposición eran las 130 perchas importadas.
- **(v6.52.1) Ante "¿qué docs movieron stock?", preferir allowlist POSITIVA sobre exclusión negativa.** `revertir solo si status === COMPLETED` y `no revertir si status === QUOTATION` son equivalentes HOY, pero degradan al revés: ante un status nuevo, la positiva no toca stock y la negativa sí. Misma familia que la lección de v6.51.0 sobre guards decorativos.
- **(v6.52.1) Un smoke de prod puede validar un fix de stock con footprint CERO sobre el stock real:** usar un SKU que no exista, de modo que un fix roto *crearía* el doc (la rama `tx.set` de `writeSaleReversal` cubre "snap no existe") y el assert sea simplemente "sigue sin existir". Y cuando el barrido completo es barato, verificar el universo entero (84/84) en vez de una muestra — sale gratis y es evidencia mucho más fuerte.
- **(v6.52.0) Antes de CADA `test:emu`, verificar que el puerto 8080 esté libre — no solo después de un RED.** `firebase emulators:exec` deja el emulador de Firestore huérfano; v6.51.0 lo registraba como exclusivo de corridas con exit ≠ 0, pero esta sesión quedó huérfano **también tras una corrida exit 0 con shutdown limpio en el log**. Mordió 3 veces. El síntoma (`Could not start Firestore Emulator, port taken`) **se disfraza de fallo de test**. Verificar SIEMPRE el `CommandLine` del PID (`Get-CimInstance Win32_Process -Filter "ProcessId=N"`) y confirmar que es `cloud-firestore-emulator-*.jar … --project_id ayrsteel-test` antes de matarlo — nunca a ciegas, puede ser algo del usuario.
- **(v6.52.0) Ante un `git merge --ff-only` que aborta por working tree sucio: verificar byte-identidad ANTES de asumir que hay cambios reales, y antes de forzar nada.** `git status` puede reportar archivos modificados que son **byte-idénticos al blob de HEAD** (CRLF commiteado vs `.gitattributes eol=lf`). Diagnóstico: `git diff --ignore-cr-at-eol --stat` vacío **+** `git hash-object --no-filters <file>` == `git rev-parse HEAD:<file>`. `git restore` NO los limpia. Pasó con 12 archivos al mergear este batch. **(v6.60.0) El método sigue vigente, pero el trigger específico ya no existe:** los 290 blobs CRLF se renormalizaron en `4e36435d`; si un `ff-only` vuelve a abortar por "sucio", primero `git ls-files --eol` para descartar que un `stash pop` o un clon con `autocrlf=true` haya reintroducido blobs CRLF.
- **(v6.52.0) Cuando un test de integración vive fuera del directorio que scopea el comando de test, no existe: nadie lo corre.** Los 14 rojos de `functions/` llevaban sesiones registrados como "4 rojos por ADC" porque `test:emu` scopea a `src/test/integration` y un `vitest run` pelado los corre **sin emulador**. Al agregar un test de integración, verificar que **algún comando del repo lo ejecute en condiciones** — si no, agregar el script (`test:emu:functions`), y agregarlo **aparte** si su `beforeEach` borra colecciones.
- **Merge a master NON-FAST-FORWARD por docs:** si `git merge develop` en master crea merge commit por divergencia de CLAUDE.md/GEMINI.md/HANDOFF.md, NO es conflicto de código. Tras `push origin master`, SIEMPRE sync-back: `git checkout develop && git merge master` (ff, absorbe el merge commit) + `git push origin develop`. Deja develop==master mismo hash → próximo merge ff limpio. Sin el sync-back el ciclo non-ff se repite cada frente.
- **Meta-nota de arranque de sesión:** primero `git fetch --all && git log origin/develop --since="1 day ago"` para detectar commits externos (GitHub Web u otro ejecutor) ANTES de codear. Aprendido cuando dos fixes idénticos (Update weight constants x2, fix Factura Desconocida) generaron trabajo duplicado + un force-push --force-with-lease.
- **Meta-nota de proceso:** Todo el código pasa por el ejecutor. GitHub Web ya NO se usa para tocar código directo (decisión del dueño 2026-08-14).
- **PARIDAD DE ENTORNOS:** ayrsteel-test y ayrsteel-2026 (prod) se mantienen A LA PAR siempre — índices Firestore, Cloud Functions, y ramas git (develop==master==origin). Todo deploy a prod (índice/función) o push se replica/verifica en test en la MISMA tanda. Verificar paridad al inicio de cada frente.
- **Scripts en Prod:** Todo script que lea/escriba prod imprime y ASSERTEA el projectId al arrancar (`if (projectId !== 'ayrsteel-2026') process.exit(1)`). Sin eso no corre.
- Ejecutor **PARA y espera OK** antes de tocar prod (no ejecutar-y-reportar).
- **NUNCA script que reimplemente callable** saltándose guards contra prod.
- **Guard laterSales NO se toca.**
- Verificar auth Antigravity a ambos proyectos al inicio.
- Sincronizar `develop` con `master` al arrancar.
- "compila/type-check" ≠ GREEN (los tests DEBEN correr con emulador).
- Densidad de `coil_finishes` **nunca** hardcodeada.
- **Deploy de Functions:** Usar el prefijo `functions:` explícito en todos los targets del `--only` para evitar omisiones silenciosas. Validar con `functions:list` post-deploy.
- **Lotes y Operaciones Destructivas:** Usar siempre un GATE entre lotes. Correr 1 lote, reportar, esperar OK. NUNCA correr de corrido.
- **Backups de purgas:** Hacer el backup fuera de workspace, y obligatoriamente probar restaurarlo en `test` mapeando `_id -> doc.id` antes de purgar en `prod`.
- **La colección `quotations` NO existe:** cotizaciones viven en `sales`. DOS orígenes distintos, no uno: perchas IMPORTADAS con prefijo `COT-` en doc.id (dedup por existencia de doc) Y cotizaciones NATIVAS con prefijo `C-` (sin `COT-`, `createQuotation` nunca genera `COT-*`). Consecuencia (confirmada en #9-A): traer "todas las cotizaciones" son 2 queries — range sobre `documentId()` para `COT-*` + `status in [...]` para nativas — no una sola filtrada por prefijo.
- **voidProductionFromCoils es fuego-y-olvido** con la cotización source: `if quoteSnap.exists` la protege — se puede voidear un log cuya cotización ya fue borrada sin fallar (solo pierde el reset de isFulfilled, que igual desaparece con el doc).
- **Timestamp de production_log = fecha del día**, no editable. Producciones contra cotizaciones con timestamp histórico quedan con fechas divergentes (venta Mar, log Ago). NO es bug, es UX.
- **Kardex es partida doble append-only** — ningún consumidor filtra por status/voided. Anulaciones se reflejan por movimientos compensatorios con `reference: logId`. Historial ruidoso pero contablemente correcto.
